[
  {
    "query": "Terraform: Teardown of BigQuery Dataset",
    "positive": "When running terraform destroy, the following error can occur:\n\n```Do you really want to destroy all resources?\n\nTerraform will destroy all your managed infrastructure, as shown above.\n\nThere is no undo. Only 'yes' will be accepted to confirm.\n\nEnter a value: yes\n\ngoogle_bigquery_dataset.homework_dataset: Destroying... [id=projects/terraform-demo-449214/datasets/homework_dataset]\n\n╷\n\n│ Error: Error when reading or editing Dataset: googleapi: Error 400: Dataset terraform-demo-449214:homework_dataset is still in use, resourceInUse\n```\n\nThis is because the dataset is still in use by a table. To delete the dataset, set the delete_contents_on_destroy property to true in the main.tf file.",
    "hard_negatives": [
      "Remove the dataset from BigQuery created by dbt and run again (with test disabled) to ensure the dataset created has all the rows.\n\nDBT: Why am I getting a new dataset after running my CI/CD Job? / What is this new dbt dataset in BigQuery?\n\nWhen you create the CI/CD job, under 'Compare Changes against an environment (Deferral)' make sure that you select 'No; do not defer to another environment'. Otherwise, dbt won’t merge your dev models into production models; it will create a new environment called ‘dbt_cloud_pr_number of pull request’",
      "1. Install the astronomer-cosmos package as a dependency. Refer to the installation guide here and see a Terraform example.\n\n2. Create a new folder, dbt/, inside the dags/ folder of your Composer GCP bucket and copy your dbt-core project there. See the example.\n\n3. Ensure your profiles.yml is configured to authenticate with a service account key. Refer to the BigQuery example.\n\n4. Create a new DAG using the DbtTaskGroup class. Use a ProfileConfig specifying a profiles_yml_filepath that points to the location of your JSON key file. See this example.\n\nYour dbt lineage graph should now appear as tasks inside a task group like this:",
      "Reference:\n\nhttps://cloud.google.com/bigquery/docs/external-data-cloud-storage\n\nSolution:\n\n```python\nfrom google.cloud import bigquery\n\n# Set table_id to the ID of the table to create\ntable_id = f\"{project_id}.{dataset_name}.{table_name}\"\n\n# Construct a BigQuery client object\nclient = bigquery.Client()\n\n# Set the external source format of your table\nexternal_source_format = \"PARQUET\"\n\n# Set the source_uris to point to your data in Google Cloud\nsource_uris = [f'gs://{bucket_name}/{object_key}/*']\n\n# Create ExternalConfig object with external source format\nexternal_config = bigquery.ExternalConfig(external_source_format)\n\n# Set source_uris that point to your data in Google Cloud\nexternal_config.source_uris = source_uris\nexternal_config.autodetect = True\n\ntable = bigquery.Table(table_id)\n\n# Set the external data configuration of the table\ntable.external_data_configuration = external_config\ntable = client.create_table(table)  # Make an API request.\n\nprint(f'Created table with external source: {table_id}')\nprint(f'Format: {table.external_data_configuration.source_format}')\n```",
      "See a list of datasets here: GitHub Datasets",
      "Kestra supports conditional branching within a single workflow to reuse shared orchestration logic while allowing dataset-specific behavior. This approach reduces duplication, keeps related steps together, and makes execution paths explicit within a single workflow definition.\n\nWhen to use a single-workflow branch:\n- Reuse common steps (e.g., data validation, enrichment) across datasets.\n- Implement dataset-specific paths (e.g., Yellow vs Green taxi) within the same flow.\n\nHow to implement:\n- Use a branching mechanism in Kestra (e.g., a condition or switch node) to evaluate a runtime variable (such as the dataset type) and route to the appropriate sub-path.\n- Place shared steps before the branch; place dataset-specific steps inside each branch.\n- Decide how branches converge: continue with a common tail after the branch or treat branches as separate end states.\n\nBest practices:\n- Keep branches modular and minimal; extract shared logic into subflows where possible.\n- Centralize error handling and logging to ensure consistency across branches.\n- Test each branch with representative inputs and monitor branch-specific failures separately.\n\nExample (illustrative pseudo YAML):\n```- id: route_by_dataset\n  type: branch\n  when:\n    - condition: ${workflow.variables.dataset_type == 'yellow'}\n      then:\n        - - id: yellow_tasks\n            tasks: [ yellow_validate, yellow_transform ]\n    - else:\n        - - id: green_tasks\n            tasks: [ green_validate, green_transform ]\n- id: end\n  type: terminate\n```\n\nNote: Adapt the exact branch/task names to your Kestra workflow definitions. The key idea is to place shared logic before the branch, branch on dataset/type, and then execute dataset-specific steps within each branch."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "Python: Ingestion with Jupyter notebook - missing 100000 records",
    "positive": "If you follow the video 1.2.2 - Ingesting NY Taxi Data to Postgres and execute the same steps, you will ingest all the data (~1.3 million rows) into the table yellow_taxi_data. However, running the whole script in the Jupyter notebook for a second time from top to bottom will result in missing the first chunk of 100,000 records. This occurs because a call to the iterator appears before the while loop, leading to the second chunk being ingested first.\n\n- Remove the cell df=next(df_iter) located higher up in the notebook than the while loop.\n- Ensure the first w(df_iter) call is within the while loop.\n\n📔 Note: The notebook is used to test the code and is not intended to be run top to bottom. The logic is organized in a later step when inserted into a .py file for the pipeline.",
    "hard_negatives": [
      "To read from multiple topics in the same Spark session, follow these steps:\n\n1. Initiate a Spark Session:\n   \n   ```python\nspark = (SparkSession\n       .builder\n       .appName(app_name)\n       .master(master=master)\n       .getOrCreate())\n   \n   spark.streams.resetTerminated()\n```\n\n2. Read Streams from Multiple Topics:\n   \n   ```python\nquery1 = spark\n       .readStream\n       ...\n       ...\n       .load()\n   \n   query2 = spark\n       .readStream\n       ...\n       ...\n       .load()\n   \n   query3 = spark\n       .readStream\n       ...\n       ...\n       .load()\n```\n\n3. Start the Queries:\n   \n   ```python\nquery1.start()\n   query2.start()\n   query3.start()\n```\n\n4. Await Termination:\n   \n   ```python\nspark.streams.awaitAnyTermination()  # Waits for any one of the queries to receive a kill signal or error failure. This is asynchronous.\n```\n\n   Note: query3.start().awaitTermination() is a blocking call. It works well when we are reading only from one topic.",
      "When you log into PgAdmin and see an empty database, the following solution can help:\n\nRun:\n   \n```bash\ndocker-compose up\n```\n\nAnd at the same time run:\n\n```bash\ndocker build -t taxi_ingest:v001 .\n\n# NETWORK NAME IS THE SAME AS THAT CREATED BY DOCKER COMPOSE\ndocker run -it \\\n  --network=pg-network \\\n  taxi_ingest:v001 \\\n  --user=postgres \\\n  --password=postgres \\\n  --host=db \\\n  --port=5432 \\\n  --db=ny_taxi \\\n  --table_name=green_tripdata \\\n  --url=${URL}\n```\n\nIt's important to use the same --network as stated in the docker-compose.yaml file.\n\nThe docker-compose.yaml file might not specify a network, as shown below:\n\n```yaml\nservices:\n  db:\n    container_name: postgres\n    image: postgres:17-alpine\n    environment:\n      ...\n    ports:\n      - '5433:5432'\n    volumes:\n      - ...\n  pgadmin:\n    container_name: pgadmin\n    image: dpage/pgadmin4:latest\n    environment:\n      ...\n    ports:\n      - '8080:80'\n    volumes:\n      - ...\n\nvolumes:\n  vol-pgdata:\n    name: vol-pgdata\n  vol-pgadmin_data:\n    name: vol-pgadmin_data\n```\n\nIf the network name is not specified, it is generated automatically: The name of the directory containing the docker-compose.yaml file in lowercase + _default.\n\nYou can find the network’s name when running docker-compose up:\n\n```pg-database Pulling pg-database Pulled \nNetwork week_1_default  Creating\nNetwork week_1_default  Created\n```",
      "The issue comes down to how Unix processes produce output and how Kestra interprets it:\n\n1. Python's logging module writes to stderr by default. If you, for instance, call logging.basicConfig() without specifying a stream argument, the root handler sends basically everything to stderr.\n\n2. Kestra maps the two standard streams to its own log levels. Anything the container writes to stdout becomes a Kestra DEBUG entry and anything written on stderr becomes ERROR. There is no middle ground.\n\nThe fix\n\nGood news is that the fix is simple: redirect Python logging to stdout:\n\n```python\nimport sys\nimport logging\n\nlogging.basicConfig(\n    level=logging.INFO,\n    format=\"%(asctime)s %(levelname)s %(message)s\",\n    stream=sys.stdout,\n)\n```\n\nThat single stream=sys.stdout argument is enough. After the change, your informational messages will show up as DEBUG in Kestra.",
      "See a list of datasets here: GitHub Datasets",
      "Reference:\n\nhttps://cloud.google.com/bigquery/docs/external-data-cloud-storage\n\nSolution:\n\n```python\nfrom google.cloud import bigquery\n\n# Set table_id to the ID of the table to create\ntable_id = f\"{project_id}.{dataset_name}.{table_name}\"\n\n# Construct a BigQuery client object\nclient = bigquery.Client()\n\n# Set the external source format of your table\nexternal_source_format = \"PARQUET\"\n\n# Set the source_uris to point to your data in Google Cloud\nsource_uris = [f'gs://{bucket_name}/{object_key}/*']\n\n# Create ExternalConfig object with external source format\nexternal_config = bigquery.ExternalConfig(external_source_format)\n\n# Set source_uris that point to your data in Google Cloud\nexternal_config.source_uris = source_uris\nexternal_config.autodetect = True\n\ntable = bigquery.Table(table_id)\n\n# Set the external data configuration of the table\ntable.external_data_configuration = external_config\ntable = client.create_table(table)  # Make an API request.\n\nprint(f'Created table with external source: {table_id}')\nprint(f'Format: {table.external_data_configuration.source_format}')\n```"
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "Docker-Compose: PgAdmin – no database in PgAdmin",
    "positive": "When you log into PgAdmin and see an empty database, the following solution can help:\n\nRun:\n   \n```bash\ndocker-compose up\n```\n\nAnd at the same time run:\n\n```bash\ndocker build -t taxi_ingest:v001 .\n\n# NETWORK NAME IS THE SAME AS THAT CREATED BY DOCKER COMPOSE\ndocker run -it \\\n  --network=pg-network \\\n  taxi_ingest:v001 \\\n  --user=postgres \\\n  --password=postgres \\\n  --host=db \\\n  --port=5432 \\\n  --db=ny_taxi \\\n  --table_name=green_tripdata \\\n  --url=${URL}\n```\n\nIt's important to use the same --network as stated in the docker-compose.yaml file.\n\nThe docker-compose.yaml file might not specify a network, as shown below:\n\n```yaml\nservices:\n  db:\n    container_name: postgres\n    image: postgres:17-alpine\n    environment:\n      ...\n    ports:\n      - '5433:5432'\n    volumes:\n      - ...\n  pgadmin:\n    container_name: pgadmin\n    image: dpage/pgadmin4:latest\n    environment:\n      ...\n    ports:\n      - '8080:80'\n    volumes:\n      - ...\n\nvolumes:\n  vol-pgdata:\n    name: vol-pgdata\n  vol-pgadmin_data:\n    name: vol-pgadmin_data\n```\n\nIf the network name is not specified, it is generated automatically: The name of the directory containing the docker-compose.yaml file in lowercase + _default.\n\nYou can find the network’s name when running docker-compose up:\n\n```pg-database Pulling pg-database Pulled \nNetwork week_1_default  Creating\nNetwork week_1_default  Created\n```",
    "hard_negatives": [
      "When attempting to start the Docker Postgres container, you may encounter the error message:\n\n```Error - postgres port is already in use.\n```\n\nOption 1: Identify and Stop the Service\n\n1. Determine which service is using the port by running:\n   \n   ```bash\nsudo lsof -i :5432\n```\n   \n2. Stop the service that is using the port:\n   \n   ```bash\nsudo service postgresql stop\n```\n\nOption 2: Map to a Different Port\n\nFor a more long-term solution, consider mapping to a different port:\n\n- Map local port 5433 to container port 5432 in your Docker configuration (Dockerfile or docker-compose.yml).\n- If using a VM, ensure that port 5433 is forwarded in the host machine configuration.\n\nThis approach prevents conflicts and allows the Docker Postgres container to run without interruption.",
      "You might have installed Docker via snap. Run the following command to verify:\n\n```bash\nsudo snap status docker\n```\n\nIf you receive the response:\n\n```error: unknown command \"status\", see 'snap help'.\n```\n\nThen uninstall Docker and install it via the official website.\n\nError message: \"Bind for 0.0.0.0:5432 failed: port is already allocated.\"",
      "If you are using MS VS Code and running gcloud in WSL2, when you first try to login to GCP via the gcloud CLI with gcloud auth application-default login, you may encounter an issue where the terminal prints a long OAuth URL and then shows a series of \"not found\" errors for browsers:\n\n```Your browser has been opened to visit:\n    https://accounts.google.com/o/oauth2/auth?response_type=code&client_id=...\n\n/usr/bin/xdg-open: 882: x-www-browser: not found\n/usr/bin/xdg-open: 882: firefox: not found\n/usr/bin/xdg-open: 882: chromium: not found\n...\nxdg-open: no method available for opening '...'\n```\n\nVS Code may show a notification: \"Your application running on port 8085 is available\" with an \"Open in Browser\" button. Clicking it may lead to an error page.\n\nSolution:\n\n1. Hover over the long OAuth URL in the terminal output.\n2. Ctrl + Click the link — VS Code will show a dialog: \"Do you want Code to open the external website?\" with buttons Open, Copy, Configure Trusted Domains, and Cancel.\n3. Click Configure Trusted Domains.\n4. A dropdown will appear with options like:\n   - \"Trust https://accounts.google.com\"\n   - \"Trust google.com and all its subdomains\"\n   - \"Trust all domains (disables link protection)\"\n   - \"Manage Trusted Domains\"\n5. Pick the first or second entry (e.g., \"Trust https://accounts.google.com\").\n\nNext time you run gcloud auth, the login page should pop up via the default browser without issues.",
      "See a list of datasets here: GitHub Datasets",
      "Reference:\n\nhttps://cloud.google.com/bigquery/docs/external-data-cloud-storage\n\nSolution:\n\n```python\nfrom google.cloud import bigquery\n\n# Set table_id to the ID of the table to create\ntable_id = f\"{project_id}.{dataset_name}.{table_name}\"\n\n# Construct a BigQuery client object\nclient = bigquery.Client()\n\n# Set the external source format of your table\nexternal_source_format = \"PARQUET\"\n\n# Set the source_uris to point to your data in Google Cloud\nsource_uris = [f'gs://{bucket_name}/{object_key}/*']\n\n# Create ExternalConfig object with external source format\nexternal_config = bigquery.ExternalConfig(external_source_format)\n\n# Set source_uris that point to your data in Google Cloud\nexternal_config.source_uris = source_uris\nexternal_config.autodetect = True\n\ntable = bigquery.Table(table_id)\n\n# Set the external data configuration of the table\ntable.external_data_configuration = external_config\ntable = client.create_table(table)  # Make an API request.\n\nprint(f'Created table with external source: {table_id}')\nprint(f'Format: {table.external_data_configuration.source_format}')\n```"
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "How can we contribute to the course?",
    "positive": "The Faust library is no longer maintained — see https://github.com/robinhood/faust. We don't recommend using it for the course.\n\nIf you don't know Java, follow the Python streaming materials in the course repo (under 06-streaming/python/) instead, which use Kafka or Redpanda directly. Watching the Java videos to understand the streaming concepts is still useful even if you skip the coding parts.",
    "hard_negatives": [
      "You don't need a confirmation email. You're accepted. You can start learning and submitting homework without registering. Registration was just to gauge interest before the start date.",
      "Deadlines are published per cohort. Find them in:\n\n- The current cohort's folder in the course repo.\n- The course website (link is in the cohort folder's README).\n- The course Google Calendar (also linked from the cohort folder).\n\nAlso keep an eye on @Au-Tomator posts in Slack for extension announcements. The submission form may show an updated deadline if instructors have changed it.",
      "Kestra supports conditional branching within a single workflow to reuse shared orchestration logic while allowing dataset-specific behavior. This approach reduces duplication, keeps related steps together, and makes execution paths explicit within a single workflow definition.\n\nWhen to use a single-workflow branch:\n- Reuse common steps (e.g., data validation, enrichment) across datasets.\n- Implement dataset-specific paths (e.g., Yellow vs Green taxi) within the same flow.\n\nHow to implement:\n- Use a branching mechanism in Kestra (e.g., a condition or switch node) to evaluate a runtime variable (such as the dataset type) and route to the appropriate sub-path.\n- Place shared steps before the branch; place dataset-specific steps inside each branch.\n- Decide how branches converge: continue with a common tail after the branch or treat branches as separate end states.\n\nBest practices:\n- Keep branches modular and minimal; extract shared logic into subflows where possible.\n- Centralize error handling and logging to ensure consistency across branches.\n- Test each branch with representative inputs and monitor branch-specific failures separately.\n\nExample (illustrative pseudo YAML):\n```- id: route_by_dataset\n  type: branch\n  when:\n    - condition: ${workflow.variables.dataset_type == 'yellow'}\n      then:\n        - - id: yellow_tasks\n            tasks: [ yellow_validate, yellow_transform ]\n    - else:\n        - - id: green_tasks\n            tasks: [ green_validate, green_transform ]\n- id: end\n  type: terminate\n```\n\nNote: Adapt the exact branch/task names to your Kestra workflow definitions. The key idea is to place shared logic before the branch, branch on dataset/type, and then execute dataset-specific steps within each branch.",
      "To read from multiple topics in the same Spark session, follow these steps:\n\n1. Initiate a Spark Session:\n   \n   ```python\nspark = (SparkSession\n       .builder\n       .appName(app_name)\n       .master(master=master)\n       .getOrCreate())\n   \n   spark.streams.resetTerminated()\n```\n\n2. Read Streams from Multiple Topics:\n   \n   ```python\nquery1 = spark\n       .readStream\n       ...\n       ...\n       .load()\n   \n   query2 = spark\n       .readStream\n       ...\n       ...\n       .load()\n   \n   query3 = spark\n       .readStream\n       ...\n       ...\n       .load()\n```\n\n3. Start the Queries:\n   \n   ```python\nquery1.start()\n   query2.start()\n   query3.start()\n```\n\n4. Await Termination:\n   \n   ```python\nspark.streams.awaitAnyTermination()  # Waits for any one of the queries to receive a kill signal or error failure. This is asynchronous.\n```\n\n   Note: query3.start().awaitTermination() is a blocking call. It works well when we are reading only from one topic.",
      "When you log into PgAdmin and see an empty database, the following solution can help:\n\nRun:\n   \n```bash\ndocker-compose up\n```\n\nAnd at the same time run:\n\n```bash\ndocker build -t taxi_ingest:v001 .\n\n# NETWORK NAME IS THE SAME AS THAT CREATED BY DOCKER COMPOSE\ndocker run -it \\\n  --network=pg-network \\\n  taxi_ingest:v001 \\\n  --user=postgres \\\n  --password=postgres \\\n  --host=db \\\n  --port=5432 \\\n  --db=ny_taxi \\\n  --table_name=green_tripdata \\\n  --url=${URL}\n```\n\nIt's important to use the same --network as stated in the docker-compose.yaml file.\n\nThe docker-compose.yaml file might not specify a network, as shown below:\n\n```yaml\nservices:\n  db:\n    container_name: postgres\n    image: postgres:17-alpine\n    environment:\n      ...\n    ports:\n      - '5433:5432'\n    volumes:\n      - ...\n  pgadmin:\n    container_name: pgadmin\n    image: dpage/pgadmin4:latest\n    environment:\n      ...\n    ports:\n      - '8080:80'\n    volumes:\n      - ...\n\nvolumes:\n  vol-pgdata:\n    name: vol-pgdata\n  vol-pgadmin_data:\n    name: vol-pgadmin_data\n```\n\nIf the network name is not specified, it is generated automatically: The name of the directory containing the docker-compose.yaml file in lowercase + _default.\n\nYou can find the network’s name when running docker-compose up:\n\n```pg-database Pulling pg-database Pulled \nNetwork week_1_default  Creating\nNetwork week_1_default  Created\n```"
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "Inconsistent number of rows when re-running fact_trips model",
    "positive": "This issue arises from the way deduplication is handled in two staging files.\n\nSolution:\n\n- Add an ORDER BY clause in the PARTITION BY section of both staging files.\n- Continue adding columns to the ORDER BY clause until the row count in the fact_trips table is consistent upon re-running the model.\n\nExplanation:\n\nWe partition by vendor_id and pickup_datetime, selecting the first row (rn=1) from these partitions. These partitions lack an order, so every execution might yield a different first row. The inconsistency leads to different rows being processed, possibly with or without an unknown borough. Consequently, the fact_trips model discards a varying number of rows based on the presence of unknown boroughs.",
    "hard_negatives": [
      "If you follow the video 1.2.2 - Ingesting NY Taxi Data to Postgres and execute the same steps, you will ingest all the data (~1.3 million rows) into the table yellow_taxi_data. However, running the whole script in the Jupyter notebook for a second time from top to bottom will result in missing the first chunk of 100,000 records. This occurs because a call to the iterator appears before the while loop, leading to the second chunk being ingested first.\n\n- Remove the cell df=next(df_iter) located higher up in the notebook than the while loop.\n- Ensure the first w(df_iter) call is within the while loop.\n\n📔 Note: The notebook is used to test the code and is not intended to be run top to bottom. The logic is organized in a later step when inserted into a .py file for the pipeline.",
      "Remove the dataset from BigQuery created by dbt and run again (with test disabled) to ensure the dataset created has all the rows.\n\nDBT: Why am I getting a new dataset after running my CI/CD Job? / What is this new dbt dataset in BigQuery?\n\nWhen you create the CI/CD job, under 'Compare Changes against an environment (Deferral)' make sure that you select 'No; do not defer to another environment'. Otherwise, dbt won’t merge your dev models into production models; it will create a new environment called ‘dbt_cloud_pr_number of pull request’",
      "Spark processes data in partitions. When you write a DataFrame to disk, Spark writes each partition as a separate output file. For example:\n\n```python\ntrips.repartition(4).write.parquet(\"output/\")\n```\n\nThis creates four parquet files because the DataFrame now has four partitions. This behavior enables Spark to write data in parallel and can improve performance on large datasets.",
      "Kestra supports conditional branching within a single workflow to reuse shared orchestration logic while allowing dataset-specific behavior. This approach reduces duplication, keeps related steps together, and makes execution paths explicit within a single workflow definition.\n\nWhen to use a single-workflow branch:\n- Reuse common steps (e.g., data validation, enrichment) across datasets.\n- Implement dataset-specific paths (e.g., Yellow vs Green taxi) within the same flow.\n\nHow to implement:\n- Use a branching mechanism in Kestra (e.g., a condition or switch node) to evaluate a runtime variable (such as the dataset type) and route to the appropriate sub-path.\n- Place shared steps before the branch; place dataset-specific steps inside each branch.\n- Decide how branches converge: continue with a common tail after the branch or treat branches as separate end states.\n\nBest practices:\n- Keep branches modular and minimal; extract shared logic into subflows where possible.\n- Centralize error handling and logging to ensure consistency across branches.\n- Test each branch with representative inputs and monitor branch-specific failures separately.\n\nExample (illustrative pseudo YAML):\n```- id: route_by_dataset\n  type: branch\n  when:\n    - condition: ${workflow.variables.dataset_type == 'yellow'}\n      then:\n        - - id: yellow_tasks\n            tasks: [ yellow_validate, yellow_transform ]\n    - else:\n        - - id: green_tasks\n            tasks: [ green_validate, green_transform ]\n- id: end\n  type: terminate\n```\n\nNote: Adapt the exact branch/task names to your Kestra workflow definitions. The key idea is to place shared logic before the branch, branch on dataset/type, and then execute dataset-specific steps within each branch.",
      "When you log into PgAdmin and see an empty database, the following solution can help:\n\nRun:\n   \n```bash\ndocker-compose up\n```\n\nAnd at the same time run:\n\n```bash\ndocker build -t taxi_ingest:v001 .\n\n# NETWORK NAME IS THE SAME AS THAT CREATED BY DOCKER COMPOSE\ndocker run -it \\\n  --network=pg-network \\\n  taxi_ingest:v001 \\\n  --user=postgres \\\n  --password=postgres \\\n  --host=db \\\n  --port=5432 \\\n  --db=ny_taxi \\\n  --table_name=green_tripdata \\\n  --url=${URL}\n```\n\nIt's important to use the same --network as stated in the docker-compose.yaml file.\n\nThe docker-compose.yaml file might not specify a network, as shown below:\n\n```yaml\nservices:\n  db:\n    container_name: postgres\n    image: postgres:17-alpine\n    environment:\n      ...\n    ports:\n      - '5433:5432'\n    volumes:\n      - ...\n  pgadmin:\n    container_name: pgadmin\n    image: dpage/pgadmin4:latest\n    environment:\n      ...\n    ports:\n      - '8080:80'\n    volumes:\n      - ...\n\nvolumes:\n  vol-pgdata:\n    name: vol-pgdata\n  vol-pgadmin_data:\n    name: vol-pgadmin_data\n```\n\nIf the network name is not specified, it is generated automatically: The name of the directory containing the docker-compose.yaml file in lowercase + _default.\n\nYou can find the network’s name when running docker-compose up:\n\n```pg-database Pulling pg-database Pulled \nNetwork week_1_default  Creating\nNetwork week_1_default  Created\n```"
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "Docker: ERRO[0000] error waiting for container: context canceled",
    "positive": "When attempting to start the Docker Postgres container, you may encounter the error message:\n\n```Error - postgres port is already in use.\n```\n\nOption 1: Identify and Stop the Service\n\n1. Determine which service is using the port by running:\n   \n   ```bash\nsudo lsof -i :5432\n```\n   \n2. Stop the service that is using the port:\n   \n   ```bash\nsudo service postgresql stop\n```\n\nOption 2: Map to a Different Port\n\nFor a more long-term solution, consider mapping to a different port:\n\n- Map local port 5433 to container port 5432 in your Docker configuration (Dockerfile or docker-compose.yml).\n- If using a VM, ensure that port 5433 is forwarded in the host machine configuration.\n\nThis approach prevents conflicts and allows the Docker Postgres container to run without interruption.",
    "hard_negatives": [
      "You might have installed Docker via snap. Run the following command to verify:\n\n```bash\nsudo snap status docker\n```\n\nIf you receive the response:\n\n```error: unknown command \"status\", see 'snap help'.\n```\n\nThen uninstall Docker and install it via the official website.\n\nError message: \"Bind for 0.0.0.0:5432 failed: port is already allocated.\"",
      "When you log into PgAdmin and see an empty database, the following solution can help:\n\nRun:\n   \n```bash\ndocker-compose up\n```\n\nAnd at the same time run:\n\n```bash\ndocker build -t taxi_ingest:v001 .\n\n# NETWORK NAME IS THE SAME AS THAT CREATED BY DOCKER COMPOSE\ndocker run -it \\\n  --network=pg-network \\\n  taxi_ingest:v001 \\\n  --user=postgres \\\n  --password=postgres \\\n  --host=db \\\n  --port=5432 \\\n  --db=ny_taxi \\\n  --table_name=green_tripdata \\\n  --url=${URL}\n```\n\nIt's important to use the same --network as stated in the docker-compose.yaml file.\n\nThe docker-compose.yaml file might not specify a network, as shown below:\n\n```yaml\nservices:\n  db:\n    container_name: postgres\n    image: postgres:17-alpine\n    environment:\n      ...\n    ports:\n      - '5433:5432'\n    volumes:\n      - ...\n  pgadmin:\n    container_name: pgadmin\n    image: dpage/pgadmin4:latest\n    environment:\n      ...\n    ports:\n      - '8080:80'\n    volumes:\n      - ...\n\nvolumes:\n  vol-pgdata:\n    name: vol-pgdata\n  vol-pgadmin_data:\n    name: vol-pgadmin_data\n```\n\nIf the network name is not specified, it is generated automatically: The name of the directory containing the docker-compose.yaml file in lowercase + _default.\n\nYou can find the network’s name when running docker-compose up:\n\n```pg-database Pulling pg-database Pulled \nNetwork week_1_default  Creating\nNetwork week_1_default  Created\n```",
      "The issue comes down to how Unix processes produce output and how Kestra interprets it:\n\n1. Python's logging module writes to stderr by default. If you, for instance, call logging.basicConfig() without specifying a stream argument, the root handler sends basically everything to stderr.\n\n2. Kestra maps the two standard streams to its own log levels. Anything the container writes to stdout becomes a Kestra DEBUG entry and anything written on stderr becomes ERROR. There is no middle ground.\n\nThe fix\n\nGood news is that the fix is simple: redirect Python logging to stdout:\n\n```python\nimport sys\nimport logging\n\nlogging.basicConfig(\n    level=logging.INFO,\n    format=\"%(asctime)s %(levelname)s %(message)s\",\n    stream=sys.stdout,\n)\n```\n\nThat single stream=sys.stdout argument is enough. After the change, your informational messages will show up as DEBUG in Kestra.",
      "If you are using MS VS Code and running gcloud in WSL2, when you first try to login to GCP via the gcloud CLI with gcloud auth application-default login, you may encounter an issue where the terminal prints a long OAuth URL and then shows a series of \"not found\" errors for browsers:\n\n```Your browser has been opened to visit:\n    https://accounts.google.com/o/oauth2/auth?response_type=code&client_id=...\n\n/usr/bin/xdg-open: 882: x-www-browser: not found\n/usr/bin/xdg-open: 882: firefox: not found\n/usr/bin/xdg-open: 882: chromium: not found\n...\nxdg-open: no method available for opening '...'\n```\n\nVS Code may show a notification: \"Your application running on port 8085 is available\" with an \"Open in Browser\" button. Clicking it may lead to an error page.\n\nSolution:\n\n1. Hover over the long OAuth URL in the terminal output.\n2. Ctrl + Click the link — VS Code will show a dialog: \"Do you want Code to open the external website?\" with buttons Open, Copy, Configure Trusted Domains, and Cancel.\n3. Click Configure Trusted Domains.\n4. A dropdown will appear with options like:\n   - \"Trust https://accounts.google.com\"\n   - \"Trust google.com and all its subdomains\"\n   - \"Trust all domains (disables link protection)\"\n   - \"Manage Trusted Domains\"\n5. Pick the first or second entry (e.g., \"Trust https://accounts.google.com\").\n\nNext time you run gcloud auth, the login page should pop up via the default browser without issues.",
      "When running terraform destroy, the following error can occur:\n\n```Do you really want to destroy all resources?\n\nTerraform will destroy all your managed infrastructure, as shown above.\n\nThere is no undo. Only 'yes' will be accepted to confirm.\n\nEnter a value: yes\n\ngoogle_bigquery_dataset.homework_dataset: Destroying... [id=projects/terraform-demo-449214/datasets/homework_dataset]\n\n╷\n\n│ Error: Error when reading or editing Dataset: googleapi: Error 400: Dataset terraform-demo-449214:homework_dataset is still in use, resourceInUse\n```\n\nThis is because the dataset is still in use by a table. To delete the dataset, set the delete_contents_on_destroy property to true in the main.tf file."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "Spark Streaming: How do I read from multiple topics in the same Spark Session",
    "positive": "To read from multiple topics in the same Spark session, follow these steps:\n\n1. Initiate a Spark Session:\n   \n   ```python\nspark = (SparkSession\n       .builder\n       .appName(app_name)\n       .master(master=master)\n       .getOrCreate())\n   \n   spark.streams.resetTerminated()\n```\n\n2. Read Streams from Multiple Topics:\n   \n   ```python\nquery1 = spark\n       .readStream\n       ...\n       ...\n       .load()\n   \n   query2 = spark\n       .readStream\n       ...\n       ...\n       .load()\n   \n   query3 = spark\n       .readStream\n       ...\n       ...\n       .load()\n```\n\n3. Start the Queries:\n   \n   ```python\nquery1.start()\n   query2.start()\n   query3.start()\n```\n\n4. Await Termination:\n   \n   ```python\nspark.streams.awaitAnyTermination()  # Waits for any one of the queries to receive a kill signal or error failure. This is asynchronous.\n```\n\n   Note: query3.start().awaitTermination() is a blocking call. It works well when we are reading only from one topic.",
    "hard_negatives": [
      "Spark processes data in partitions. When you write a DataFrame to disk, Spark writes each partition as a separate output file. For example:\n\n```python\ntrips.repartition(4).write.parquet(\"output/\")\n```\n\nThis creates four parquet files because the DataFrame now has four partitions. This behavior enables Spark to write data in parallel and can improve performance on large datasets.",
      "The Faust library is no longer maintained — see https://github.com/robinhood/faust. We don't recommend using it for the course.\n\nIf you don't know Java, follow the Python streaming materials in the course repo (under 06-streaming/python/) instead, which use Kafka or Redpanda directly. Watching the Java videos to understand the streaming concepts is still useful even if you skip the coding parts.",
      "The issue comes down to how Unix processes produce output and how Kestra interprets it:\n\n1. Python's logging module writes to stderr by default. If you, for instance, call logging.basicConfig() without specifying a stream argument, the root handler sends basically everything to stderr.\n\n2. Kestra maps the two standard streams to its own log levels. Anything the container writes to stdout becomes a Kestra DEBUG entry and anything written on stderr becomes ERROR. There is no middle ground.\n\nThe fix\n\nGood news is that the fix is simple: redirect Python logging to stdout:\n\n```python\nimport sys\nimport logging\n\nlogging.basicConfig(\n    level=logging.INFO,\n    format=\"%(asctime)s %(levelname)s %(message)s\",\n    stream=sys.stdout,\n)\n```\n\nThat single stream=sys.stdout argument is enough. After the change, your informational messages will show up as DEBUG in Kestra.",
      "Kestra supports conditional branching within a single workflow to reuse shared orchestration logic while allowing dataset-specific behavior. This approach reduces duplication, keeps related steps together, and makes execution paths explicit within a single workflow definition.\n\nWhen to use a single-workflow branch:\n- Reuse common steps (e.g., data validation, enrichment) across datasets.\n- Implement dataset-specific paths (e.g., Yellow vs Green taxi) within the same flow.\n\nHow to implement:\n- Use a branching mechanism in Kestra (e.g., a condition or switch node) to evaluate a runtime variable (such as the dataset type) and route to the appropriate sub-path.\n- Place shared steps before the branch; place dataset-specific steps inside each branch.\n- Decide how branches converge: continue with a common tail after the branch or treat branches as separate end states.\n\nBest practices:\n- Keep branches modular and minimal; extract shared logic into subflows where possible.\n- Centralize error handling and logging to ensure consistency across branches.\n- Test each branch with representative inputs and monitor branch-specific failures separately.\n\nExample (illustrative pseudo YAML):\n```- id: route_by_dataset\n  type: branch\n  when:\n    - condition: ${workflow.variables.dataset_type == 'yellow'}\n      then:\n        - - id: yellow_tasks\n            tasks: [ yellow_validate, yellow_transform ]\n    - else:\n        - - id: green_tasks\n            tasks: [ green_validate, green_transform ]\n- id: end\n  type: terminate\n```\n\nNote: Adapt the exact branch/task names to your Kestra workflow definitions. The key idea is to place shared logic before the branch, branch on dataset/type, and then execute dataset-specific steps within each branch.",
      "When you log into PgAdmin and see an empty database, the following solution can help:\n\nRun:\n   \n```bash\ndocker-compose up\n```\n\nAnd at the same time run:\n\n```bash\ndocker build -t taxi_ingest:v001 .\n\n# NETWORK NAME IS THE SAME AS THAT CREATED BY DOCKER COMPOSE\ndocker run -it \\\n  --network=pg-network \\\n  taxi_ingest:v001 \\\n  --user=postgres \\\n  --password=postgres \\\n  --host=db \\\n  --port=5432 \\\n  --db=ny_taxi \\\n  --table_name=green_tripdata \\\n  --url=${URL}\n```\n\nIt's important to use the same --network as stated in the docker-compose.yaml file.\n\nThe docker-compose.yaml file might not specify a network, as shown below:\n\n```yaml\nservices:\n  db:\n    container_name: postgres\n    image: postgres:17-alpine\n    environment:\n      ...\n    ports:\n      - '5433:5432'\n    volumes:\n      - ...\n  pgadmin:\n    container_name: pgadmin\n    image: dpage/pgadmin4:latest\n    environment:\n      ...\n    ports:\n      - '8080:80'\n    volumes:\n      - ...\n\nvolumes:\n  vol-pgdata:\n    name: vol-pgdata\n  vol-pgadmin_data:\n    name: vol-pgadmin_data\n```\n\nIf the network name is not specified, it is generated automatically: The name of the directory containing the docker-compose.yaml file in lowercase + _default.\n\nYou can find the network’s name when running docker-compose up:\n\n```pg-database Pulling pg-database Pulled \nNetwork week_1_default  Creating\nNetwork week_1_default  Created\n```"
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "How does dbt handle dependencies between models?",
    "positive": "Dbt provides a mechanism called ref to manage dependencies between models. By referencing other models using the ref keyword in SQL, dbt automatically understands the dependencies and ensures the correct execution order.",
    "hard_negatives": [
      "Remove the dataset from BigQuery created by dbt and run again (with test disabled) to ensure the dataset created has all the rows.\n\nDBT: Why am I getting a new dataset after running my CI/CD Job? / What is this new dbt dataset in BigQuery?\n\nWhen you create the CI/CD job, under 'Compare Changes against an environment (Deferral)' make sure that you select 'No; do not defer to another environment'. Otherwise, dbt won’t merge your dev models into production models; it will create a new environment called ‘dbt_cloud_pr_number of pull request’",
      "1. Install the astronomer-cosmos package as a dependency. Refer to the installation guide here and see a Terraform example.\n\n2. Create a new folder, dbt/, inside the dags/ folder of your Composer GCP bucket and copy your dbt-core project there. See the example.\n\n3. Ensure your profiles.yml is configured to authenticate with a service account key. Refer to the BigQuery example.\n\n4. Create a new DAG using the DbtTaskGroup class. Use a ProfileConfig specifying a profiles_yml_filepath that points to the location of your JSON key file. See this example.\n\nYour dbt lineage graph should now appear as tasks inside a task group like this:",
      "To enable CI jobs (running dbt on pull requests), dbt Cloud needs a native integration with GitHub, GitLab, or Azure DevOps — the generic \"Git Clone\" / SSH connection method does not unlock the \"Run on Pull Requests\" trigger and you'll see:\n\n```Triggered by pull requests\nThis feature is only available for dbt repositories connected through dbt Cloud's native integration with Github, Gitlab, or Azure DevOps\n```\n\nThe dbt Cloud Developer plan also doesn't support CI jobs — you need Team or Enterprise. (See the dbt Cloud CI prerequisites.)\n\nSwitch from Git Clone to native GitHub integration\n\n1. Connect your GitHub account in dbt Cloud: Profile Settings → Linked Accounts → connect GitHub and grant the requested permissions. (dbt docs)\n\n2. Disconnect the existing Git Clone connection: Account Settings → Projects → your project → Repository → Disconnect.\n\n3. Reconfigure with the GitHub option, selecting the repository.\n\n4. (If your dbt project lives in a subfolder of the repo) set Project Subdirectory in the project settings to point to that folder.\n\n5. In Deploy → Job → Configuration → Triggers, you should now see \"Run on Pull Requests\" as an available toggle.\n\nCI job runs but says \"valid dbt project was not found\"\n\nUsually one of:\n\n- Project is in a subfolder, but Project Subdirectory isn't set.\n- The CI environment is configured to run on a custom branch that doesn't exist.\n- Uncommitted/unpushed changes.\n\nIn Deploy → Environments → your environment → Settings, set \"Custom branch\" if you're not on the default branch, and confirm it matches a real branch in GitHub.",
      "This issue arises from the way deduplication is handled in two staging files.\n\nSolution:\n\n- Add an ORDER BY clause in the PARTITION BY section of both staging files.\n- Continue adding columns to the ORDER BY clause until the row count in the fact_trips table is consistent upon re-running the model.\n\nExplanation:\n\nWe partition by vendor_id and pickup_datetime, selecting the first row (rn=1) from these partitions. These partitions lack an order, so every execution might yield a different first row. The inconsistency leads to different rows being processed, possibly with or without an unknown borough. Consequently, the fact_trips model discards a varying number of rows based on the presence of unknown boroughs.",
      "Error:\n\n```plaintext\nThis project does not have a development environment configured. Please create a development environment and configure your development credentials to use the dbt IDE.\n```\n\nThe error message provides guidance on resolving this issue. Follow the guide in the dbt cloud setup documentation. Additional instructions can be found in the video @1:42."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "How to fix Python logs shown as Kestra error messages?",
    "positive": "The issue comes down to how Unix processes produce output and how Kestra interprets it:\n\n1. Python's logging module writes to stderr by default. If you, for instance, call logging.basicConfig() without specifying a stream argument, the root handler sends basically everything to stderr.\n\n2. Kestra maps the two standard streams to its own log levels. Anything the container writes to stdout becomes a Kestra DEBUG entry and anything written on stderr becomes ERROR. There is no middle ground.\n\nThe fix\n\nGood news is that the fix is simple: redirect Python logging to stdout:\n\n```python\nimport sys\nimport logging\n\nlogging.basicConfig(\n    level=logging.INFO,\n    format=\"%(asctime)s %(levelname)s %(message)s\",\n    stream=sys.stdout,\n)\n```\n\nThat single stream=sys.stdout argument is enough. After the change, your informational messages will show up as DEBUG in Kestra.",
    "hard_negatives": [
      "Kestra supports conditional branching within a single workflow to reuse shared orchestration logic while allowing dataset-specific behavior. This approach reduces duplication, keeps related steps together, and makes execution paths explicit within a single workflow definition.\n\nWhen to use a single-workflow branch:\n- Reuse common steps (e.g., data validation, enrichment) across datasets.\n- Implement dataset-specific paths (e.g., Yellow vs Green taxi) within the same flow.\n\nHow to implement:\n- Use a branching mechanism in Kestra (e.g., a condition or switch node) to evaluate a runtime variable (such as the dataset type) and route to the appropriate sub-path.\n- Place shared steps before the branch; place dataset-specific steps inside each branch.\n- Decide how branches converge: continue with a common tail after the branch or treat branches as separate end states.\n\nBest practices:\n- Keep branches modular and minimal; extract shared logic into subflows where possible.\n- Centralize error handling and logging to ensure consistency across branches.\n- Test each branch with representative inputs and monitor branch-specific failures separately.\n\nExample (illustrative pseudo YAML):\n```- id: route_by_dataset\n  type: branch\n  when:\n    - condition: ${workflow.variables.dataset_type == 'yellow'}\n      then:\n        - - id: yellow_tasks\n            tasks: [ yellow_validate, yellow_transform ]\n    - else:\n        - - id: green_tasks\n            tasks: [ green_validate, green_transform ]\n- id: end\n  type: terminate\n```\n\nNote: Adapt the exact branch/task names to your Kestra workflow definitions. The key idea is to place shared logic before the branch, branch on dataset/type, and then execute dataset-specific steps within each branch.",
      "To read from multiple topics in the same Spark session, follow these steps:\n\n1. Initiate a Spark Session:\n   \n   ```python\nspark = (SparkSession\n       .builder\n       .appName(app_name)\n       .master(master=master)\n       .getOrCreate())\n   \n   spark.streams.resetTerminated()\n```\n\n2. Read Streams from Multiple Topics:\n   \n   ```python\nquery1 = spark\n       .readStream\n       ...\n       ...\n       .load()\n   \n   query2 = spark\n       .readStream\n       ...\n       ...\n       .load()\n   \n   query3 = spark\n       .readStream\n       ...\n       ...\n       .load()\n```\n\n3. Start the Queries:\n   \n   ```python\nquery1.start()\n   query2.start()\n   query3.start()\n```\n\n4. Await Termination:\n   \n   ```python\nspark.streams.awaitAnyTermination()  # Waits for any one of the queries to receive a kill signal or error failure. This is asynchronous.\n```\n\n   Note: query3.start().awaitTermination() is a blocking call. It works well when we are reading only from one topic.",
      "If you are using MS VS Code and running gcloud in WSL2, when you first try to login to GCP via the gcloud CLI with gcloud auth application-default login, you may encounter an issue where the terminal prints a long OAuth URL and then shows a series of \"not found\" errors for browsers:\n\n```Your browser has been opened to visit:\n    https://accounts.google.com/o/oauth2/auth?response_type=code&client_id=...\n\n/usr/bin/xdg-open: 882: x-www-browser: not found\n/usr/bin/xdg-open: 882: firefox: not found\n/usr/bin/xdg-open: 882: chromium: not found\n...\nxdg-open: no method available for opening '...'\n```\n\nVS Code may show a notification: \"Your application running on port 8085 is available\" with an \"Open in Browser\" button. Clicking it may lead to an error page.\n\nSolution:\n\n1. Hover over the long OAuth URL in the terminal output.\n2. Ctrl + Click the link — VS Code will show a dialog: \"Do you want Code to open the external website?\" with buttons Open, Copy, Configure Trusted Domains, and Cancel.\n3. Click Configure Trusted Domains.\n4. A dropdown will appear with options like:\n   - \"Trust https://accounts.google.com\"\n   - \"Trust google.com and all its subdomains\"\n   - \"Trust all domains (disables link protection)\"\n   - \"Manage Trusted Domains\"\n5. Pick the first or second entry (e.g., \"Trust https://accounts.google.com\").\n\nNext time you run gcloud auth, the login page should pop up via the default browser without issues.",
      "When attempting to start the Docker Postgres container, you may encounter the error message:\n\n```Error - postgres port is already in use.\n```\n\nOption 1: Identify and Stop the Service\n\n1. Determine which service is using the port by running:\n   \n   ```bash\nsudo lsof -i :5432\n```\n   \n2. Stop the service that is using the port:\n   \n   ```bash\nsudo service postgresql stop\n```\n\nOption 2: Map to a Different Port\n\nFor a more long-term solution, consider mapping to a different port:\n\n- Map local port 5433 to container port 5432 in your Docker configuration (Dockerfile or docker-compose.yml).\n- If using a VM, ensure that port 5433 is forwarded in the host machine configuration.\n\nThis approach prevents conflicts and allows the Docker Postgres container to run without interruption.",
      "Error:\n\n```plaintext\nThis project does not have a development environment configured. Please create a development environment and configure your development credentials to use the dbt IDE.\n```\n\nThe error message provides guidance on resolving this issue. Follow the guide in the dbt cloud setup documentation. Additional instructions can be found in the video @1:42."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "Setup: No development environment",
    "positive": "Error:\n\n```plaintext\nThis project does not have a development environment configured. Please create a development environment and configure your development credentials to use the dbt IDE.\n```\n\nThe error message provides guidance on resolving this issue. Follow the guide in the dbt cloud setup documentation. Additional instructions can be found in the video @1:42.",
    "hard_negatives": [
      "To enable CI jobs (running dbt on pull requests), dbt Cloud needs a native integration with GitHub, GitLab, or Azure DevOps — the generic \"Git Clone\" / SSH connection method does not unlock the \"Run on Pull Requests\" trigger and you'll see:\n\n```Triggered by pull requests\nThis feature is only available for dbt repositories connected through dbt Cloud's native integration with Github, Gitlab, or Azure DevOps\n```\n\nThe dbt Cloud Developer plan also doesn't support CI jobs — you need Team or Enterprise. (See the dbt Cloud CI prerequisites.)\n\nSwitch from Git Clone to native GitHub integration\n\n1. Connect your GitHub account in dbt Cloud: Profile Settings → Linked Accounts → connect GitHub and grant the requested permissions. (dbt docs)\n\n2. Disconnect the existing Git Clone connection: Account Settings → Projects → your project → Repository → Disconnect.\n\n3. Reconfigure with the GitHub option, selecting the repository.\n\n4. (If your dbt project lives in a subfolder of the repo) set Project Subdirectory in the project settings to point to that folder.\n\n5. In Deploy → Job → Configuration → Triggers, you should now see \"Run on Pull Requests\" as an available toggle.\n\nCI job runs but says \"valid dbt project was not found\"\n\nUsually one of:\n\n- Project is in a subfolder, but Project Subdirectory isn't set.\n- The CI environment is configured to run on a custom branch that doesn't exist.\n- Uncommitted/unpushed changes.\n\nIn Deploy → Environments → your environment → Settings, set \"Custom branch\" if you're not on the default branch, and confirm it matches a real branch in GitHub.",
      "Remove the dataset from BigQuery created by dbt and run again (with test disabled) to ensure the dataset created has all the rows.\n\nDBT: Why am I getting a new dataset after running my CI/CD Job? / What is this new dbt dataset in BigQuery?\n\nWhen you create the CI/CD job, under 'Compare Changes against an environment (Deferral)' make sure that you select 'No; do not defer to another environment'. Otherwise, dbt won’t merge your dev models into production models; it will create a new environment called ‘dbt_cloud_pr_number of pull request’",
      "In my setup, all of the dependencies listed in build.gradle were not installed in -1.0-SNAPSHOT.jar.\n\nSolution:\n\n1. In the build.gradle file, add the following at the end:\n   \n   ```groovy\nshadowJar {\n       archiveBaseName = \"java-kafka-rides\"\n       archiveClassifier = ''\n   }\n```\n\n2. In the command line, run:\n   \n   ```bash\ngradle shadowjar\n```\n\n3. Execute the script from java-kafka-rides-1.0-SNAPSHOT.jar created by the shadowjar.",
      "When you log into PgAdmin and see an empty database, the following solution can help:\n\nRun:\n   \n```bash\ndocker-compose up\n```\n\nAnd at the same time run:\n\n```bash\ndocker build -t taxi_ingest:v001 .\n\n# NETWORK NAME IS THE SAME AS THAT CREATED BY DOCKER COMPOSE\ndocker run -it \\\n  --network=pg-network \\\n  taxi_ingest:v001 \\\n  --user=postgres \\\n  --password=postgres \\\n  --host=db \\\n  --port=5432 \\\n  --db=ny_taxi \\\n  --table_name=green_tripdata \\\n  --url=${URL}\n```\n\nIt's important to use the same --network as stated in the docker-compose.yaml file.\n\nThe docker-compose.yaml file might not specify a network, as shown below:\n\n```yaml\nservices:\n  db:\n    container_name: postgres\n    image: postgres:17-alpine\n    environment:\n      ...\n    ports:\n      - '5433:5432'\n    volumes:\n      - ...\n  pgadmin:\n    container_name: pgadmin\n    image: dpage/pgadmin4:latest\n    environment:\n      ...\n    ports:\n      - '8080:80'\n    volumes:\n      - ...\n\nvolumes:\n  vol-pgdata:\n    name: vol-pgdata\n  vol-pgadmin_data:\n    name: vol-pgadmin_data\n```\n\nIf the network name is not specified, it is generated automatically: The name of the directory containing the docker-compose.yaml file in lowercase + _default.\n\nYou can find the network’s name when running docker-compose up:\n\n```pg-database Pulling pg-database Pulled \nNetwork week_1_default  Creating\nNetwork week_1_default  Created\n```",
      "Kestra supports conditional branching within a single workflow to reuse shared orchestration logic while allowing dataset-specific behavior. This approach reduces duplication, keeps related steps together, and makes execution paths explicit within a single workflow definition.\n\nWhen to use a single-workflow branch:\n- Reuse common steps (e.g., data validation, enrichment) across datasets.\n- Implement dataset-specific paths (e.g., Yellow vs Green taxi) within the same flow.\n\nHow to implement:\n- Use a branching mechanism in Kestra (e.g., a condition or switch node) to evaluate a runtime variable (such as the dataset type) and route to the appropriate sub-path.\n- Place shared steps before the branch; place dataset-specific steps inside each branch.\n- Decide how branches converge: continue with a common tail after the branch or treat branches as separate end states.\n\nBest practices:\n- Keep branches modular and minimal; extract shared logic into subflows where possible.\n- Centralize error handling and logging to ensure consistency across branches.\n- Test each branch with representative inputs and monitor branch-specific failures separately.\n\nExample (illustrative pseudo YAML):\n```- id: route_by_dataset\n  type: branch\n  when:\n    - condition: ${workflow.variables.dataset_type == 'yellow'}\n      then:\n        - - id: yellow_tasks\n            tasks: [ yellow_validate, yellow_transform ]\n    - else:\n        - - id: green_tasks\n            tasks: [ green_validate, green_transform ]\n- id: end\n  type: terminate\n```\n\nNote: Adapt the exact branch/task names to your Kestra workflow definitions. The key idea is to place shared logic before the branch, branch on dataset/type, and then execute dataset-specific steps within each branch."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "Environment - Could not establish connection to \"MyServerName\": Got bad result from install script",
    "positive": "To enable CI jobs (running dbt on pull requests), dbt Cloud needs a native integration with GitHub, GitLab, or Azure DevOps — the generic \"Git Clone\" / SSH connection method does not unlock the \"Run on Pull Requests\" trigger and you'll see:\n\n```Triggered by pull requests\nThis feature is only available for dbt repositories connected through dbt Cloud's native integration with Github, Gitlab, or Azure DevOps\n```\n\nThe dbt Cloud Developer plan also doesn't support CI jobs — you need Team or Enterprise. (See the dbt Cloud CI prerequisites.)\n\nSwitch from Git Clone to native GitHub integration\n\n1. Connect your GitHub account in dbt Cloud: Profile Settings → Linked Accounts → connect GitHub and grant the requested permissions. (dbt docs)\n\n2. Disconnect the existing Git Clone connection: Account Settings → Projects → your project → Repository → Disconnect.\n\n3. Reconfigure with the GitHub option, selecting the repository.\n\n4. (If your dbt project lives in a subfolder of the repo) set Project Subdirectory in the project settings to point to that folder.\n\n5. In Deploy → Job → Configuration → Triggers, you should now see \"Run on Pull Requests\" as an available toggle.\n\nCI job runs but says \"valid dbt project was not found\"\n\nUsually one of:\n\n- Project is in a subfolder, but Project Subdirectory isn't set.\n- The CI environment is configured to run on a custom branch that doesn't exist.\n- Uncommitted/unpushed changes.\n\nIn Deploy → Environments → your environment → Settings, set \"Custom branch\" if you're not on the default branch, and confirm it matches a real branch in GitHub.",
    "hard_negatives": [
      "Error:\n\n```plaintext\nThis project does not have a development environment configured. Please create a development environment and configure your development credentials to use the dbt IDE.\n```\n\nThe error message provides guidance on resolving this issue. Follow the guide in the dbt cloud setup documentation. Additional instructions can be found in the video @1:42.",
      "In my setup, all of the dependencies listed in build.gradle were not installed in -1.0-SNAPSHOT.jar.\n\nSolution:\n\n1. In the build.gradle file, add the following at the end:\n   \n   ```groovy\nshadowJar {\n       archiveBaseName = \"java-kafka-rides\"\n       archiveClassifier = ''\n   }\n```\n\n2. In the command line, run:\n   \n   ```bash\ngradle shadowjar\n```\n\n3. Execute the script from java-kafka-rides-1.0-SNAPSHOT.jar created by the shadowjar.",
      "You might have installed Docker via snap. Run the following command to verify:\n\n```bash\nsudo snap status docker\n```\n\nIf you receive the response:\n\n```error: unknown command \"status\", see 'snap help'.\n```\n\nThen uninstall Docker and install it via the official website.\n\nError message: \"Bind for 0.0.0.0:5432 failed: port is already allocated.\"",
      "If you are using MS VS Code and running gcloud in WSL2, when you first try to login to GCP via the gcloud CLI with gcloud auth application-default login, you may encounter an issue where the terminal prints a long OAuth URL and then shows a series of \"not found\" errors for browsers:\n\n```Your browser has been opened to visit:\n    https://accounts.google.com/o/oauth2/auth?response_type=code&client_id=...\n\n/usr/bin/xdg-open: 882: x-www-browser: not found\n/usr/bin/xdg-open: 882: firefox: not found\n/usr/bin/xdg-open: 882: chromium: not found\n...\nxdg-open: no method available for opening '...'\n```\n\nVS Code may show a notification: \"Your application running on port 8085 is available\" with an \"Open in Browser\" button. Clicking it may lead to an error page.\n\nSolution:\n\n1. Hover over the long OAuth URL in the terminal output.\n2. Ctrl + Click the link — VS Code will show a dialog: \"Do you want Code to open the external website?\" with buttons Open, Copy, Configure Trusted Domains, and Cancel.\n3. Click Configure Trusted Domains.\n4. A dropdown will appear with options like:\n   - \"Trust https://accounts.google.com\"\n   - \"Trust google.com and all its subdomains\"\n   - \"Trust all domains (disables link protection)\"\n   - \"Manage Trusted Domains\"\n5. Pick the first or second entry (e.g., \"Trust https://accounts.google.com\").\n\nNext time you run gcloud auth, the login page should pop up via the default browser without issues.",
      "This issue occurs when attempting to connect to a GCP VM using VSCode on a Windows machine. You can resolve it by changing a registry value in the registry editor.\n\nOpen the Run command window:\n- Use the shortcut keys Windows + R, or\n- Right-click \"Start\" and click \"Run\".\n\nOpen the Registry Editor:\n- Type regedit in the Run command window, then press Enter.\n\nChange the registry value:\n- Navigate to HKEY_CURRENT_USER\\Software\\Microsoft\\Command Processor.\n- Change the \"Autorun\" value from \"if exists\" to a blank.\n\nAlternatively, you can delete the saved fingerprint within the known_hosts file:\n\nIn Windows, locate the file at C:\\Users\\\\.ssh\\known_hosts and remove the entry for the server."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "Homework: Qn 6: Did anyone get an exact match for one of the options given in Module 3 homework Q6?",
    "positive": "Many people aren’t getting an exact match, but are very close to one of the options. It is suggested to choose the closest option.",
    "hard_negatives": [
      "You don't need a confirmation email. You're accepted. You can start learning and submitting homework without registering. Registration was just to gauge interest before the start date.",
      "When running terraform destroy, the following error can occur:\n\n```Do you really want to destroy all resources?\n\nTerraform will destroy all your managed infrastructure, as shown above.\n\nThere is no undo. Only 'yes' will be accepted to confirm.\n\nEnter a value: yes\n\ngoogle_bigquery_dataset.homework_dataset: Destroying... [id=projects/terraform-demo-449214/datasets/homework_dataset]\n\n╷\n\n│ Error: Error when reading or editing Dataset: googleapi: Error 400: Dataset terraform-demo-449214:homework_dataset is still in use, resourceInUse\n```\n\nThis is because the dataset is still in use by a table. To delete the dataset, set the delete_contents_on_destroy property to true in the main.tf file.",
      "See a list of datasets here: GitHub Datasets",
      "Make sure you don’t have a dlt.py file saved in the same directory as your working file.",
      "To read from multiple topics in the same Spark session, follow these steps:\n\n1. Initiate a Spark Session:\n   \n   ```python\nspark = (SparkSession\n       .builder\n       .appName(app_name)\n       .master(master=master)\n       .getOrCreate())\n   \n   spark.streams.resetTerminated()\n```\n\n2. Read Streams from Multiple Topics:\n   \n   ```python\nquery1 = spark\n       .readStream\n       ...\n       ...\n       .load()\n   \n   query2 = spark\n       .readStream\n       ...\n       ...\n       .load()\n   \n   query3 = spark\n       .readStream\n       ...\n       ...\n       .load()\n```\n\n3. Start the Queries:\n   \n   ```python\nquery1.start()\n   query2.start()\n   query3.start()\n```\n\n4. Await Termination:\n   \n   ```python\nspark.streams.awaitAnyTermination()  # Waits for any one of the queries to receive a kill signal or error failure. This is asynchronous.\n```\n\n   Note: query3.start().awaitTermination() is a blocking call. It works well when we are reading only from one topic."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "Postgres: bind: address already in use",
    "positive": "When attempting to start the Docker Postgres container, you may encounter the error message:\n\n```Error - postgres port is already in use.\n```\n\nOption 1: Identify and Stop the Service\n\n1. Determine which service is using the port by running:\n   \n   ```bash\nsudo lsof -i :5432\n```\n   \n2. Stop the service that is using the port:\n   \n   ```bash\nsudo service postgresql stop\n```\n\nOption 2: Map to a Different Port\n\nFor a more long-term solution, consider mapping to a different port:\n\n- Map local port 5433 to container port 5432 in your Docker configuration (Dockerfile or docker-compose.yml).\n- If using a VM, ensure that port 5433 is forwarded in the host machine configuration.\n\nThis approach prevents conflicts and allows the Docker Postgres container to run without interruption.",
    "hard_negatives": [
      "Use the PostgreSQL COPY FROM feature, which is compatible with CSV files.\n\nSteps:\n\n1. Create the Table\n   \n   First, create your table (example):\n   \n   ```sql\nCREATE TABLE taxis (\n   \n   …\n   \n   );\n```\n\n2. Use COPY Functionality\n\n   Use the COPY command (example):\n\n   ```sql\nCOPY taxis FROM PROGRAM\n   'url'\n   WITH (\n   FORMAT csv,\n   HEADER true,\n   ENCODING utf8\n   );\n```\n\n   - Syntax for COPY:\n\n   ```sql\nCOPY table_name [ ( column_name [, ...] ) ]\n   FROM { 'filename' | PROGRAM 'command' | STDIN }\n   [ [ WITH ] ( option [, ...] ) ]\n   [ WHERE condition ]\n```",
      "When you log into PgAdmin and see an empty database, the following solution can help:\n\nRun:\n   \n```bash\ndocker-compose up\n```\n\nAnd at the same time run:\n\n```bash\ndocker build -t taxi_ingest:v001 .\n\n# NETWORK NAME IS THE SAME AS THAT CREATED BY DOCKER COMPOSE\ndocker run -it \\\n  --network=pg-network \\\n  taxi_ingest:v001 \\\n  --user=postgres \\\n  --password=postgres \\\n  --host=db \\\n  --port=5432 \\\n  --db=ny_taxi \\\n  --table_name=green_tripdata \\\n  --url=${URL}\n```\n\nIt's important to use the same --network as stated in the docker-compose.yaml file.\n\nThe docker-compose.yaml file might not specify a network, as shown below:\n\n```yaml\nservices:\n  db:\n    container_name: postgres\n    image: postgres:17-alpine\n    environment:\n      ...\n    ports:\n      - '5433:5432'\n    volumes:\n      - ...\n  pgadmin:\n    container_name: pgadmin\n    image: dpage/pgadmin4:latest\n    environment:\n      ...\n    ports:\n      - '8080:80'\n    volumes:\n      - ...\n\nvolumes:\n  vol-pgdata:\n    name: vol-pgdata\n  vol-pgadmin_data:\n    name: vol-pgadmin_data\n```\n\nIf the network name is not specified, it is generated automatically: The name of the directory containing the docker-compose.yaml file in lowercase + _default.\n\nYou can find the network’s name when running docker-compose up:\n\n```pg-database Pulling pg-database Pulled \nNetwork week_1_default  Creating\nNetwork week_1_default  Created\n```",
      "You might have installed Docker via snap. Run the following command to verify:\n\n```bash\nsudo snap status docker\n```\n\nIf you receive the response:\n\n```error: unknown command \"status\", see 'snap help'.\n```\n\nThen uninstall Docker and install it via the official website.\n\nError message: \"Bind for 0.0.0.0:5432 failed: port is already allocated.\"",
      "If you follow the video 1.2.2 - Ingesting NY Taxi Data to Postgres and execute the same steps, you will ingest all the data (~1.3 million rows) into the table yellow_taxi_data. However, running the whole script in the Jupyter notebook for a second time from top to bottom will result in missing the first chunk of 100,000 records. This occurs because a call to the iterator appears before the while loop, leading to the second chunk being ingested first.\n\n- Remove the cell df=next(df_iter) located higher up in the notebook than the while loop.\n- Ensure the first w(df_iter) call is within the while loop.\n\n📔 Note: The notebook is used to test the code and is not intended to be run top to bottom. The logic is organized in a later step when inserted into a .py file for the pipeline.",
      "Kestra supports conditional branching within a single workflow to reuse shared orchestration logic while allowing dataset-specific behavior. This approach reduces duplication, keeps related steps together, and makes execution paths explicit within a single workflow definition.\n\nWhen to use a single-workflow branch:\n- Reuse common steps (e.g., data validation, enrichment) across datasets.\n- Implement dataset-specific paths (e.g., Yellow vs Green taxi) within the same flow.\n\nHow to implement:\n- Use a branching mechanism in Kestra (e.g., a condition or switch node) to evaluate a runtime variable (such as the dataset type) and route to the appropriate sub-path.\n- Place shared steps before the branch; place dataset-specific steps inside each branch.\n- Decide how branches converge: continue with a common tail after the branch or treat branches as separate end states.\n\nBest practices:\n- Keep branches modular and minimal; extract shared logic into subflows where possible.\n- Centralize error handling and logging to ensure consistency across branches.\n- Test each branch with representative inputs and monitor branch-specific failures separately.\n\nExample (illustrative pseudo YAML):\n```- id: route_by_dataset\n  type: branch\n  when:\n    - condition: ${workflow.variables.dataset_type == 'yellow'}\n      then:\n        - - id: yellow_tasks\n            tasks: [ yellow_validate, yellow_transform ]\n    - else:\n        - - id: green_tasks\n            tasks: [ green_validate, green_transform ]\n- id: end\n  type: terminate\n```\n\nNote: Adapt the exact branch/task names to your Kestra workflow definitions. The key idea is to place shared logic before the branch, branch on dataset/type, and then execute dataset-specific steps within each branch."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "Postgres: \"Column does not exist\" but it actually does (Pyscopg2 error in MacBook Pro M2)",
    "positive": "In join queries, if you mention the column name directly or enclose it in single quotes, you'll encounter an error saying \"column does not exist\".\n\nSolution: Enclose the column names in double quotes, and it will work correctly.",
    "hard_negatives": [
      "When attempting to start the Docker Postgres container, you may encounter the error message:\n\n```Error - postgres port is already in use.\n```\n\nOption 1: Identify and Stop the Service\n\n1. Determine which service is using the port by running:\n   \n   ```bash\nsudo lsof -i :5432\n```\n   \n2. Stop the service that is using the port:\n   \n   ```bash\nsudo service postgresql stop\n```\n\nOption 2: Map to a Different Port\n\nFor a more long-term solution, consider mapping to a different port:\n\n- Map local port 5433 to container port 5432 in your Docker configuration (Dockerfile or docker-compose.yml).\n- If using a VM, ensure that port 5433 is forwarded in the host machine configuration.\n\nThis approach prevents conflicts and allows the Docker Postgres container to run without interruption.",
      "If you are using MS VS Code and running gcloud in WSL2, when you first try to login to GCP via the gcloud CLI with gcloud auth application-default login, you may encounter an issue where the terminal prints a long OAuth URL and then shows a series of \"not found\" errors for browsers:\n\n```Your browser has been opened to visit:\n    https://accounts.google.com/o/oauth2/auth?response_type=code&client_id=...\n\n/usr/bin/xdg-open: 882: x-www-browser: not found\n/usr/bin/xdg-open: 882: firefox: not found\n/usr/bin/xdg-open: 882: chromium: not found\n...\nxdg-open: no method available for opening '...'\n```\n\nVS Code may show a notification: \"Your application running on port 8085 is available\" with an \"Open in Browser\" button. Clicking it may lead to an error page.\n\nSolution:\n\n1. Hover over the long OAuth URL in the terminal output.\n2. Ctrl + Click the link — VS Code will show a dialog: \"Do you want Code to open the external website?\" with buttons Open, Copy, Configure Trusted Domains, and Cancel.\n3. Click Configure Trusted Domains.\n4. A dropdown will appear with options like:\n   - \"Trust https://accounts.google.com\"\n   - \"Trust google.com and all its subdomains\"\n   - \"Trust all domains (disables link protection)\"\n   - \"Manage Trusted Domains\"\n5. Pick the first or second entry (e.g., \"Trust https://accounts.google.com\").\n\nNext time you run gcloud auth, the login page should pop up via the default browser without issues.",
      "If you follow the video 1.2.2 - Ingesting NY Taxi Data to Postgres and execute the same steps, you will ingest all the data (~1.3 million rows) into the table yellow_taxi_data. However, running the whole script in the Jupyter notebook for a second time from top to bottom will result in missing the first chunk of 100,000 records. This occurs because a call to the iterator appears before the while loop, leading to the second chunk being ingested first.\n\n- Remove the cell df=next(df_iter) located higher up in the notebook than the while loop.\n- Ensure the first w(df_iter) call is within the while loop.\n\n📔 Note: The notebook is used to test the code and is not intended to be run top to bottom. The logic is organized in a later step when inserted into a .py file for the pipeline.",
      "When you log into PgAdmin and see an empty database, the following solution can help:\n\nRun:\n   \n```bash\ndocker-compose up\n```\n\nAnd at the same time run:\n\n```bash\ndocker build -t taxi_ingest:v001 .\n\n# NETWORK NAME IS THE SAME AS THAT CREATED BY DOCKER COMPOSE\ndocker run -it \\\n  --network=pg-network \\\n  taxi_ingest:v001 \\\n  --user=postgres \\\n  --password=postgres \\\n  --host=db \\\n  --port=5432 \\\n  --db=ny_taxi \\\n  --table_name=green_tripdata \\\n  --url=${URL}\n```\n\nIt's important to use the same --network as stated in the docker-compose.yaml file.\n\nThe docker-compose.yaml file might not specify a network, as shown below:\n\n```yaml\nservices:\n  db:\n    container_name: postgres\n    image: postgres:17-alpine\n    environment:\n      ...\n    ports:\n      - '5433:5432'\n    volumes:\n      - ...\n  pgadmin:\n    container_name: pgadmin\n    image: dpage/pgadmin4:latest\n    environment:\n      ...\n    ports:\n      - '8080:80'\n    volumes:\n      - ...\n\nvolumes:\n  vol-pgdata:\n    name: vol-pgdata\n  vol-pgadmin_data:\n    name: vol-pgadmin_data\n```\n\nIf the network name is not specified, it is generated automatically: The name of the directory containing the docker-compose.yaml file in lowercase + _default.\n\nYou can find the network’s name when running docker-compose up:\n\n```pg-database Pulling pg-database Pulled \nNetwork week_1_default  Creating\nNetwork week_1_default  Created\n```",
      "Use the PostgreSQL COPY FROM feature, which is compatible with CSV files.\n\nSteps:\n\n1. Create the Table\n   \n   First, create your table (example):\n   \n   ```sql\nCREATE TABLE taxis (\n   \n   …\n   \n   );\n```\n\n2. Use COPY Functionality\n\n   Use the COPY command (example):\n\n   ```sql\nCOPY taxis FROM PROGRAM\n   'url'\n   WITH (\n   FORMAT csv,\n   HEADER true,\n   ENCODING utf8\n   );\n```\n\n   - Syntax for COPY:\n\n   ```sql\nCOPY table_name [ ( column_name [, ...] ) ]\n   FROM { 'filename' | PROGRAM 'command' | STDIN }\n   [ [ WITH ] ( option [, ...] ) ]\n   [ WHERE condition ]\n```"
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "GCP: Do I need to delete my instance in Google Cloud?",
    "positive": "In this lecture, Alexey deleted his instance in Google Cloud. Do I have to do it?\n\nNo, do not delete your instance in Google Cloud Platform. Otherwise, you will have to set it up again for the week 1 readings.",
    "hard_negatives": [
      "If you are using MS VS Code and running gcloud in WSL2, when you first try to login to GCP via the gcloud CLI with gcloud auth application-default login, you may encounter an issue where the terminal prints a long OAuth URL and then shows a series of \"not found\" errors for browsers:\n\n```Your browser has been opened to visit:\n    https://accounts.google.com/o/oauth2/auth?response_type=code&client_id=...\n\n/usr/bin/xdg-open: 882: x-www-browser: not found\n/usr/bin/xdg-open: 882: firefox: not found\n/usr/bin/xdg-open: 882: chromium: not found\n...\nxdg-open: no method available for opening '...'\n```\n\nVS Code may show a notification: \"Your application running on port 8085 is available\" with an \"Open in Browser\" button. Clicking it may lead to an error page.\n\nSolution:\n\n1. Hover over the long OAuth URL in the terminal output.\n2. Ctrl + Click the link — VS Code will show a dialog: \"Do you want Code to open the external website?\" with buttons Open, Copy, Configure Trusted Domains, and Cancel.\n3. Click Configure Trusted Domains.\n4. A dropdown will appear with options like:\n   - \"Trust https://accounts.google.com\"\n   - \"Trust google.com and all its subdomains\"\n   - \"Trust all domains (disables link protection)\"\n   - \"Manage Trusted Domains\"\n5. Pick the first or second entry (e.g., \"Trust https://accounts.google.com\").\n\nNext time you run gcloud auth, the login page should pop up via the default browser without issues.",
      "This issue occurs when attempting to connect to a GCP VM using VSCode on a Windows machine. You can resolve it by changing a registry value in the registry editor.\n\nOpen the Run command window:\n- Use the shortcut keys Windows + R, or\n- Right-click \"Start\" and click \"Run\".\n\nOpen the Registry Editor:\n- Type regedit in the Run command window, then press Enter.\n\nChange the registry value:\n- Navigate to HKEY_CURRENT_USER\\Software\\Microsoft\\Command Processor.\n- Change the \"Autorun\" value from \"if exists\" to a blank.\n\nAlternatively, you can delete the saved fingerprint within the known_hosts file:\n\nIn Windows, locate the file at C:\\Users\\\\.ssh\\known_hosts and remove the entry for the server.",
      "Reference:\n\nhttps://cloud.google.com/bigquery/docs/external-data-cloud-storage\n\nSolution:\n\n```python\nfrom google.cloud import bigquery\n\n# Set table_id to the ID of the table to create\ntable_id = f\"{project_id}.{dataset_name}.{table_name}\"\n\n# Construct a BigQuery client object\nclient = bigquery.Client()\n\n# Set the external source format of your table\nexternal_source_format = \"PARQUET\"\n\n# Set the source_uris to point to your data in Google Cloud\nsource_uris = [f'gs://{bucket_name}/{object_key}/*']\n\n# Create ExternalConfig object with external source format\nexternal_config = bigquery.ExternalConfig(external_source_format)\n\n# Set source_uris that point to your data in Google Cloud\nexternal_config.source_uris = source_uris\nexternal_config.autodetect = True\n\ntable = bigquery.Table(table_id)\n\n# Set the external data configuration of the table\ntable.external_data_configuration = external_config\ntable = client.create_table(table)  # Make an API request.\n\nprint(f'Created table with external source: {table_id}')\nprint(f'Format: {table.external_data_configuration.source_format}')\n```",
      "When running terraform destroy, the following error can occur:\n\n```Do you really want to destroy all resources?\n\nTerraform will destroy all your managed infrastructure, as shown above.\n\nThere is no undo. Only 'yes' will be accepted to confirm.\n\nEnter a value: yes\n\ngoogle_bigquery_dataset.homework_dataset: Destroying... [id=projects/terraform-demo-449214/datasets/homework_dataset]\n\n╷\n\n│ Error: Error when reading or editing Dataset: googleapi: Error 400: Dataset terraform-demo-449214:homework_dataset is still in use, resourceInUse\n```\n\nThis is because the dataset is still in use by a table. To delete the dataset, set the delete_contents_on_destroy property to true in the main.tf file.",
      "1. Install the astronomer-cosmos package as a dependency. Refer to the installation guide here and see a Terraform example.\n\n2. Create a new folder, dbt/, inside the dags/ folder of your Composer GCP bucket and copy your dbt-core project there. See the example.\n\n3. Ensure your profiles.yml is configured to authenticate with a service account key. Refer to the BigQuery example.\n\n4. Create a new DAG using the DbtTaskGroup class. Use a ProfileConfig specifying a profiles_yml_filepath that points to the location of your JSON key file. See this example.\n\nYour dbt lineage graph should now appear as tasks inside a task group like this:"
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "Why does Kestra support conditional branching within a single workflow instead of separate flows?",
    "positive": "Kestra supports conditional branching within a single workflow to reuse shared orchestration logic while allowing dataset-specific behavior. This approach reduces duplication, keeps related steps together, and makes execution paths explicit within a single workflow definition.\n\nWhen to use a single-workflow branch:\n- Reuse common steps (e.g., data validation, enrichment) across datasets.\n- Implement dataset-specific paths (e.g., Yellow vs Green taxi) within the same flow.\n\nHow to implement:\n- Use a branching mechanism in Kestra (e.g., a condition or switch node) to evaluate a runtime variable (such as the dataset type) and route to the appropriate sub-path.\n- Place shared steps before the branch; place dataset-specific steps inside each branch.\n- Decide how branches converge: continue with a common tail after the branch or treat branches as separate end states.\n\nBest practices:\n- Keep branches modular and minimal; extract shared logic into subflows where possible.\n- Centralize error handling and logging to ensure consistency across branches.\n- Test each branch with representative inputs and monitor branch-specific failures separately.\n\nExample (illustrative pseudo YAML):\n```- id: route_by_dataset\n  type: branch\n  when:\n    - condition: ${workflow.variables.dataset_type == 'yellow'}\n      then:\n        - - id: yellow_tasks\n            tasks: [ yellow_validate, yellow_transform ]\n    - else:\n        - - id: green_tasks\n            tasks: [ green_validate, green_transform ]\n- id: end\n  type: terminate\n```\n\nNote: Adapt the exact branch/task names to your Kestra workflow definitions. The key idea is to place shared logic before the branch, branch on dataset/type, and then execute dataset-specific steps within each branch.",
    "hard_negatives": [
      "The issue comes down to how Unix processes produce output and how Kestra interprets it:\n\n1. Python's logging module writes to stderr by default. If you, for instance, call logging.basicConfig() without specifying a stream argument, the root handler sends basically everything to stderr.\n\n2. Kestra maps the two standard streams to its own log levels. Anything the container writes to stdout becomes a Kestra DEBUG entry and anything written on stderr becomes ERROR. There is no middle ground.\n\nThe fix\n\nGood news is that the fix is simple: redirect Python logging to stdout:\n\n```python\nimport sys\nimport logging\n\nlogging.basicConfig(\n    level=logging.INFO,\n    format=\"%(asctime)s %(levelname)s %(message)s\",\n    stream=sys.stdout,\n)\n```\n\nThat single stream=sys.stdout argument is enough. After the change, your informational messages will show up as DEBUG in Kestra.",
      "Spark processes data in partitions. When you write a DataFrame to disk, Spark writes each partition as a separate output file. For example:\n\n```python\ntrips.repartition(4).write.parquet(\"output/\")\n```\n\nThis creates four parquet files because the DataFrame now has four partitions. This behavior enables Spark to write data in parallel and can improve performance on large datasets.",
      "See a list of datasets here: GitHub Datasets",
      "To enable CI jobs (running dbt on pull requests), dbt Cloud needs a native integration with GitHub, GitLab, or Azure DevOps — the generic \"Git Clone\" / SSH connection method does not unlock the \"Run on Pull Requests\" trigger and you'll see:\n\n```Triggered by pull requests\nThis feature is only available for dbt repositories connected through dbt Cloud's native integration with Github, Gitlab, or Azure DevOps\n```\n\nThe dbt Cloud Developer plan also doesn't support CI jobs — you need Team or Enterprise. (See the dbt Cloud CI prerequisites.)\n\nSwitch from Git Clone to native GitHub integration\n\n1. Connect your GitHub account in dbt Cloud: Profile Settings → Linked Accounts → connect GitHub and grant the requested permissions. (dbt docs)\n\n2. Disconnect the existing Git Clone connection: Account Settings → Projects → your project → Repository → Disconnect.\n\n3. Reconfigure with the GitHub option, selecting the repository.\n\n4. (If your dbt project lives in a subfolder of the repo) set Project Subdirectory in the project settings to point to that folder.\n\n5. In Deploy → Job → Configuration → Triggers, you should now see \"Run on Pull Requests\" as an available toggle.\n\nCI job runs but says \"valid dbt project was not found\"\n\nUsually one of:\n\n- Project is in a subfolder, but Project Subdirectory isn't set.\n- The CI environment is configured to run on a custom branch that doesn't exist.\n- Uncommitted/unpushed changes.\n\nIn Deploy → Environments → your environment → Settings, set \"Custom branch\" if you're not on the default branch, and confirm it matches a real branch in GitHub.",
      "Many people aren’t getting an exact match, but are very close to one of the options. It is suggested to choose the closest option."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "GCP: BQ - Create External Table using Python",
    "positive": "Reference:\n\nhttps://cloud.google.com/bigquery/docs/external-data-cloud-storage\n\nSolution:\n\n```python\nfrom google.cloud import bigquery\n\n# Set table_id to the ID of the table to create\ntable_id = f\"{project_id}.{dataset_name}.{table_name}\"\n\n# Construct a BigQuery client object\nclient = bigquery.Client()\n\n# Set the external source format of your table\nexternal_source_format = \"PARQUET\"\n\n# Set the source_uris to point to your data in Google Cloud\nsource_uris = [f'gs://{bucket_name}/{object_key}/*']\n\n# Create ExternalConfig object with external source format\nexternal_config = bigquery.ExternalConfig(external_source_format)\n\n# Set source_uris that point to your data in Google Cloud\nexternal_config.source_uris = source_uris\nexternal_config.autodetect = True\n\ntable = bigquery.Table(table_id)\n\n# Set the external data configuration of the table\ntable.external_data_configuration = external_config\ntable = client.create_table(table)  # Make an API request.\n\nprint(f'Created table with external source: {table_id}')\nprint(f'Format: {table.external_data_configuration.source_format}')\n```",
    "hard_negatives": [
      "Use the PostgreSQL COPY FROM feature, which is compatible with CSV files.\n\nSteps:\n\n1. Create the Table\n   \n   First, create your table (example):\n   \n   ```sql\nCREATE TABLE taxis (\n   \n   …\n   \n   );\n```\n\n2. Use COPY Functionality\n\n   Use the COPY command (example):\n\n   ```sql\nCOPY taxis FROM PROGRAM\n   'url'\n   WITH (\n   FORMAT csv,\n   HEADER true,\n   ENCODING utf8\n   );\n```\n\n   - Syntax for COPY:\n\n   ```sql\nCOPY table_name [ ( column_name [, ...] ) ]\n   FROM { 'filename' | PROGRAM 'command' | STDIN }\n   [ [ WITH ] ( option [, ...] ) ]\n   [ WHERE condition ]\n```",
      "If you are using MS VS Code and running gcloud in WSL2, when you first try to login to GCP via the gcloud CLI with gcloud auth application-default login, you may encounter an issue where the terminal prints a long OAuth URL and then shows a series of \"not found\" errors for browsers:\n\n```Your browser has been opened to visit:\n    https://accounts.google.com/o/oauth2/auth?response_type=code&client_id=...\n\n/usr/bin/xdg-open: 882: x-www-browser: not found\n/usr/bin/xdg-open: 882: firefox: not found\n/usr/bin/xdg-open: 882: chromium: not found\n...\nxdg-open: no method available for opening '...'\n```\n\nVS Code may show a notification: \"Your application running on port 8085 is available\" with an \"Open in Browser\" button. Clicking it may lead to an error page.\n\nSolution:\n\n1. Hover over the long OAuth URL in the terminal output.\n2. Ctrl + Click the link — VS Code will show a dialog: \"Do you want Code to open the external website?\" with buttons Open, Copy, Configure Trusted Domains, and Cancel.\n3. Click Configure Trusted Domains.\n4. A dropdown will appear with options like:\n   - \"Trust https://accounts.google.com\"\n   - \"Trust google.com and all its subdomains\"\n   - \"Trust all domains (disables link protection)\"\n   - \"Manage Trusted Domains\"\n5. Pick the first or second entry (e.g., \"Trust https://accounts.google.com\").\n\nNext time you run gcloud auth, the login page should pop up via the default browser without issues.",
      "1. Install the astronomer-cosmos package as a dependency. Refer to the installation guide here and see a Terraform example.\n\n2. Create a new folder, dbt/, inside the dags/ folder of your Composer GCP bucket and copy your dbt-core project there. See the example.\n\n3. Ensure your profiles.yml is configured to authenticate with a service account key. Refer to the BigQuery example.\n\n4. Create a new DAG using the DbtTaskGroup class. Use a ProfileConfig specifying a profiles_yml_filepath that points to the location of your JSON key file. See this example.\n\nYour dbt lineage graph should now appear as tasks inside a task group like this:",
      "This issue occurs when attempting to connect to a GCP VM using VSCode on a Windows machine. You can resolve it by changing a registry value in the registry editor.\n\nOpen the Run command window:\n- Use the shortcut keys Windows + R, or\n- Right-click \"Start\" and click \"Run\".\n\nOpen the Registry Editor:\n- Type regedit in the Run command window, then press Enter.\n\nChange the registry value:\n- Navigate to HKEY_CURRENT_USER\\Software\\Microsoft\\Command Processor.\n- Change the \"Autorun\" value from \"if exists\" to a blank.\n\nAlternatively, you can delete the saved fingerprint within the known_hosts file:\n\nIn Windows, locate the file at C:\\Users\\\\.ssh\\known_hosts and remove the entry for the server.",
      "To read from multiple topics in the same Spark session, follow these steps:\n\n1. Initiate a Spark Session:\n   \n   ```python\nspark = (SparkSession\n       .builder\n       .appName(app_name)\n       .master(master=master)\n       .getOrCreate())\n   \n   spark.streams.resetTerminated()\n```\n\n2. Read Streams from Multiple Topics:\n   \n   ```python\nquery1 = spark\n       .readStream\n       ...\n       ...\n       .load()\n   \n   query2 = spark\n       .readStream\n       ...\n       ...\n       .load()\n   \n   query3 = spark\n       .readStream\n       ...\n       ...\n       .load()\n```\n\n3. Start the Queries:\n   \n   ```python\nquery1.start()\n   query2.start()\n   query3.start()\n```\n\n4. Await Termination:\n   \n   ```python\nspark.streams.awaitAnyTermination()  # Waits for any one of the queries to receive a kill signal or error failure. This is asynchronous.\n```\n\n   Note: query3.start().awaitTermination() is a blocking call. It works well when we are reading only from one topic."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "dbt CI: setting up Continuous Integration with GitHub",
    "positive": "To enable CI jobs (running dbt on pull requests), dbt Cloud needs a native integration with GitHub, GitLab, or Azure DevOps — the generic \"Git Clone\" / SSH connection method does not unlock the \"Run on Pull Requests\" trigger and you'll see:\n\n```Triggered by pull requests\nThis feature is only available for dbt repositories connected through dbt Cloud's native integration with Github, Gitlab, or Azure DevOps\n```\n\nThe dbt Cloud Developer plan also doesn't support CI jobs — you need Team or Enterprise. (See the dbt Cloud CI prerequisites.)\n\nSwitch from Git Clone to native GitHub integration\n\n1. Connect your GitHub account in dbt Cloud: Profile Settings → Linked Accounts → connect GitHub and grant the requested permissions. (dbt docs)\n\n2. Disconnect the existing Git Clone connection: Account Settings → Projects → your project → Repository → Disconnect.\n\n3. Reconfigure with the GitHub option, selecting the repository.\n\n4. (If your dbt project lives in a subfolder of the repo) set Project Subdirectory in the project settings to point to that folder.\n\n5. In Deploy → Job → Configuration → Triggers, you should now see \"Run on Pull Requests\" as an available toggle.\n\nCI job runs but says \"valid dbt project was not found\"\n\nUsually one of:\n\n- Project is in a subfolder, but Project Subdirectory isn't set.\n- The CI environment is configured to run on a custom branch that doesn't exist.\n- Uncommitted/unpushed changes.\n\nIn Deploy → Environments → your environment → Settings, set \"Custom branch\" if you're not on the default branch, and confirm it matches a real branch in GitHub.",
    "hard_negatives": [
      "Remove the dataset from BigQuery created by dbt and run again (with test disabled) to ensure the dataset created has all the rows.\n\nDBT: Why am I getting a new dataset after running my CI/CD Job? / What is this new dbt dataset in BigQuery?\n\nWhen you create the CI/CD job, under 'Compare Changes against an environment (Deferral)' make sure that you select 'No; do not defer to another environment'. Otherwise, dbt won’t merge your dev models into production models; it will create a new environment called ‘dbt_cloud_pr_number of pull request’",
      "Dbt provides a mechanism called ref to manage dependencies between models. By referencing other models using the ref keyword in SQL, dbt automatically understands the dependencies and ensures the correct execution order.",
      "See a list of datasets here: GitHub Datasets",
      "Instruction on how to store secrets that will be available in GitHub Codespaces. See Managing your account-specific secrets for GitHub Codespaces - GitHub Docs.",
      "Error:\n\n```plaintext\nThis project does not have a development environment configured. Please create a development environment and configure your development credentials to use the dbt IDE.\n```\n\nThe error message provides guidance on resolving this issue. Follow the guide in the dbt cloud setup documentation. Additional instructions can be found in the video @1:42."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "Does anyone know nice and relatively large datasets?",
    "positive": "See a list of datasets here: GitHub Datasets",
    "hard_negatives": [
      "Spark processes data in partitions. When you write a DataFrame to disk, Spark writes each partition as a separate output file. For example:\n\n```python\ntrips.repartition(4).write.parquet(\"output/\")\n```\n\nThis creates four parquet files because the DataFrame now has four partitions. This behavior enables Spark to write data in parallel and can improve performance on large datasets.",
      "Remove the dataset from BigQuery created by dbt and run again (with test disabled) to ensure the dataset created has all the rows.\n\nDBT: Why am I getting a new dataset after running my CI/CD Job? / What is this new dbt dataset in BigQuery?\n\nWhen you create the CI/CD job, under 'Compare Changes against an environment (Deferral)' make sure that you select 'No; do not defer to another environment'. Otherwise, dbt won’t merge your dev models into production models; it will create a new environment called ‘dbt_cloud_pr_number of pull request’",
      "When running terraform destroy, the following error can occur:\n\n```Do you really want to destroy all resources?\n\nTerraform will destroy all your managed infrastructure, as shown above.\n\nThere is no undo. Only 'yes' will be accepted to confirm.\n\nEnter a value: yes\n\ngoogle_bigquery_dataset.homework_dataset: Destroying... [id=projects/terraform-demo-449214/datasets/homework_dataset]\n\n╷\n\n│ Error: Error when reading or editing Dataset: googleapi: Error 400: Dataset terraform-demo-449214:homework_dataset is still in use, resourceInUse\n```\n\nThis is because the dataset is still in use by a table. To delete the dataset, set the delete_contents_on_destroy property to true in the main.tf file.",
      "Kestra supports conditional branching within a single workflow to reuse shared orchestration logic while allowing dataset-specific behavior. This approach reduces duplication, keeps related steps together, and makes execution paths explicit within a single workflow definition.\n\nWhen to use a single-workflow branch:\n- Reuse common steps (e.g., data validation, enrichment) across datasets.\n- Implement dataset-specific paths (e.g., Yellow vs Green taxi) within the same flow.\n\nHow to implement:\n- Use a branching mechanism in Kestra (e.g., a condition or switch node) to evaluate a runtime variable (such as the dataset type) and route to the appropriate sub-path.\n- Place shared steps before the branch; place dataset-specific steps inside each branch.\n- Decide how branches converge: continue with a common tail after the branch or treat branches as separate end states.\n\nBest practices:\n- Keep branches modular and minimal; extract shared logic into subflows where possible.\n- Centralize error handling and logging to ensure consistency across branches.\n- Test each branch with representative inputs and monitor branch-specific failures separately.\n\nExample (illustrative pseudo YAML):\n```- id: route_by_dataset\n  type: branch\n  when:\n    - condition: ${workflow.variables.dataset_type == 'yellow'}\n      then:\n        - - id: yellow_tasks\n            tasks: [ yellow_validate, yellow_transform ]\n    - else:\n        - - id: green_tasks\n            tasks: [ green_validate, green_transform ]\n- id: end\n  type: terminate\n```\n\nNote: Adapt the exact branch/task names to your Kestra workflow definitions. The key idea is to place shared logic before the branch, branch on dataset/type, and then execute dataset-specific steps within each branch.",
      "Reference:\n\nhttps://cloud.google.com/bigquery/docs/external-data-cloud-storage\n\nSolution:\n\n```python\nfrom google.cloud import bigquery\n\n# Set table_id to the ID of the table to create\ntable_id = f\"{project_id}.{dataset_name}.{table_name}\"\n\n# Construct a BigQuery client object\nclient = bigquery.Client()\n\n# Set the external source format of your table\nexternal_source_format = \"PARQUET\"\n\n# Set the source_uris to point to your data in Google Cloud\nsource_uris = [f'gs://{bucket_name}/{object_key}/*']\n\n# Create ExternalConfig object with external source format\nexternal_config = bigquery.ExternalConfig(external_source_format)\n\n# Set source_uris that point to your data in Google Cloud\nexternal_config.source_uris = source_uris\nexternal_config.autodetect = True\n\ntable = bigquery.Table(table_id)\n\n# Set the external data configuration of the table\ntable.external_data_configuration = external_config\ntable = client.create_table(table)  # Make an API request.\n\nprint(f'Created table with external source: {table_id}')\nprint(f'Format: {table.external_data_configuration.source_format}')\n```"
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "How to run a dbt-core project as an Airflow Task Group on Google Cloud Composer using a service account JSON key",
    "positive": "1. Install the astronomer-cosmos package as a dependency. Refer to the installation guide here and see a Terraform example.\n\n2. Create a new folder, dbt/, inside the dags/ folder of your Composer GCP bucket and copy your dbt-core project there. See the example.\n\n3. Ensure your profiles.yml is configured to authenticate with a service account key. Refer to the BigQuery example.\n\n4. Create a new DAG using the DbtTaskGroup class. Use a ProfileConfig specifying a profiles_yml_filepath that points to the location of your JSON key file. See this example.\n\nYour dbt lineage graph should now appear as tasks inside a task group like this:",
    "hard_negatives": [
      "To enable CI jobs (running dbt on pull requests), dbt Cloud needs a native integration with GitHub, GitLab, or Azure DevOps — the generic \"Git Clone\" / SSH connection method does not unlock the \"Run on Pull Requests\" trigger and you'll see:\n\n```Triggered by pull requests\nThis feature is only available for dbt repositories connected through dbt Cloud's native integration with Github, Gitlab, or Azure DevOps\n```\n\nThe dbt Cloud Developer plan also doesn't support CI jobs — you need Team or Enterprise. (See the dbt Cloud CI prerequisites.)\n\nSwitch from Git Clone to native GitHub integration\n\n1. Connect your GitHub account in dbt Cloud: Profile Settings → Linked Accounts → connect GitHub and grant the requested permissions. (dbt docs)\n\n2. Disconnect the existing Git Clone connection: Account Settings → Projects → your project → Repository → Disconnect.\n\n3. Reconfigure with the GitHub option, selecting the repository.\n\n4. (If your dbt project lives in a subfolder of the repo) set Project Subdirectory in the project settings to point to that folder.\n\n5. In Deploy → Job → Configuration → Triggers, you should now see \"Run on Pull Requests\" as an available toggle.\n\nCI job runs but says \"valid dbt project was not found\"\n\nUsually one of:\n\n- Project is in a subfolder, but Project Subdirectory isn't set.\n- The CI environment is configured to run on a custom branch that doesn't exist.\n- Uncommitted/unpushed changes.\n\nIn Deploy → Environments → your environment → Settings, set \"Custom branch\" if you're not on the default branch, and confirm it matches a real branch in GitHub.",
      "Reference:\n\nhttps://cloud.google.com/bigquery/docs/external-data-cloud-storage\n\nSolution:\n\n```python\nfrom google.cloud import bigquery\n\n# Set table_id to the ID of the table to create\ntable_id = f\"{project_id}.{dataset_name}.{table_name}\"\n\n# Construct a BigQuery client object\nclient = bigquery.Client()\n\n# Set the external source format of your table\nexternal_source_format = \"PARQUET\"\n\n# Set the source_uris to point to your data in Google Cloud\nsource_uris = [f'gs://{bucket_name}/{object_key}/*']\n\n# Create ExternalConfig object with external source format\nexternal_config = bigquery.ExternalConfig(external_source_format)\n\n# Set source_uris that point to your data in Google Cloud\nexternal_config.source_uris = source_uris\nexternal_config.autodetect = True\n\ntable = bigquery.Table(table_id)\n\n# Set the external data configuration of the table\ntable.external_data_configuration = external_config\ntable = client.create_table(table)  # Make an API request.\n\nprint(f'Created table with external source: {table_id}')\nprint(f'Format: {table.external_data_configuration.source_format}')\n```",
      "Error:\n\n```plaintext\nThis project does not have a development environment configured. Please create a development environment and configure your development credentials to use the dbt IDE.\n```\n\nThe error message provides guidance on resolving this issue. Follow the guide in the dbt cloud setup documentation. Additional instructions can be found in the video @1:42.",
      "Remove the dataset from BigQuery created by dbt and run again (with test disabled) to ensure the dataset created has all the rows.\n\nDBT: Why am I getting a new dataset after running my CI/CD Job? / What is this new dbt dataset in BigQuery?\n\nWhen you create the CI/CD job, under 'Compare Changes against an environment (Deferral)' make sure that you select 'No; do not defer to another environment'. Otherwise, dbt won’t merge your dev models into production models; it will create a new environment called ‘dbt_cloud_pr_number of pull request’",
      "Dbt provides a mechanism called ref to manage dependencies between models. By referencing other models using the ref keyword in SQL, dbt automatically understands the dependencies and ensures the correct execution order."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "What is the fastest way to upload taxi data to dbt-postgres?",
    "positive": "If you follow the video 1.2.2 - Ingesting NY Taxi Data to Postgres and execute the same steps, you will ingest all the data (~1.3 million rows) into the table yellow_taxi_data. However, running the whole script in the Jupyter notebook for a second time from top to bottom will result in missing the first chunk of 100,000 records. This occurs because a call to the iterator appears before the while loop, leading to the second chunk being ingested first.\n\n- Remove the cell df=next(df_iter) located higher up in the notebook than the while loop.\n- Ensure the first w(df_iter) call is within the while loop.\n\n📔 Note: The notebook is used to test the code and is not intended to be run top to bottom. The logic is organized in a later step when inserted into a .py file for the pipeline.",
    "hard_negatives": [
      "When you log into PgAdmin and see an empty database, the following solution can help:\n\nRun:\n   \n```bash\ndocker-compose up\n```\n\nAnd at the same time run:\n\n```bash\ndocker build -t taxi_ingest:v001 .\n\n# NETWORK NAME IS THE SAME AS THAT CREATED BY DOCKER COMPOSE\ndocker run -it \\\n  --network=pg-network \\\n  taxi_ingest:v001 \\\n  --user=postgres \\\n  --password=postgres \\\n  --host=db \\\n  --port=5432 \\\n  --db=ny_taxi \\\n  --table_name=green_tripdata \\\n  --url=${URL}\n```\n\nIt's important to use the same --network as stated in the docker-compose.yaml file.\n\nThe docker-compose.yaml file might not specify a network, as shown below:\n\n```yaml\nservices:\n  db:\n    container_name: postgres\n    image: postgres:17-alpine\n    environment:\n      ...\n    ports:\n      - '5433:5432'\n    volumes:\n      - ...\n  pgadmin:\n    container_name: pgadmin\n    image: dpage/pgadmin4:latest\n    environment:\n      ...\n    ports:\n      - '8080:80'\n    volumes:\n      - ...\n\nvolumes:\n  vol-pgdata:\n    name: vol-pgdata\n  vol-pgadmin_data:\n    name: vol-pgadmin_data\n```\n\nIf the network name is not specified, it is generated automatically: The name of the directory containing the docker-compose.yaml file in lowercase + _default.\n\nYou can find the network’s name when running docker-compose up:\n\n```pg-database Pulling pg-database Pulled \nNetwork week_1_default  Creating\nNetwork week_1_default  Created\n```",
      "Use the PostgreSQL COPY FROM feature, which is compatible with CSV files.\n\nSteps:\n\n1. Create the Table\n   \n   First, create your table (example):\n   \n   ```sql\nCREATE TABLE taxis (\n   \n   …\n   \n   );\n```\n\n2. Use COPY Functionality\n\n   Use the COPY command (example):\n\n   ```sql\nCOPY taxis FROM PROGRAM\n   'url'\n   WITH (\n   FORMAT csv,\n   HEADER true,\n   ENCODING utf8\n   );\n```\n\n   - Syntax for COPY:\n\n   ```sql\nCOPY table_name [ ( column_name [, ...] ) ]\n   FROM { 'filename' | PROGRAM 'command' | STDIN }\n   [ [ WITH ] ( option [, ...] ) ]\n   [ WHERE condition ]\n```",
      "When attempting to start the Docker Postgres container, you may encounter the error message:\n\n```Error - postgres port is already in use.\n```\n\nOption 1: Identify and Stop the Service\n\n1. Determine which service is using the port by running:\n   \n   ```bash\nsudo lsof -i :5432\n```\n   \n2. Stop the service that is using the port:\n   \n   ```bash\nsudo service postgresql stop\n```\n\nOption 2: Map to a Different Port\n\nFor a more long-term solution, consider mapping to a different port:\n\n- Map local port 5433 to container port 5432 in your Docker configuration (Dockerfile or docker-compose.yml).\n- If using a VM, ensure that port 5433 is forwarded in the host machine configuration.\n\nThis approach prevents conflicts and allows the Docker Postgres container to run without interruption.",
      "Kestra supports conditional branching within a single workflow to reuse shared orchestration logic while allowing dataset-specific behavior. This approach reduces duplication, keeps related steps together, and makes execution paths explicit within a single workflow definition.\n\nWhen to use a single-workflow branch:\n- Reuse common steps (e.g., data validation, enrichment) across datasets.\n- Implement dataset-specific paths (e.g., Yellow vs Green taxi) within the same flow.\n\nHow to implement:\n- Use a branching mechanism in Kestra (e.g., a condition or switch node) to evaluate a runtime variable (such as the dataset type) and route to the appropriate sub-path.\n- Place shared steps before the branch; place dataset-specific steps inside each branch.\n- Decide how branches converge: continue with a common tail after the branch or treat branches as separate end states.\n\nBest practices:\n- Keep branches modular and minimal; extract shared logic into subflows where possible.\n- Centralize error handling and logging to ensure consistency across branches.\n- Test each branch with representative inputs and monitor branch-specific failures separately.\n\nExample (illustrative pseudo YAML):\n```- id: route_by_dataset\n  type: branch\n  when:\n    - condition: ${workflow.variables.dataset_type == 'yellow'}\n      then:\n        - - id: yellow_tasks\n            tasks: [ yellow_validate, yellow_transform ]\n    - else:\n        - - id: green_tasks\n            tasks: [ green_validate, green_transform ]\n- id: end\n  type: terminate\n```\n\nNote: Adapt the exact branch/task names to your Kestra workflow definitions. The key idea is to place shared logic before the branch, branch on dataset/type, and then execute dataset-specific steps within each branch.",
      "To enable CI jobs (running dbt on pull requests), dbt Cloud needs a native integration with GitHub, GitLab, or Azure DevOps — the generic \"Git Clone\" / SSH connection method does not unlock the \"Run on Pull Requests\" trigger and you'll see:\n\n```Triggered by pull requests\nThis feature is only available for dbt repositories connected through dbt Cloud's native integration with Github, Gitlab, or Azure DevOps\n```\n\nThe dbt Cloud Developer plan also doesn't support CI jobs — you need Team or Enterprise. (See the dbt Cloud CI prerequisites.)\n\nSwitch from Git Clone to native GitHub integration\n\n1. Connect your GitHub account in dbt Cloud: Profile Settings → Linked Accounts → connect GitHub and grant the requested permissions. (dbt docs)\n\n2. Disconnect the existing Git Clone connection: Account Settings → Projects → your project → Repository → Disconnect.\n\n3. Reconfigure with the GitHub option, selecting the repository.\n\n4. (If your dbt project lives in a subfolder of the repo) set Project Subdirectory in the project settings to point to that folder.\n\n5. In Deploy → Job → Configuration → Triggers, you should now see \"Run on Pull Requests\" as an available toggle.\n\nCI job runs but says \"valid dbt project was not found\"\n\nUsually one of:\n\n- Project is in a subfolder, but Project Subdirectory isn't set.\n- The CI environment is configured to run on a custom branch that doesn't exist.\n- Uncommitted/unpushed changes.\n\nIn Deploy → Environments → your environment → Settings, set \"Custom branch\" if you're not on the default branch, and confirm it matches a real branch in GitHub."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "GCP gcloud + MS VS Code - gcloud auth hangs",
    "positive": "If you are using MS VS Code and running gcloud in WSL2, when you first try to login to GCP via the gcloud CLI with gcloud auth application-default login, you may encounter an issue where the terminal prints a long OAuth URL and then shows a series of \"not found\" errors for browsers:\n\n```Your browser has been opened to visit:\n    https://accounts.google.com/o/oauth2/auth?response_type=code&client_id=...\n\n/usr/bin/xdg-open: 882: x-www-browser: not found\n/usr/bin/xdg-open: 882: firefox: not found\n/usr/bin/xdg-open: 882: chromium: not found\n...\nxdg-open: no method available for opening '...'\n```\n\nVS Code may show a notification: \"Your application running on port 8085 is available\" with an \"Open in Browser\" button. Clicking it may lead to an error page.\n\nSolution:\n\n1. Hover over the long OAuth URL in the terminal output.\n2. Ctrl + Click the link — VS Code will show a dialog: \"Do you want Code to open the external website?\" with buttons Open, Copy, Configure Trusted Domains, and Cancel.\n3. Click Configure Trusted Domains.\n4. A dropdown will appear with options like:\n   - \"Trust https://accounts.google.com\"\n   - \"Trust google.com and all its subdomains\"\n   - \"Trust all domains (disables link protection)\"\n   - \"Manage Trusted Domains\"\n5. Pick the first or second entry (e.g., \"Trust https://accounts.google.com\").\n\nNext time you run gcloud auth, the login page should pop up via the default browser without issues.",
    "hard_negatives": [
      "This issue occurs when attempting to connect to a GCP VM using VSCode on a Windows machine. You can resolve it by changing a registry value in the registry editor.\n\nOpen the Run command window:\n- Use the shortcut keys Windows + R, or\n- Right-click \"Start\" and click \"Run\".\n\nOpen the Registry Editor:\n- Type regedit in the Run command window, then press Enter.\n\nChange the registry value:\n- Navigate to HKEY_CURRENT_USER\\Software\\Microsoft\\Command Processor.\n- Change the \"Autorun\" value from \"if exists\" to a blank.\n\nAlternatively, you can delete the saved fingerprint within the known_hosts file:\n\nIn Windows, locate the file at C:\\Users\\\\.ssh\\known_hosts and remove the entry for the server.",
      "1. Install the astronomer-cosmos package as a dependency. Refer to the installation guide here and see a Terraform example.\n\n2. Create a new folder, dbt/, inside the dags/ folder of your Composer GCP bucket and copy your dbt-core project there. See the example.\n\n3. Ensure your profiles.yml is configured to authenticate with a service account key. Refer to the BigQuery example.\n\n4. Create a new DAG using the DbtTaskGroup class. Use a ProfileConfig specifying a profiles_yml_filepath that points to the location of your JSON key file. See this example.\n\nYour dbt lineage graph should now appear as tasks inside a task group like this:",
      "Reference:\n\nhttps://cloud.google.com/bigquery/docs/external-data-cloud-storage\n\nSolution:\n\n```python\nfrom google.cloud import bigquery\n\n# Set table_id to the ID of the table to create\ntable_id = f\"{project_id}.{dataset_name}.{table_name}\"\n\n# Construct a BigQuery client object\nclient = bigquery.Client()\n\n# Set the external source format of your table\nexternal_source_format = \"PARQUET\"\n\n# Set the source_uris to point to your data in Google Cloud\nsource_uris = [f'gs://{bucket_name}/{object_key}/*']\n\n# Create ExternalConfig object with external source format\nexternal_config = bigquery.ExternalConfig(external_source_format)\n\n# Set source_uris that point to your data in Google Cloud\nexternal_config.source_uris = source_uris\nexternal_config.autodetect = True\n\ntable = bigquery.Table(table_id)\n\n# Set the external data configuration of the table\ntable.external_data_configuration = external_config\ntable = client.create_table(table)  # Make an API request.\n\nprint(f'Created table with external source: {table_id}')\nprint(f'Format: {table.external_data_configuration.source_format}')\n```",
      "To enable CI jobs (running dbt on pull requests), dbt Cloud needs a native integration with GitHub, GitLab, or Azure DevOps — the generic \"Git Clone\" / SSH connection method does not unlock the \"Run on Pull Requests\" trigger and you'll see:\n\n```Triggered by pull requests\nThis feature is only available for dbt repositories connected through dbt Cloud's native integration with Github, Gitlab, or Azure DevOps\n```\n\nThe dbt Cloud Developer plan also doesn't support CI jobs — you need Team or Enterprise. (See the dbt Cloud CI prerequisites.)\n\nSwitch from Git Clone to native GitHub integration\n\n1. Connect your GitHub account in dbt Cloud: Profile Settings → Linked Accounts → connect GitHub and grant the requested permissions. (dbt docs)\n\n2. Disconnect the existing Git Clone connection: Account Settings → Projects → your project → Repository → Disconnect.\n\n3. Reconfigure with the GitHub option, selecting the repository.\n\n4. (If your dbt project lives in a subfolder of the repo) set Project Subdirectory in the project settings to point to that folder.\n\n5. In Deploy → Job → Configuration → Triggers, you should now see \"Run on Pull Requests\" as an available toggle.\n\nCI job runs but says \"valid dbt project was not found\"\n\nUsually one of:\n\n- Project is in a subfolder, but Project Subdirectory isn't set.\n- The CI environment is configured to run on a custom branch that doesn't exist.\n- Uncommitted/unpushed changes.\n\nIn Deploy → Environments → your environment → Settings, set \"Custom branch\" if you're not on the default branch, and confirm it matches a real branch in GitHub.",
      "Instruction on how to store secrets that will be available in GitHub Codespaces. See Managing your account-specific secrets for GitHub Codespaces - GitHub Docs."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "Java Kafka: <project_name>-1.0-SNAPSHOT.jar errors: package xxx does not exist even after gradle build",
    "positive": "In my setup, all of the dependencies listed in build.gradle were not installed in -1.0-SNAPSHOT.jar.\n\nSolution:\n\n1. In the build.gradle file, add the following at the end:\n   \n   ```groovy\nshadowJar {\n       archiveBaseName = \"java-kafka-rides\"\n       archiveClassifier = ''\n   }\n```\n\n2. In the command line, run:\n   \n   ```bash\ngradle shadowjar\n```\n\n3. Execute the script from java-kafka-rides-1.0-SNAPSHOT.jar created by the shadowjar.",
    "hard_negatives": [
      "Error:\n\n```plaintext\nThis project does not have a development environment configured. Please create a development environment and configure your development credentials to use the dbt IDE.\n```\n\nThe error message provides guidance on resolving this issue. Follow the guide in the dbt cloud setup documentation. Additional instructions can be found in the video @1:42.",
      "The Faust library is no longer maintained — see https://github.com/robinhood/faust. We don't recommend using it for the course.\n\nIf you don't know Java, follow the Python streaming materials in the course repo (under 06-streaming/python/) instead, which use Kafka or Redpanda directly. Watching the Java videos to understand the streaming concepts is still useful even if you skip the coding parts.",
      "In join queries, if you mention the column name directly or enclose it in single quotes, you'll encounter an error saying \"column does not exist\".\n\nSolution: Enclose the column names in double quotes, and it will work correctly.",
      "You might have installed Docker via snap. Run the following command to verify:\n\n```bash\nsudo snap status docker\n```\n\nIf you receive the response:\n\n```error: unknown command \"status\", see 'snap help'.\n```\n\nThen uninstall Docker and install it via the official website.\n\nError message: \"Bind for 0.0.0.0:5432 failed: port is already allocated.\"",
      "When running terraform destroy, the following error can occur:\n\n```Do you really want to destroy all resources?\n\nTerraform will destroy all your managed infrastructure, as shown above.\n\nThere is no undo. Only 'yes' will be accepted to confirm.\n\nEnter a value: yes\n\ngoogle_bigquery_dataset.homework_dataset: Destroying... [id=projects/terraform-demo-449214/datasets/homework_dataset]\n\n╷\n\n│ Error: Error when reading or editing Dataset: googleapi: Error 400: Dataset terraform-demo-449214:homework_dataset is still in use, resourceInUse\n```\n\nThis is because the dataset is still in use by a table. To delete the dataset, set the delete_contents_on_destroy property to true in the main.tf file."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "Homework: What are homework and project deadlines?",
    "positive": "You don't need a confirmation email. You're accepted. You can start learning and submitting homework without registering. Registration was just to gauge interest before the start date.",
    "hard_negatives": [
      "Deadlines are published per cohort. Find them in:\n\n- The current cohort's folder in the course repo.\n- The course website (link is in the cohort folder's README).\n- The course Google Calendar (also linked from the cohort folder).\n\nAlso keep an eye on @Au-Tomator posts in Slack for extension announcements. The submission form may show an updated deadline if instructors have changed it.",
      "When running terraform destroy, the following error can occur:\n\n```Do you really want to destroy all resources?\n\nTerraform will destroy all your managed infrastructure, as shown above.\n\nThere is no undo. Only 'yes' will be accepted to confirm.\n\nEnter a value: yes\n\ngoogle_bigquery_dataset.homework_dataset: Destroying... [id=projects/terraform-demo-449214/datasets/homework_dataset]\n\n╷\n\n│ Error: Error when reading or editing Dataset: googleapi: Error 400: Dataset terraform-demo-449214:homework_dataset is still in use, resourceInUse\n```\n\nThis is because the dataset is still in use by a table. To delete the dataset, set the delete_contents_on_destroy property to true in the main.tf file.",
      "To read from multiple topics in the same Spark session, follow these steps:\n\n1. Initiate a Spark Session:\n   \n   ```python\nspark = (SparkSession\n       .builder\n       .appName(app_name)\n       .master(master=master)\n       .getOrCreate())\n   \n   spark.streams.resetTerminated()\n```\n\n2. Read Streams from Multiple Topics:\n   \n   ```python\nquery1 = spark\n       .readStream\n       ...\n       ...\n       .load()\n   \n   query2 = spark\n       .readStream\n       ...\n       ...\n       .load()\n   \n   query3 = spark\n       .readStream\n       ...\n       ...\n       .load()\n```\n\n3. Start the Queries:\n   \n   ```python\nquery1.start()\n   query2.start()\n   query3.start()\n```\n\n4. Await Termination:\n   \n   ```python\nspark.streams.awaitAnyTermination()  # Waits for any one of the queries to receive a kill signal or error failure. This is asynchronous.\n```\n\n   Note: query3.start().awaitTermination() is a blocking call. It works well when we are reading only from one topic.",
      "Kestra supports conditional branching within a single workflow to reuse shared orchestration logic while allowing dataset-specific behavior. This approach reduces duplication, keeps related steps together, and makes execution paths explicit within a single workflow definition.\n\nWhen to use a single-workflow branch:\n- Reuse common steps (e.g., data validation, enrichment) across datasets.\n- Implement dataset-specific paths (e.g., Yellow vs Green taxi) within the same flow.\n\nHow to implement:\n- Use a branching mechanism in Kestra (e.g., a condition or switch node) to evaluate a runtime variable (such as the dataset type) and route to the appropriate sub-path.\n- Place shared steps before the branch; place dataset-specific steps inside each branch.\n- Decide how branches converge: continue with a common tail after the branch or treat branches as separate end states.\n\nBest practices:\n- Keep branches modular and minimal; extract shared logic into subflows where possible.\n- Centralize error handling and logging to ensure consistency across branches.\n- Test each branch with representative inputs and monitor branch-specific failures separately.\n\nExample (illustrative pseudo YAML):\n```- id: route_by_dataset\n  type: branch\n  when:\n    - condition: ${workflow.variables.dataset_type == 'yellow'}\n      then:\n        - - id: yellow_tasks\n            tasks: [ yellow_validate, yellow_transform ]\n    - else:\n        - - id: green_tasks\n            tasks: [ green_validate, green_transform ]\n- id: end\n  type: terminate\n```\n\nNote: Adapt the exact branch/task names to your Kestra workflow definitions. The key idea is to place shared logic before the branch, branch on dataset/type, and then execute dataset-specific steps within each branch.",
      "To enable CI jobs (running dbt on pull requests), dbt Cloud needs a native integration with GitHub, GitLab, or Azure DevOps — the generic \"Git Clone\" / SSH connection method does not unlock the \"Run on Pull Requests\" trigger and you'll see:\n\n```Triggered by pull requests\nThis feature is only available for dbt repositories connected through dbt Cloud's native integration with Github, Gitlab, or Azure DevOps\n```\n\nThe dbt Cloud Developer plan also doesn't support CI jobs — you need Team or Enterprise. (See the dbt Cloud CI prerequisites.)\n\nSwitch from Git Clone to native GitHub integration\n\n1. Connect your GitHub account in dbt Cloud: Profile Settings → Linked Accounts → connect GitHub and grant the requested permissions. (dbt docs)\n\n2. Disconnect the existing Git Clone connection: Account Settings → Projects → your project → Repository → Disconnect.\n\n3. Reconfigure with the GitHub option, selecting the repository.\n\n4. (If your dbt project lives in a subfolder of the repo) set Project Subdirectory in the project settings to point to that folder.\n\n5. In Deploy → Job → Configuration → Triggers, you should now see \"Run on Pull Requests\" as an available toggle.\n\nCI job runs but says \"valid dbt project was not found\"\n\nUsually one of:\n\n- Project is in a subfolder, but Project Subdirectory isn't set.\n- The CI environment is configured to run on a custom branch that doesn't exist.\n- Uncommitted/unpushed changes.\n\nIn Deploy → Environments → your environment → Settings, set \"Custom branch\" if you're not on the default branch, and confirm it matches a real branch in GitHub."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "DBT: I ran dbt run without specifying variable which gave me a table of 100 rows. I ran again with the variable value specified but my table still has 100 rows in BQ.",
    "positive": "Remove the dataset from BigQuery created by dbt and run again (with test disabled) to ensure the dataset created has all the rows.\n\nDBT: Why am I getting a new dataset after running my CI/CD Job? / What is this new dbt dataset in BigQuery?\n\nWhen you create the CI/CD job, under 'Compare Changes against an environment (Deferral)' make sure that you select 'No; do not defer to another environment'. Otherwise, dbt won’t merge your dev models into production models; it will create a new environment called ‘dbt_cloud_pr_number of pull request’",
    "hard_negatives": [
      "Dbt provides a mechanism called ref to manage dependencies between models. By referencing other models using the ref keyword in SQL, dbt automatically understands the dependencies and ensures the correct execution order.",
      "To enable CI jobs (running dbt on pull requests), dbt Cloud needs a native integration with GitHub, GitLab, or Azure DevOps — the generic \"Git Clone\" / SSH connection method does not unlock the \"Run on Pull Requests\" trigger and you'll see:\n\n```Triggered by pull requests\nThis feature is only available for dbt repositories connected through dbt Cloud's native integration with Github, Gitlab, or Azure DevOps\n```\n\nThe dbt Cloud Developer plan also doesn't support CI jobs — you need Team or Enterprise. (See the dbt Cloud CI prerequisites.)\n\nSwitch from Git Clone to native GitHub integration\n\n1. Connect your GitHub account in dbt Cloud: Profile Settings → Linked Accounts → connect GitHub and grant the requested permissions. (dbt docs)\n\n2. Disconnect the existing Git Clone connection: Account Settings → Projects → your project → Repository → Disconnect.\n\n3. Reconfigure with the GitHub option, selecting the repository.\n\n4. (If your dbt project lives in a subfolder of the repo) set Project Subdirectory in the project settings to point to that folder.\n\n5. In Deploy → Job → Configuration → Triggers, you should now see \"Run on Pull Requests\" as an available toggle.\n\nCI job runs but says \"valid dbt project was not found\"\n\nUsually one of:\n\n- Project is in a subfolder, but Project Subdirectory isn't set.\n- The CI environment is configured to run on a custom branch that doesn't exist.\n- Uncommitted/unpushed changes.\n\nIn Deploy → Environments → your environment → Settings, set \"Custom branch\" if you're not on the default branch, and confirm it matches a real branch in GitHub.",
      "Error:\n\n```plaintext\nThis project does not have a development environment configured. Please create a development environment and configure your development credentials to use the dbt IDE.\n```\n\nThe error message provides guidance on resolving this issue. Follow the guide in the dbt cloud setup documentation. Additional instructions can be found in the video @1:42.",
      "Make sure you don’t have a dlt.py file saved in the same directory as your working file.",
      "1. Install the astronomer-cosmos package as a dependency. Refer to the installation guide here and see a Terraform example.\n\n2. Create a new folder, dbt/, inside the dags/ folder of your Composer GCP bucket and copy your dbt-core project there. See the example.\n\n3. Ensure your profiles.yml is configured to authenticate with a service account key. Refer to the BigQuery example.\n\n4. Create a new DAG using the DbtTaskGroup class. Use a ProfileConfig specifying a profiles_yml_filepath that points to the location of your JSON key file. See this example.\n\nYour dbt lineage graph should now appear as tasks inside a task group like this:"
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "Why does Spark write multiple parquet files after repartitioning a DataFrame?",
    "positive": "Spark processes data in partitions. When you write a DataFrame to disk, Spark writes each partition as a separate output file. For example:\n\n```python\ntrips.repartition(4).write.parquet(\"output/\")\n```\n\nThis creates four parquet files because the DataFrame now has four partitions. This behavior enables Spark to write data in parallel and can improve performance on large datasets.",
    "hard_negatives": [
      "To read from multiple topics in the same Spark session, follow these steps:\n\n1. Initiate a Spark Session:\n   \n   ```python\nspark = (SparkSession\n       .builder\n       .appName(app_name)\n       .master(master=master)\n       .getOrCreate())\n   \n   spark.streams.resetTerminated()\n```\n\n2. Read Streams from Multiple Topics:\n   \n   ```python\nquery1 = spark\n       .readStream\n       ...\n       ...\n       .load()\n   \n   query2 = spark\n       .readStream\n       ...\n       ...\n       .load()\n   \n   query3 = spark\n       .readStream\n       ...\n       ...\n       .load()\n```\n\n3. Start the Queries:\n   \n   ```python\nquery1.start()\n   query2.start()\n   query3.start()\n```\n\n4. Await Termination:\n   \n   ```python\nspark.streams.awaitAnyTermination()  # Waits for any one of the queries to receive a kill signal or error failure. This is asynchronous.\n```\n\n   Note: query3.start().awaitTermination() is a blocking call. It works well when we are reading only from one topic.",
      "Reference:\n\nhttps://cloud.google.com/bigquery/docs/external-data-cloud-storage\n\nSolution:\n\n```python\nfrom google.cloud import bigquery\n\n# Set table_id to the ID of the table to create\ntable_id = f\"{project_id}.{dataset_name}.{table_name}\"\n\n# Construct a BigQuery client object\nclient = bigquery.Client()\n\n# Set the external source format of your table\nexternal_source_format = \"PARQUET\"\n\n# Set the source_uris to point to your data in Google Cloud\nsource_uris = [f'gs://{bucket_name}/{object_key}/*']\n\n# Create ExternalConfig object with external source format\nexternal_config = bigquery.ExternalConfig(external_source_format)\n\n# Set source_uris that point to your data in Google Cloud\nexternal_config.source_uris = source_uris\nexternal_config.autodetect = True\n\ntable = bigquery.Table(table_id)\n\n# Set the external data configuration of the table\ntable.external_data_configuration = external_config\ntable = client.create_table(table)  # Make an API request.\n\nprint(f'Created table with external source: {table_id}')\nprint(f'Format: {table.external_data_configuration.source_format}')\n```",
      "Kestra supports conditional branching within a single workflow to reuse shared orchestration logic while allowing dataset-specific behavior. This approach reduces duplication, keeps related steps together, and makes execution paths explicit within a single workflow definition.\n\nWhen to use a single-workflow branch:\n- Reuse common steps (e.g., data validation, enrichment) across datasets.\n- Implement dataset-specific paths (e.g., Yellow vs Green taxi) within the same flow.\n\nHow to implement:\n- Use a branching mechanism in Kestra (e.g., a condition or switch node) to evaluate a runtime variable (such as the dataset type) and route to the appropriate sub-path.\n- Place shared steps before the branch; place dataset-specific steps inside each branch.\n- Decide how branches converge: continue with a common tail after the branch or treat branches as separate end states.\n\nBest practices:\n- Keep branches modular and minimal; extract shared logic into subflows where possible.\n- Centralize error handling and logging to ensure consistency across branches.\n- Test each branch with representative inputs and monitor branch-specific failures separately.\n\nExample (illustrative pseudo YAML):\n```- id: route_by_dataset\n  type: branch\n  when:\n    - condition: ${workflow.variables.dataset_type == 'yellow'}\n      then:\n        - - id: yellow_tasks\n            tasks: [ yellow_validate, yellow_transform ]\n    - else:\n        - - id: green_tasks\n            tasks: [ green_validate, green_transform ]\n- id: end\n  type: terminate\n```\n\nNote: Adapt the exact branch/task names to your Kestra workflow definitions. The key idea is to place shared logic before the branch, branch on dataset/type, and then execute dataset-specific steps within each branch.",
      "Remove the dataset from BigQuery created by dbt and run again (with test disabled) to ensure the dataset created has all the rows.\n\nDBT: Why am I getting a new dataset after running my CI/CD Job? / What is this new dbt dataset in BigQuery?\n\nWhen you create the CI/CD job, under 'Compare Changes against an environment (Deferral)' make sure that you select 'No; do not defer to another environment'. Otherwise, dbt won’t merge your dev models into production models; it will create a new environment called ‘dbt_cloud_pr_number of pull request’",
      "This issue arises from the way deduplication is handled in two staging files.\n\nSolution:\n\n- Add an ORDER BY clause in the PARTITION BY section of both staging files.\n- Continue adding columns to the ORDER BY clause until the row count in the fact_trips table is consistent upon re-running the model.\n\nExplanation:\n\nWe partition by vendor_id and pickup_datetime, selecting the first row (rn=1) from these partitions. These partitions lack an order, so every execution might yield a different first row. The inconsistency leads to different rows being processed, possibly with or without an unknown borough. Consequently, the fact_trips model discards a varying number of rows based on the presence of unknown boroughs."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "GitHub Codespaces: How to store secrets",
    "positive": "Instruction on how to store secrets that will be available in GitHub Codespaces. See Managing your account-specific secrets for GitHub Codespaces - GitHub Docs.",
    "hard_negatives": [
      "See a list of datasets here: GitHub Datasets",
      "To enable CI jobs (running dbt on pull requests), dbt Cloud needs a native integration with GitHub, GitLab, or Azure DevOps — the generic \"Git Clone\" / SSH connection method does not unlock the \"Run on Pull Requests\" trigger and you'll see:\n\n```Triggered by pull requests\nThis feature is only available for dbt repositories connected through dbt Cloud's native integration with Github, Gitlab, or Azure DevOps\n```\n\nThe dbt Cloud Developer plan also doesn't support CI jobs — you need Team or Enterprise. (See the dbt Cloud CI prerequisites.)\n\nSwitch from Git Clone to native GitHub integration\n\n1. Connect your GitHub account in dbt Cloud: Profile Settings → Linked Accounts → connect GitHub and grant the requested permissions. (dbt docs)\n\n2. Disconnect the existing Git Clone connection: Account Settings → Projects → your project → Repository → Disconnect.\n\n3. Reconfigure with the GitHub option, selecting the repository.\n\n4. (If your dbt project lives in a subfolder of the repo) set Project Subdirectory in the project settings to point to that folder.\n\n5. In Deploy → Job → Configuration → Triggers, you should now see \"Run on Pull Requests\" as an available toggle.\n\nCI job runs but says \"valid dbt project was not found\"\n\nUsually one of:\n\n- Project is in a subfolder, but Project Subdirectory isn't set.\n- The CI environment is configured to run on a custom branch that doesn't exist.\n- Uncommitted/unpushed changes.\n\nIn Deploy → Environments → your environment → Settings, set \"Custom branch\" if you're not on the default branch, and confirm it matches a real branch in GitHub.",
      "The Faust library is no longer maintained — see https://github.com/robinhood/faust. We don't recommend using it for the course.\n\nIf you don't know Java, follow the Python streaming materials in the course repo (under 06-streaming/python/) instead, which use Kafka or Redpanda directly. Watching the Java videos to understand the streaming concepts is still useful even if you skip the coding parts.",
      "If you are using MS VS Code and running gcloud in WSL2, when you first try to login to GCP via the gcloud CLI with gcloud auth application-default login, you may encounter an issue where the terminal prints a long OAuth URL and then shows a series of \"not found\" errors for browsers:\n\n```Your browser has been opened to visit:\n    https://accounts.google.com/o/oauth2/auth?response_type=code&client_id=...\n\n/usr/bin/xdg-open: 882: x-www-browser: not found\n/usr/bin/xdg-open: 882: firefox: not found\n/usr/bin/xdg-open: 882: chromium: not found\n...\nxdg-open: no method available for opening '...'\n```\n\nVS Code may show a notification: \"Your application running on port 8085 is available\" with an \"Open in Browser\" button. Clicking it may lead to an error page.\n\nSolution:\n\n1. Hover over the long OAuth URL in the terminal output.\n2. Ctrl + Click the link — VS Code will show a dialog: \"Do you want Code to open the external website?\" with buttons Open, Copy, Configure Trusted Domains, and Cancel.\n3. Click Configure Trusted Domains.\n4. A dropdown will appear with options like:\n   - \"Trust https://accounts.google.com\"\n   - \"Trust google.com and all its subdomains\"\n   - \"Trust all domains (disables link protection)\"\n   - \"Manage Trusted Domains\"\n5. Pick the first or second entry (e.g., \"Trust https://accounts.google.com\").\n\nNext time you run gcloud auth, the login page should pop up via the default browser without issues.",
      "To read from multiple topics in the same Spark session, follow these steps:\n\n1. Initiate a Spark Session:\n   \n   ```python\nspark = (SparkSession\n       .builder\n       .appName(app_name)\n       .master(master=master)\n       .getOrCreate())\n   \n   spark.streams.resetTerminated()\n```\n\n2. Read Streams from Multiple Topics:\n   \n   ```python\nquery1 = spark\n       .readStream\n       ...\n       ...\n       .load()\n   \n   query2 = spark\n       .readStream\n       ...\n       ...\n       .load()\n   \n   query3 = spark\n       .readStream\n       ...\n       ...\n       .load()\n```\n\n3. Start the Queries:\n   \n   ```python\nquery1.start()\n   query2.start()\n   query3.start()\n```\n\n4. Await Termination:\n   \n   ```python\nspark.streams.awaitAnyTermination()  # Waits for any one of the queries to receive a kill signal or error failure. This is asynchronous.\n```\n\n   Note: query3.start().awaitTermination() is a blocking call. It works well when we are reading only from one topic."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "Problem with importing the dlt or dlt.sources module",
    "positive": "Make sure you don’t have a dlt.py file saved in the same directory as your working file.",
    "hard_negatives": [
      "The issue comes down to how Unix processes produce output and how Kestra interprets it:\n\n1. Python's logging module writes to stderr by default. If you, for instance, call logging.basicConfig() without specifying a stream argument, the root handler sends basically everything to stderr.\n\n2. Kestra maps the two standard streams to its own log levels. Anything the container writes to stdout becomes a Kestra DEBUG entry and anything written on stderr becomes ERROR. There is no middle ground.\n\nThe fix\n\nGood news is that the fix is simple: redirect Python logging to stdout:\n\n```python\nimport sys\nimport logging\n\nlogging.basicConfig(\n    level=logging.INFO,\n    format=\"%(asctime)s %(levelname)s %(message)s\",\n    stream=sys.stdout,\n)\n```\n\nThat single stream=sys.stdout argument is enough. After the change, your informational messages will show up as DEBUG in Kestra.",
      "Reference:\n\nhttps://cloud.google.com/bigquery/docs/external-data-cloud-storage\n\nSolution:\n\n```python\nfrom google.cloud import bigquery\n\n# Set table_id to the ID of the table to create\ntable_id = f\"{project_id}.{dataset_name}.{table_name}\"\n\n# Construct a BigQuery client object\nclient = bigquery.Client()\n\n# Set the external source format of your table\nexternal_source_format = \"PARQUET\"\n\n# Set the source_uris to point to your data in Google Cloud\nsource_uris = [f'gs://{bucket_name}/{object_key}/*']\n\n# Create ExternalConfig object with external source format\nexternal_config = bigquery.ExternalConfig(external_source_format)\n\n# Set source_uris that point to your data in Google Cloud\nexternal_config.source_uris = source_uris\nexternal_config.autodetect = True\n\ntable = bigquery.Table(table_id)\n\n# Set the external data configuration of the table\ntable.external_data_configuration = external_config\ntable = client.create_table(table)  # Make an API request.\n\nprint(f'Created table with external source: {table_id}')\nprint(f'Format: {table.external_data_configuration.source_format}')\n```",
      "To enable CI jobs (running dbt on pull requests), dbt Cloud needs a native integration with GitHub, GitLab, or Azure DevOps — the generic \"Git Clone\" / SSH connection method does not unlock the \"Run on Pull Requests\" trigger and you'll see:\n\n```Triggered by pull requests\nThis feature is only available for dbt repositories connected through dbt Cloud's native integration with Github, Gitlab, or Azure DevOps\n```\n\nThe dbt Cloud Developer plan also doesn't support CI jobs — you need Team or Enterprise. (See the dbt Cloud CI prerequisites.)\n\nSwitch from Git Clone to native GitHub integration\n\n1. Connect your GitHub account in dbt Cloud: Profile Settings → Linked Accounts → connect GitHub and grant the requested permissions. (dbt docs)\n\n2. Disconnect the existing Git Clone connection: Account Settings → Projects → your project → Repository → Disconnect.\n\n3. Reconfigure with the GitHub option, selecting the repository.\n\n4. (If your dbt project lives in a subfolder of the repo) set Project Subdirectory in the project settings to point to that folder.\n\n5. In Deploy → Job → Configuration → Triggers, you should now see \"Run on Pull Requests\" as an available toggle.\n\nCI job runs but says \"valid dbt project was not found\"\n\nUsually one of:\n\n- Project is in a subfolder, but Project Subdirectory isn't set.\n- The CI environment is configured to run on a custom branch that doesn't exist.\n- Uncommitted/unpushed changes.\n\nIn Deploy → Environments → your environment → Settings, set \"Custom branch\" if you're not on the default branch, and confirm it matches a real branch in GitHub.",
      "Dbt provides a mechanism called ref to manage dependencies between models. By referencing other models using the ref keyword in SQL, dbt automatically understands the dependencies and ensures the correct execution order.",
      "Error:\n\n```plaintext\nThis project does not have a development environment configured. Please create a development environment and configure your development credentials to use the dbt IDE.\n```\n\nThe error message provides guidance on resolving this issue. Follow the guide in the dbt cloud setup documentation. Additional instructions can be found in the video @1:42."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "Can I use the Faust library for the streaming module?",
    "positive": "The Faust library is no longer maintained — see https://github.com/robinhood/faust. We don't recommend using it for the course.\n\nIf you don't know Java, follow the Python streaming materials in the course repo (under 06-streaming/python/) instead, which use Kafka or Redpanda directly. Watching the Java videos to understand the streaming concepts is still useful even if you skip the coding parts.",
    "hard_negatives": [
      "The issue comes down to how Unix processes produce output and how Kestra interprets it:\n\n1. Python's logging module writes to stderr by default. If you, for instance, call logging.basicConfig() without specifying a stream argument, the root handler sends basically everything to stderr.\n\n2. Kestra maps the two standard streams to its own log levels. Anything the container writes to stdout becomes a Kestra DEBUG entry and anything written on stderr becomes ERROR. There is no middle ground.\n\nThe fix\n\nGood news is that the fix is simple: redirect Python logging to stdout:\n\n```python\nimport sys\nimport logging\n\nlogging.basicConfig(\n    level=logging.INFO,\n    format=\"%(asctime)s %(levelname)s %(message)s\",\n    stream=sys.stdout,\n)\n```\n\nThat single stream=sys.stdout argument is enough. After the change, your informational messages will show up as DEBUG in Kestra.",
      "To read from multiple topics in the same Spark session, follow these steps:\n\n1. Initiate a Spark Session:\n   \n   ```python\nspark = (SparkSession\n       .builder\n       .appName(app_name)\n       .master(master=master)\n       .getOrCreate())\n   \n   spark.streams.resetTerminated()\n```\n\n2. Read Streams from Multiple Topics:\n   \n   ```python\nquery1 = spark\n       .readStream\n       ...\n       ...\n       .load()\n   \n   query2 = spark\n       .readStream\n       ...\n       ...\n       .load()\n   \n   query3 = spark\n       .readStream\n       ...\n       ...\n       .load()\n```\n\n3. Start the Queries:\n   \n   ```python\nquery1.start()\n   query2.start()\n   query3.start()\n```\n\n4. Await Termination:\n   \n   ```python\nspark.streams.awaitAnyTermination()  # Waits for any one of the queries to receive a kill signal or error failure. This is asynchronous.\n```\n\n   Note: query3.start().awaitTermination() is a blocking call. It works well when we are reading only from one topic.",
      "Kestra supports conditional branching within a single workflow to reuse shared orchestration logic while allowing dataset-specific behavior. This approach reduces duplication, keeps related steps together, and makes execution paths explicit within a single workflow definition.\n\nWhen to use a single-workflow branch:\n- Reuse common steps (e.g., data validation, enrichment) across datasets.\n- Implement dataset-specific paths (e.g., Yellow vs Green taxi) within the same flow.\n\nHow to implement:\n- Use a branching mechanism in Kestra (e.g., a condition or switch node) to evaluate a runtime variable (such as the dataset type) and route to the appropriate sub-path.\n- Place shared steps before the branch; place dataset-specific steps inside each branch.\n- Decide how branches converge: continue with a common tail after the branch or treat branches as separate end states.\n\nBest practices:\n- Keep branches modular and minimal; extract shared logic into subflows where possible.\n- Centralize error handling and logging to ensure consistency across branches.\n- Test each branch with representative inputs and monitor branch-specific failures separately.\n\nExample (illustrative pseudo YAML):\n```- id: route_by_dataset\n  type: branch\n  when:\n    - condition: ${workflow.variables.dataset_type == 'yellow'}\n      then:\n        - - id: yellow_tasks\n            tasks: [ yellow_validate, yellow_transform ]\n    - else:\n        - - id: green_tasks\n            tasks: [ green_validate, green_transform ]\n- id: end\n  type: terminate\n```\n\nNote: Adapt the exact branch/task names to your Kestra workflow definitions. The key idea is to place shared logic before the branch, branch on dataset/type, and then execute dataset-specific steps within each branch.",
      "Use the PostgreSQL COPY FROM feature, which is compatible with CSV files.\n\nSteps:\n\n1. Create the Table\n   \n   First, create your table (example):\n   \n   ```sql\nCREATE TABLE taxis (\n   \n   …\n   \n   );\n```\n\n2. Use COPY Functionality\n\n   Use the COPY command (example):\n\n   ```sql\nCOPY taxis FROM PROGRAM\n   'url'\n   WITH (\n   FORMAT csv,\n   HEADER true,\n   ENCODING utf8\n   );\n```\n\n   - Syntax for COPY:\n\n   ```sql\nCOPY table_name [ ( column_name [, ...] ) ]\n   FROM { 'filename' | PROGRAM 'command' | STDIN }\n   [ [ WITH ] ( option [, ...] ) ]\n   [ WHERE condition ]\n```",
      "1. Install the astronomer-cosmos package as a dependency. Refer to the installation guide here and see a Terraform example.\n\n2. Create a new folder, dbt/, inside the dags/ folder of your Composer GCP bucket and copy your dbt-core project there. See the example.\n\n3. Ensure your profiles.yml is configured to authenticate with a service account key. Refer to the BigQuery example.\n\n4. Create a new DAG using the DbtTaskGroup class. Use a ProfileConfig specifying a profiles_yml_filepath that points to the location of your JSON key file. See this example.\n\nYour dbt lineage graph should now appear as tasks inside a task group like this:"
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "Course: I have registered for the Data Engineering Bootcamp. When can I expect to receive the confirmation email?",
    "positive": "You don't need a confirmation email. You're accepted. You can start learning and submitting homework without registering. Registration was just to gauge interest before the start date.",
    "hard_negatives": [
      "See a list of datasets here: GitHub Datasets",
      "Deadlines are published per cohort. Find them in:\n\n- The current cohort's folder in the course repo.\n- The course website (link is in the cohort folder's README).\n- The course Google Calendar (also linked from the cohort folder).\n\nAlso keep an eye on @Au-Tomator posts in Slack for extension announcements. The submission form may show an updated deadline if instructors have changed it.",
      "When running terraform destroy, the following error can occur:\n\n```Do you really want to destroy all resources?\n\nTerraform will destroy all your managed infrastructure, as shown above.\n\nThere is no undo. Only 'yes' will be accepted to confirm.\n\nEnter a value: yes\n\ngoogle_bigquery_dataset.homework_dataset: Destroying... [id=projects/terraform-demo-449214/datasets/homework_dataset]\n\n╷\n\n│ Error: Error when reading or editing Dataset: googleapi: Error 400: Dataset terraform-demo-449214:homework_dataset is still in use, resourceInUse\n```\n\nThis is because the dataset is still in use by a table. To delete the dataset, set the delete_contents_on_destroy property to true in the main.tf file.",
      "Many people aren’t getting an exact match, but are very close to one of the options. It is suggested to choose the closest option.",
      "Make sure you don’t have a dlt.py file saved in the same directory as your working file."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "When should partitioning be used instead of clustering, and vice versa?",
    "positive": "Spark processes data in partitions. When you write a DataFrame to disk, Spark writes each partition as a separate output file. For example:\n\n```python\ntrips.repartition(4).write.parquet(\"output/\")\n```\n\nThis creates four parquet files because the DataFrame now has four partitions. This behavior enables Spark to write data in parallel and can improve performance on large datasets.",
    "hard_negatives": [
      "This issue arises from the way deduplication is handled in two staging files.\n\nSolution:\n\n- Add an ORDER BY clause in the PARTITION BY section of both staging files.\n- Continue adding columns to the ORDER BY clause until the row count in the fact_trips table is consistent upon re-running the model.\n\nExplanation:\n\nWe partition by vendor_id and pickup_datetime, selecting the first row (rn=1) from these partitions. These partitions lack an order, so every execution might yield a different first row. The inconsistency leads to different rows being processed, possibly with or without an unknown borough. Consequently, the fact_trips model discards a varying number of rows based on the presence of unknown boroughs.",
      "Kestra supports conditional branching within a single workflow to reuse shared orchestration logic while allowing dataset-specific behavior. This approach reduces duplication, keeps related steps together, and makes execution paths explicit within a single workflow definition.\n\nWhen to use a single-workflow branch:\n- Reuse common steps (e.g., data validation, enrichment) across datasets.\n- Implement dataset-specific paths (e.g., Yellow vs Green taxi) within the same flow.\n\nHow to implement:\n- Use a branching mechanism in Kestra (e.g., a condition or switch node) to evaluate a runtime variable (such as the dataset type) and route to the appropriate sub-path.\n- Place shared steps before the branch; place dataset-specific steps inside each branch.\n- Decide how branches converge: continue with a common tail after the branch or treat branches as separate end states.\n\nBest practices:\n- Keep branches modular and minimal; extract shared logic into subflows where possible.\n- Centralize error handling and logging to ensure consistency across branches.\n- Test each branch with representative inputs and monitor branch-specific failures separately.\n\nExample (illustrative pseudo YAML):\n```- id: route_by_dataset\n  type: branch\n  when:\n    - condition: ${workflow.variables.dataset_type == 'yellow'}\n      then:\n        - - id: yellow_tasks\n            tasks: [ yellow_validate, yellow_transform ]\n    - else:\n        - - id: green_tasks\n            tasks: [ green_validate, green_transform ]\n- id: end\n  type: terminate\n```\n\nNote: Adapt the exact branch/task names to your Kestra workflow definitions. The key idea is to place shared logic before the branch, branch on dataset/type, and then execute dataset-specific steps within each branch.",
      "Many people aren’t getting an exact match, but are very close to one of the options. It is suggested to choose the closest option.",
      "See a list of datasets here: GitHub Datasets",
      "The Faust library is no longer maintained — see https://github.com/robinhood/faust. We don't recommend using it for the course.\n\nIf you don't know Java, follow the Python streaming materials in the course repo (under 06-streaming/python/) instead, which use Kafka or Redpanda directly. Watching the Java videos to understand the streaming concepts is still useful even if you skip the coding parts."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "Docker: Setting up Docker on Mac",
    "positive": "You might have installed Docker via snap. Run the following command to verify:\n\n```bash\nsudo snap status docker\n```\n\nIf you receive the response:\n\n```error: unknown command \"status\", see 'snap help'.\n```\n\nThen uninstall Docker and install it via the official website.\n\nError message: \"Bind for 0.0.0.0:5432 failed: port is already allocated.\"",
    "hard_negatives": [
      "When attempting to start the Docker Postgres container, you may encounter the error message:\n\n```Error - postgres port is already in use.\n```\n\nOption 1: Identify and Stop the Service\n\n1. Determine which service is using the port by running:\n   \n   ```bash\nsudo lsof -i :5432\n```\n   \n2. Stop the service that is using the port:\n   \n   ```bash\nsudo service postgresql stop\n```\n\nOption 2: Map to a Different Port\n\nFor a more long-term solution, consider mapping to a different port:\n\n- Map local port 5433 to container port 5432 in your Docker configuration (Dockerfile or docker-compose.yml).\n- If using a VM, ensure that port 5433 is forwarded in the host machine configuration.\n\nThis approach prevents conflicts and allows the Docker Postgres container to run without interruption.",
      "When you log into PgAdmin and see an empty database, the following solution can help:\n\nRun:\n   \n```bash\ndocker-compose up\n```\n\nAnd at the same time run:\n\n```bash\ndocker build -t taxi_ingest:v001 .\n\n# NETWORK NAME IS THE SAME AS THAT CREATED BY DOCKER COMPOSE\ndocker run -it \\\n  --network=pg-network \\\n  taxi_ingest:v001 \\\n  --user=postgres \\\n  --password=postgres \\\n  --host=db \\\n  --port=5432 \\\n  --db=ny_taxi \\\n  --table_name=green_tripdata \\\n  --url=${URL}\n```\n\nIt's important to use the same --network as stated in the docker-compose.yaml file.\n\nThe docker-compose.yaml file might not specify a network, as shown below:\n\n```yaml\nservices:\n  db:\n    container_name: postgres\n    image: postgres:17-alpine\n    environment:\n      ...\n    ports:\n      - '5433:5432'\n    volumes:\n      - ...\n  pgadmin:\n    container_name: pgadmin\n    image: dpage/pgadmin4:latest\n    environment:\n      ...\n    ports:\n      - '8080:80'\n    volumes:\n      - ...\n\nvolumes:\n  vol-pgdata:\n    name: vol-pgdata\n  vol-pgadmin_data:\n    name: vol-pgadmin_data\n```\n\nIf the network name is not specified, it is generated automatically: The name of the directory containing the docker-compose.yaml file in lowercase + _default.\n\nYou can find the network’s name when running docker-compose up:\n\n```pg-database Pulling pg-database Pulled \nNetwork week_1_default  Creating\nNetwork week_1_default  Created\n```",
      "Many people aren’t getting an exact match, but are very close to one of the options. It is suggested to choose the closest option.",
      "To enable CI jobs (running dbt on pull requests), dbt Cloud needs a native integration with GitHub, GitLab, or Azure DevOps — the generic \"Git Clone\" / SSH connection method does not unlock the \"Run on Pull Requests\" trigger and you'll see:\n\n```Triggered by pull requests\nThis feature is only available for dbt repositories connected through dbt Cloud's native integration with Github, Gitlab, or Azure DevOps\n```\n\nThe dbt Cloud Developer plan also doesn't support CI jobs — you need Team or Enterprise. (See the dbt Cloud CI prerequisites.)\n\nSwitch from Git Clone to native GitHub integration\n\n1. Connect your GitHub account in dbt Cloud: Profile Settings → Linked Accounts → connect GitHub and grant the requested permissions. (dbt docs)\n\n2. Disconnect the existing Git Clone connection: Account Settings → Projects → your project → Repository → Disconnect.\n\n3. Reconfigure with the GitHub option, selecting the repository.\n\n4. (If your dbt project lives in a subfolder of the repo) set Project Subdirectory in the project settings to point to that folder.\n\n5. In Deploy → Job → Configuration → Triggers, you should now see \"Run on Pull Requests\" as an available toggle.\n\nCI job runs but says \"valid dbt project was not found\"\n\nUsually one of:\n\n- Project is in a subfolder, but Project Subdirectory isn't set.\n- The CI environment is configured to run on a custom branch that doesn't exist.\n- Uncommitted/unpushed changes.\n\nIn Deploy → Environments → your environment → Settings, set \"Custom branch\" if you're not on the default branch, and confirm it matches a real branch in GitHub.",
      "If you are using MS VS Code and running gcloud in WSL2, when you first try to login to GCP via the gcloud CLI with gcloud auth application-default login, you may encounter an issue where the terminal prints a long OAuth URL and then shows a series of \"not found\" errors for browsers:\n\n```Your browser has been opened to visit:\n    https://accounts.google.com/o/oauth2/auth?response_type=code&client_id=...\n\n/usr/bin/xdg-open: 882: x-www-browser: not found\n/usr/bin/xdg-open: 882: firefox: not found\n/usr/bin/xdg-open: 882: chromium: not found\n...\nxdg-open: no method available for opening '...'\n```\n\nVS Code may show a notification: \"Your application running on port 8085 is available\" with an \"Open in Browser\" button. Clicking it may lead to an error page.\n\nSolution:\n\n1. Hover over the long OAuth URL in the terminal output.\n2. Ctrl + Click the link — VS Code will show a dialog: \"Do you want Code to open the external website?\" with buttons Open, Copy, Configure Trusted Domains, and Cancel.\n3. Click Configure Trusted Domains.\n4. A dropdown will appear with options like:\n   - \"Trust https://accounts.google.com\"\n   - \"Trust google.com and all its subdomains\"\n   - \"Trust all domains (disables link protection)\"\n   - \"Manage Trusted Domains\"\n5. Pick the first or second entry (e.g., \"Trust https://accounts.google.com\").\n\nNext time you run gcloud auth, the login page should pop up via the default browser without issues."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "Why does Kestra use IANA timezone identifiers instead of offsets?",
    "positive": "Kestra supports conditional branching within a single workflow to reuse shared orchestration logic while allowing dataset-specific behavior. This approach reduces duplication, keeps related steps together, and makes execution paths explicit within a single workflow definition.\n\nWhen to use a single-workflow branch:\n- Reuse common steps (e.g., data validation, enrichment) across datasets.\n- Implement dataset-specific paths (e.g., Yellow vs Green taxi) within the same flow.\n\nHow to implement:\n- Use a branching mechanism in Kestra (e.g., a condition or switch node) to evaluate a runtime variable (such as the dataset type) and route to the appropriate sub-path.\n- Place shared steps before the branch; place dataset-specific steps inside each branch.\n- Decide how branches converge: continue with a common tail after the branch or treat branches as separate end states.\n\nBest practices:\n- Keep branches modular and minimal; extract shared logic into subflows where possible.\n- Centralize error handling and logging to ensure consistency across branches.\n- Test each branch with representative inputs and monitor branch-specific failures separately.\n\nExample (illustrative pseudo YAML):\n```- id: route_by_dataset\n  type: branch\n  when:\n    - condition: ${workflow.variables.dataset_type == 'yellow'}\n      then:\n        - - id: yellow_tasks\n            tasks: [ yellow_validate, yellow_transform ]\n    - else:\n        - - id: green_tasks\n            tasks: [ green_validate, green_transform ]\n- id: end\n  type: terminate\n```\n\nNote: Adapt the exact branch/task names to your Kestra workflow definitions. The key idea is to place shared logic before the branch, branch on dataset/type, and then execute dataset-specific steps within each branch.",
    "hard_negatives": [
      "The issue comes down to how Unix processes produce output and how Kestra interprets it:\n\n1. Python's logging module writes to stderr by default. If you, for instance, call logging.basicConfig() without specifying a stream argument, the root handler sends basically everything to stderr.\n\n2. Kestra maps the two standard streams to its own log levels. Anything the container writes to stdout becomes a Kestra DEBUG entry and anything written on stderr becomes ERROR. There is no middle ground.\n\nThe fix\n\nGood news is that the fix is simple: redirect Python logging to stdout:\n\n```python\nimport sys\nimport logging\n\nlogging.basicConfig(\n    level=logging.INFO,\n    format=\"%(asctime)s %(levelname)s %(message)s\",\n    stream=sys.stdout,\n)\n```\n\nThat single stream=sys.stdout argument is enough. After the change, your informational messages will show up as DEBUG in Kestra.",
      "The Faust library is no longer maintained — see https://github.com/robinhood/faust. We don't recommend using it for the course.\n\nIf you don't know Java, follow the Python streaming materials in the course repo (under 06-streaming/python/) instead, which use Kafka or Redpanda directly. Watching the Java videos to understand the streaming concepts is still useful even if you skip the coding parts.",
      "This issue arises from the way deduplication is handled in two staging files.\n\nSolution:\n\n- Add an ORDER BY clause in the PARTITION BY section of both staging files.\n- Continue adding columns to the ORDER BY clause until the row count in the fact_trips table is consistent upon re-running the model.\n\nExplanation:\n\nWe partition by vendor_id and pickup_datetime, selecting the first row (rn=1) from these partitions. These partitions lack an order, so every execution might yield a different first row. The inconsistency leads to different rows being processed, possibly with or without an unknown borough. Consequently, the fact_trips model discards a varying number of rows based on the presence of unknown boroughs.",
      "See a list of datasets here: GitHub Datasets",
      "When attempting to start the Docker Postgres container, you may encounter the error message:\n\n```Error - postgres port is already in use.\n```\n\nOption 1: Identify and Stop the Service\n\n1. Determine which service is using the port by running:\n   \n   ```bash\nsudo lsof -i :5432\n```\n   \n2. Stop the service that is using the port:\n   \n   ```bash\nsudo service postgresql stop\n```\n\nOption 2: Map to a Different Port\n\nFor a more long-term solution, consider mapping to a different port:\n\n- Map local port 5433 to container port 5432 in your Docker configuration (Dockerfile or docker-compose.yml).\n- If using a VM, ensure that port 5433 is forwarded in the host machine configuration.\n\nThis approach prevents conflicts and allows the Docker Postgres container to run without interruption."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "Ingestion: When attempting to use the provided quick script to load trip data into GCS, you receive error Access Denied from the S3 bucket",
    "positive": "If you follow the video 1.2.2 - Ingesting NY Taxi Data to Postgres and execute the same steps, you will ingest all the data (~1.3 million rows) into the table yellow_taxi_data. However, running the whole script in the Jupyter notebook for a second time from top to bottom will result in missing the first chunk of 100,000 records. This occurs because a call to the iterator appears before the while loop, leading to the second chunk being ingested first.\n\n- Remove the cell df=next(df_iter) located higher up in the notebook than the while loop.\n- Ensure the first w(df_iter) call is within the while loop.\n\n📔 Note: The notebook is used to test the code and is not intended to be run top to bottom. The logic is organized in a later step when inserted into a .py file for the pipeline.",
    "hard_negatives": [
      "When attempting to start the Docker Postgres container, you may encounter the error message:\n\n```Error - postgres port is already in use.\n```\n\nOption 1: Identify and Stop the Service\n\n1. Determine which service is using the port by running:\n   \n   ```bash\nsudo lsof -i :5432\n```\n   \n2. Stop the service that is using the port:\n   \n   ```bash\nsudo service postgresql stop\n```\n\nOption 2: Map to a Different Port\n\nFor a more long-term solution, consider mapping to a different port:\n\n- Map local port 5433 to container port 5432 in your Docker configuration (Dockerfile or docker-compose.yml).\n- If using a VM, ensure that port 5433 is forwarded in the host machine configuration.\n\nThis approach prevents conflicts and allows the Docker Postgres container to run without interruption.",
      "This issue occurs when attempting to connect to a GCP VM using VSCode on a Windows machine. You can resolve it by changing a registry value in the registry editor.\n\nOpen the Run command window:\n- Use the shortcut keys Windows + R, or\n- Right-click \"Start\" and click \"Run\".\n\nOpen the Registry Editor:\n- Type regedit in the Run command window, then press Enter.\n\nChange the registry value:\n- Navigate to HKEY_CURRENT_USER\\Software\\Microsoft\\Command Processor.\n- Change the \"Autorun\" value from \"if exists\" to a blank.\n\nAlternatively, you can delete the saved fingerprint within the known_hosts file:\n\nIn Windows, locate the file at C:\\Users\\\\.ssh\\known_hosts and remove the entry for the server.",
      "When you log into PgAdmin and see an empty database, the following solution can help:\n\nRun:\n   \n```bash\ndocker-compose up\n```\n\nAnd at the same time run:\n\n```bash\ndocker build -t taxi_ingest:v001 .\n\n# NETWORK NAME IS THE SAME AS THAT CREATED BY DOCKER COMPOSE\ndocker run -it \\\n  --network=pg-network \\\n  taxi_ingest:v001 \\\n  --user=postgres \\\n  --password=postgres \\\n  --host=db \\\n  --port=5432 \\\n  --db=ny_taxi \\\n  --table_name=green_tripdata \\\n  --url=${URL}\n```\n\nIt's important to use the same --network as stated in the docker-compose.yaml file.\n\nThe docker-compose.yaml file might not specify a network, as shown below:\n\n```yaml\nservices:\n  db:\n    container_name: postgres\n    image: postgres:17-alpine\n    environment:\n      ...\n    ports:\n      - '5433:5432'\n    volumes:\n      - ...\n  pgadmin:\n    container_name: pgadmin\n    image: dpage/pgadmin4:latest\n    environment:\n      ...\n    ports:\n      - '8080:80'\n    volumes:\n      - ...\n\nvolumes:\n  vol-pgdata:\n    name: vol-pgdata\n  vol-pgadmin_data:\n    name: vol-pgadmin_data\n```\n\nIf the network name is not specified, it is generated automatically: The name of the directory containing the docker-compose.yaml file in lowercase + _default.\n\nYou can find the network’s name when running docker-compose up:\n\n```pg-database Pulling pg-database Pulled \nNetwork week_1_default  Creating\nNetwork week_1_default  Created\n```",
      "If you are using MS VS Code and running gcloud in WSL2, when you first try to login to GCP via the gcloud CLI with gcloud auth application-default login, you may encounter an issue where the terminal prints a long OAuth URL and then shows a series of \"not found\" errors for browsers:\n\n```Your browser has been opened to visit:\n    https://accounts.google.com/o/oauth2/auth?response_type=code&client_id=...\n\n/usr/bin/xdg-open: 882: x-www-browser: not found\n/usr/bin/xdg-open: 882: firefox: not found\n/usr/bin/xdg-open: 882: chromium: not found\n...\nxdg-open: no method available for opening '...'\n```\n\nVS Code may show a notification: \"Your application running on port 8085 is available\" with an \"Open in Browser\" button. Clicking it may lead to an error page.\n\nSolution:\n\n1. Hover over the long OAuth URL in the terminal output.\n2. Ctrl + Click the link — VS Code will show a dialog: \"Do you want Code to open the external website?\" with buttons Open, Copy, Configure Trusted Domains, and Cancel.\n3. Click Configure Trusted Domains.\n4. A dropdown will appear with options like:\n   - \"Trust https://accounts.google.com\"\n   - \"Trust google.com and all its subdomains\"\n   - \"Trust all domains (disables link protection)\"\n   - \"Manage Trusted Domains\"\n5. Pick the first or second entry (e.g., \"Trust https://accounts.google.com\").\n\nNext time you run gcloud auth, the login page should pop up via the default browser without issues.",
      "This issue arises from the way deduplication is handled in two staging files.\n\nSolution:\n\n- Add an ORDER BY clause in the PARTITION BY section of both staging files.\n- Continue adding columns to the ORDER BY clause until the row count in the fact_trips table is consistent upon re-running the model.\n\nExplanation:\n\nWe partition by vendor_id and pickup_datetime, selecting the first row (rn=1) from these partitions. These partitions lack an order, so every execution might yield a different first row. The inconsistency leads to different rows being processed, possibly with or without an unknown borough. Consequently, the fact_trips model discards a varying number of rows based on the presence of unknown boroughs."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "Build: Why do my Fact_trips only contain a few days of data?",
    "positive": "This issue arises from the way deduplication is handled in two staging files.\n\nSolution:\n\n- Add an ORDER BY clause in the PARTITION BY section of both staging files.\n- Continue adding columns to the ORDER BY clause until the row count in the fact_trips table is consistent upon re-running the model.\n\nExplanation:\n\nWe partition by vendor_id and pickup_datetime, selecting the first row (rn=1) from these partitions. These partitions lack an order, so every execution might yield a different first row. The inconsistency leads to different rows being processed, possibly with or without an unknown borough. Consequently, the fact_trips model discards a varying number of rows based on the presence of unknown boroughs.",
    "hard_negatives": [
      "Spark processes data in partitions. When you write a DataFrame to disk, Spark writes each partition as a separate output file. For example:\n\n```python\ntrips.repartition(4).write.parquet(\"output/\")\n```\n\nThis creates four parquet files because the DataFrame now has four partitions. This behavior enables Spark to write data in parallel and can improve performance on large datasets.",
      "When you log into PgAdmin and see an empty database, the following solution can help:\n\nRun:\n   \n```bash\ndocker-compose up\n```\n\nAnd at the same time run:\n\n```bash\ndocker build -t taxi_ingest:v001 .\n\n# NETWORK NAME IS THE SAME AS THAT CREATED BY DOCKER COMPOSE\ndocker run -it \\\n  --network=pg-network \\\n  taxi_ingest:v001 \\\n  --user=postgres \\\n  --password=postgres \\\n  --host=db \\\n  --port=5432 \\\n  --db=ny_taxi \\\n  --table_name=green_tripdata \\\n  --url=${URL}\n```\n\nIt's important to use the same --network as stated in the docker-compose.yaml file.\n\nThe docker-compose.yaml file might not specify a network, as shown below:\n\n```yaml\nservices:\n  db:\n    container_name: postgres\n    image: postgres:17-alpine\n    environment:\n      ...\n    ports:\n      - '5433:5432'\n    volumes:\n      - ...\n  pgadmin:\n    container_name: pgadmin\n    image: dpage/pgadmin4:latest\n    environment:\n      ...\n    ports:\n      - '8080:80'\n    volumes:\n      - ...\n\nvolumes:\n  vol-pgdata:\n    name: vol-pgdata\n  vol-pgadmin_data:\n    name: vol-pgadmin_data\n```\n\nIf the network name is not specified, it is generated automatically: The name of the directory containing the docker-compose.yaml file in lowercase + _default.\n\nYou can find the network’s name when running docker-compose up:\n\n```pg-database Pulling pg-database Pulled \nNetwork week_1_default  Creating\nNetwork week_1_default  Created\n```",
      "If you follow the video 1.2.2 - Ingesting NY Taxi Data to Postgres and execute the same steps, you will ingest all the data (~1.3 million rows) into the table yellow_taxi_data. However, running the whole script in the Jupyter notebook for a second time from top to bottom will result in missing the first chunk of 100,000 records. This occurs because a call to the iterator appears before the while loop, leading to the second chunk being ingested first.\n\n- Remove the cell df=next(df_iter) located higher up in the notebook than the while loop.\n- Ensure the first w(df_iter) call is within the while loop.\n\n📔 Note: The notebook is used to test the code and is not intended to be run top to bottom. The logic is organized in a later step when inserted into a .py file for the pipeline.",
      "See a list of datasets here: GitHub Datasets",
      "When running terraform destroy, the following error can occur:\n\n```Do you really want to destroy all resources?\n\nTerraform will destroy all your managed infrastructure, as shown above.\n\nThere is no undo. Only 'yes' will be accepted to confirm.\n\nEnter a value: yes\n\ngoogle_bigquery_dataset.homework_dataset: Destroying... [id=projects/terraform-demo-449214/datasets/homework_dataset]\n\n╷\n\n│ Error: Error when reading or editing Dataset: googleapi: Error 400: Dataset terraform-demo-449214:homework_dataset is still in use, resourceInUse\n```\n\nThis is because the dataset is still in use by a table. To delete the dataset, set the delete_contents_on_destroy property to true in the main.tf file."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "Terraform - Error 403 : Access denied",
    "positive": "When running terraform destroy, the following error can occur:\n\n```Do you really want to destroy all resources?\n\nTerraform will destroy all your managed infrastructure, as shown above.\n\nThere is no undo. Only 'yes' will be accepted to confirm.\n\nEnter a value: yes\n\ngoogle_bigquery_dataset.homework_dataset: Destroying... [id=projects/terraform-demo-449214/datasets/homework_dataset]\n\n╷\n\n│ Error: Error when reading or editing Dataset: googleapi: Error 400: Dataset terraform-demo-449214:homework_dataset is still in use, resourceInUse\n```\n\nThis is because the dataset is still in use by a table. To delete the dataset, set the delete_contents_on_destroy property to true in the main.tf file.",
    "hard_negatives": [
      "1. Install the astronomer-cosmos package as a dependency. Refer to the installation guide here and see a Terraform example.\n\n2. Create a new folder, dbt/, inside the dags/ folder of your Composer GCP bucket and copy your dbt-core project there. See the example.\n\n3. Ensure your profiles.yml is configured to authenticate with a service account key. Refer to the BigQuery example.\n\n4. Create a new DAG using the DbtTaskGroup class. Use a ProfileConfig specifying a profiles_yml_filepath that points to the location of your JSON key file. See this example.\n\nYour dbt lineage graph should now appear as tasks inside a task group like this:",
      "When attempting to start the Docker Postgres container, you may encounter the error message:\n\n```Error - postgres port is already in use.\n```\n\nOption 1: Identify and Stop the Service\n\n1. Determine which service is using the port by running:\n   \n   ```bash\nsudo lsof -i :5432\n```\n   \n2. Stop the service that is using the port:\n   \n   ```bash\nsudo service postgresql stop\n```\n\nOption 2: Map to a Different Port\n\nFor a more long-term solution, consider mapping to a different port:\n\n- Map local port 5433 to container port 5432 in your Docker configuration (Dockerfile or docker-compose.yml).\n- If using a VM, ensure that port 5433 is forwarded in the host machine configuration.\n\nThis approach prevents conflicts and allows the Docker Postgres container to run without interruption.",
      "If you are using MS VS Code and running gcloud in WSL2, when you first try to login to GCP via the gcloud CLI with gcloud auth application-default login, you may encounter an issue where the terminal prints a long OAuth URL and then shows a series of \"not found\" errors for browsers:\n\n```Your browser has been opened to visit:\n    https://accounts.google.com/o/oauth2/auth?response_type=code&client_id=...\n\n/usr/bin/xdg-open: 882: x-www-browser: not found\n/usr/bin/xdg-open: 882: firefox: not found\n/usr/bin/xdg-open: 882: chromium: not found\n...\nxdg-open: no method available for opening '...'\n```\n\nVS Code may show a notification: \"Your application running on port 8085 is available\" with an \"Open in Browser\" button. Clicking it may lead to an error page.\n\nSolution:\n\n1. Hover over the long OAuth URL in the terminal output.\n2. Ctrl + Click the link — VS Code will show a dialog: \"Do you want Code to open the external website?\" with buttons Open, Copy, Configure Trusted Domains, and Cancel.\n3. Click Configure Trusted Domains.\n4. A dropdown will appear with options like:\n   - \"Trust https://accounts.google.com\"\n   - \"Trust google.com and all its subdomains\"\n   - \"Trust all domains (disables link protection)\"\n   - \"Manage Trusted Domains\"\n5. Pick the first or second entry (e.g., \"Trust https://accounts.google.com\").\n\nNext time you run gcloud auth, the login page should pop up via the default browser without issues.",
      "Error:\n\n```plaintext\nThis project does not have a development environment configured. Please create a development environment and configure your development credentials to use the dbt IDE.\n```\n\nThe error message provides guidance on resolving this issue. Follow the guide in the dbt cloud setup documentation. Additional instructions can be found in the video @1:42.",
      "You might have installed Docker via snap. Run the following command to verify:\n\n```bash\nsudo snap status docker\n```\n\nIf you receive the response:\n\n```error: unknown command \"status\", see 'snap help'.\n```\n\nThen uninstall Docker and install it via the official website.\n\nError message: \"Bind for 0.0.0.0:5432 failed: port is already allocated.\""
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "Kafka homework Q3: There are options that support the scaling concept more than the others.",
    "positive": "The Faust library is no longer maintained — see https://github.com/robinhood/faust. We don't recommend using it for the course.\n\nIf you don't know Java, follow the Python streaming materials in the course repo (under 06-streaming/python/) instead, which use Kafka or Redpanda directly. Watching the Java videos to understand the streaming concepts is still useful even if you skip the coding parts.",
    "hard_negatives": [
      "In my setup, all of the dependencies listed in build.gradle were not installed in -1.0-SNAPSHOT.jar.\n\nSolution:\n\n1. In the build.gradle file, add the following at the end:\n   \n   ```groovy\nshadowJar {\n       archiveBaseName = \"java-kafka-rides\"\n       archiveClassifier = ''\n   }\n```\n\n2. In the command line, run:\n   \n   ```bash\ngradle shadowjar\n```\n\n3. Execute the script from java-kafka-rides-1.0-SNAPSHOT.jar created by the shadowjar.",
      "Many people aren’t getting an exact match, but are very close to one of the options. It is suggested to choose the closest option.",
      "Kestra supports conditional branching within a single workflow to reuse shared orchestration logic while allowing dataset-specific behavior. This approach reduces duplication, keeps related steps together, and makes execution paths explicit within a single workflow definition.\n\nWhen to use a single-workflow branch:\n- Reuse common steps (e.g., data validation, enrichment) across datasets.\n- Implement dataset-specific paths (e.g., Yellow vs Green taxi) within the same flow.\n\nHow to implement:\n- Use a branching mechanism in Kestra (e.g., a condition or switch node) to evaluate a runtime variable (such as the dataset type) and route to the appropriate sub-path.\n- Place shared steps before the branch; place dataset-specific steps inside each branch.\n- Decide how branches converge: continue with a common tail after the branch or treat branches as separate end states.\n\nBest practices:\n- Keep branches modular and minimal; extract shared logic into subflows where possible.\n- Centralize error handling and logging to ensure consistency across branches.\n- Test each branch with representative inputs and monitor branch-specific failures separately.\n\nExample (illustrative pseudo YAML):\n```- id: route_by_dataset\n  type: branch\n  when:\n    - condition: ${workflow.variables.dataset_type == 'yellow'}\n      then:\n        - - id: yellow_tasks\n            tasks: [ yellow_validate, yellow_transform ]\n    - else:\n        - - id: green_tasks\n            tasks: [ green_validate, green_transform ]\n- id: end\n  type: terminate\n```\n\nNote: Adapt the exact branch/task names to your Kestra workflow definitions. The key idea is to place shared logic before the branch, branch on dataset/type, and then execute dataset-specific steps within each branch.",
      "You don't need a confirmation email. You're accepted. You can start learning and submitting homework without registering. Registration was just to gauge interest before the start date.",
      "See a list of datasets here: GitHub Datasets"
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "CURL: curl: (6) Could not resolve host: output.csv",
    "positive": "This issue occurs when attempting to connect to a GCP VM using VSCode on a Windows machine. You can resolve it by changing a registry value in the registry editor.\n\nOpen the Run command window:\n- Use the shortcut keys Windows + R, or\n- Right-click \"Start\" and click \"Run\".\n\nOpen the Registry Editor:\n- Type regedit in the Run command window, then press Enter.\n\nChange the registry value:\n- Navigate to HKEY_CURRENT_USER\\Software\\Microsoft\\Command Processor.\n- Change the \"Autorun\" value from \"if exists\" to a blank.\n\nAlternatively, you can delete the saved fingerprint within the known_hosts file:\n\nIn Windows, locate the file at C:\\Users\\\\.ssh\\known_hosts and remove the entry for the server.",
    "hard_negatives": [
      "Spark processes data in partitions. When you write a DataFrame to disk, Spark writes each partition as a separate output file. For example:\n\n```python\ntrips.repartition(4).write.parquet(\"output/\")\n```\n\nThis creates four parquet files because the DataFrame now has four partitions. This behavior enables Spark to write data in parallel and can improve performance on large datasets.",
      "The issue comes down to how Unix processes produce output and how Kestra interprets it:\n\n1. Python's logging module writes to stderr by default. If you, for instance, call logging.basicConfig() without specifying a stream argument, the root handler sends basically everything to stderr.\n\n2. Kestra maps the two standard streams to its own log levels. Anything the container writes to stdout becomes a Kestra DEBUG entry and anything written on stderr becomes ERROR. There is no middle ground.\n\nThe fix\n\nGood news is that the fix is simple: redirect Python logging to stdout:\n\n```python\nimport sys\nimport logging\n\nlogging.basicConfig(\n    level=logging.INFO,\n    format=\"%(asctime)s %(levelname)s %(message)s\",\n    stream=sys.stdout,\n)\n```\n\nThat single stream=sys.stdout argument is enough. After the change, your informational messages will show up as DEBUG in Kestra.",
      "Use the PostgreSQL COPY FROM feature, which is compatible with CSV files.\n\nSteps:\n\n1. Create the Table\n   \n   First, create your table (example):\n   \n   ```sql\nCREATE TABLE taxis (\n   \n   …\n   \n   );\n```\n\n2. Use COPY Functionality\n\n   Use the COPY command (example):\n\n   ```sql\nCOPY taxis FROM PROGRAM\n   'url'\n   WITH (\n   FORMAT csv,\n   HEADER true,\n   ENCODING utf8\n   );\n```\n\n   - Syntax for COPY:\n\n   ```sql\nCOPY table_name [ ( column_name [, ...] ) ]\n   FROM { 'filename' | PROGRAM 'command' | STDIN }\n   [ [ WITH ] ( option [, ...] ) ]\n   [ WHERE condition ]\n```",
      "When you log into PgAdmin and see an empty database, the following solution can help:\n\nRun:\n   \n```bash\ndocker-compose up\n```\n\nAnd at the same time run:\n\n```bash\ndocker build -t taxi_ingest:v001 .\n\n# NETWORK NAME IS THE SAME AS THAT CREATED BY DOCKER COMPOSE\ndocker run -it \\\n  --network=pg-network \\\n  taxi_ingest:v001 \\\n  --user=postgres \\\n  --password=postgres \\\n  --host=db \\\n  --port=5432 \\\n  --db=ny_taxi \\\n  --table_name=green_tripdata \\\n  --url=${URL}\n```\n\nIt's important to use the same --network as stated in the docker-compose.yaml file.\n\nThe docker-compose.yaml file might not specify a network, as shown below:\n\n```yaml\nservices:\n  db:\n    container_name: postgres\n    image: postgres:17-alpine\n    environment:\n      ...\n    ports:\n      - '5433:5432'\n    volumes:\n      - ...\n  pgadmin:\n    container_name: pgadmin\n    image: dpage/pgadmin4:latest\n    environment:\n      ...\n    ports:\n      - '8080:80'\n    volumes:\n      - ...\n\nvolumes:\n  vol-pgdata:\n    name: vol-pgdata\n  vol-pgadmin_data:\n    name: vol-pgadmin_data\n```\n\nIf the network name is not specified, it is generated automatically: The name of the directory containing the docker-compose.yaml file in lowercase + _default.\n\nYou can find the network’s name when running docker-compose up:\n\n```pg-database Pulling pg-database Pulled \nNetwork week_1_default  Creating\nNetwork week_1_default  Created\n```",
      "See a list of datasets here: GitHub Datasets"
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "How to obtain the DDL of a table in BigQuery?",
    "positive": "Reference:\n\nhttps://cloud.google.com/bigquery/docs/external-data-cloud-storage\n\nSolution:\n\n```python\nfrom google.cloud import bigquery\n\n# Set table_id to the ID of the table to create\ntable_id = f\"{project_id}.{dataset_name}.{table_name}\"\n\n# Construct a BigQuery client object\nclient = bigquery.Client()\n\n# Set the external source format of your table\nexternal_source_format = \"PARQUET\"\n\n# Set the source_uris to point to your data in Google Cloud\nsource_uris = [f'gs://{bucket_name}/{object_key}/*']\n\n# Create ExternalConfig object with external source format\nexternal_config = bigquery.ExternalConfig(external_source_format)\n\n# Set source_uris that point to your data in Google Cloud\nexternal_config.source_uris = source_uris\nexternal_config.autodetect = True\n\ntable = bigquery.Table(table_id)\n\n# Set the external data configuration of the table\ntable.external_data_configuration = external_config\ntable = client.create_table(table)  # Make an API request.\n\nprint(f'Created table with external source: {table_id}')\nprint(f'Format: {table.external_data_configuration.source_format}')\n```",
    "hard_negatives": [
      "1. Install the astronomer-cosmos package as a dependency. Refer to the installation guide here and see a Terraform example.\n\n2. Create a new folder, dbt/, inside the dags/ folder of your Composer GCP bucket and copy your dbt-core project there. See the example.\n\n3. Ensure your profiles.yml is configured to authenticate with a service account key. Refer to the BigQuery example.\n\n4. Create a new DAG using the DbtTaskGroup class. Use a ProfileConfig specifying a profiles_yml_filepath that points to the location of your JSON key file. See this example.\n\nYour dbt lineage graph should now appear as tasks inside a task group like this:",
      "Remove the dataset from BigQuery created by dbt and run again (with test disabled) to ensure the dataset created has all the rows.\n\nDBT: Why am I getting a new dataset after running my CI/CD Job? / What is this new dbt dataset in BigQuery?\n\nWhen you create the CI/CD job, under 'Compare Changes against an environment (Deferral)' make sure that you select 'No; do not defer to another environment'. Otherwise, dbt won’t merge your dev models into production models; it will create a new environment called ‘dbt_cloud_pr_number of pull request’",
      "When running terraform destroy, the following error can occur:\n\n```Do you really want to destroy all resources?\n\nTerraform will destroy all your managed infrastructure, as shown above.\n\nThere is no undo. Only 'yes' will be accepted to confirm.\n\nEnter a value: yes\n\ngoogle_bigquery_dataset.homework_dataset: Destroying... [id=projects/terraform-demo-449214/datasets/homework_dataset]\n\n╷\n\n│ Error: Error when reading or editing Dataset: googleapi: Error 400: Dataset terraform-demo-449214:homework_dataset is still in use, resourceInUse\n```\n\nThis is because the dataset is still in use by a table. To delete the dataset, set the delete_contents_on_destroy property to true in the main.tf file.",
      "Use the PostgreSQL COPY FROM feature, which is compatible with CSV files.\n\nSteps:\n\n1. Create the Table\n   \n   First, create your table (example):\n   \n   ```sql\nCREATE TABLE taxis (\n   \n   …\n   \n   );\n```\n\n2. Use COPY Functionality\n\n   Use the COPY command (example):\n\n   ```sql\nCOPY taxis FROM PROGRAM\n   'url'\n   WITH (\n   FORMAT csv,\n   HEADER true,\n   ENCODING utf8\n   );\n```\n\n   - Syntax for COPY:\n\n   ```sql\nCOPY table_name [ ( column_name [, ...] ) ]\n   FROM { 'filename' | PROGRAM 'command' | STDIN }\n   [ [ WITH ] ( option [, ...] ) ]\n   [ WHERE condition ]\n```",
      "If you follow the video 1.2.2 - Ingesting NY Taxi Data to Postgres and execute the same steps, you will ingest all the data (~1.3 million rows) into the table yellow_taxi_data. However, running the whole script in the Jupyter notebook for a second time from top to bottom will result in missing the first chunk of 100,000 records. This occurs because a call to the iterator appears before the while loop, leading to the second chunk being ingested first.\n\n- Remove the cell df=next(df_iter) located higher up in the notebook than the while loop.\n- Ensure the first w(df_iter) call is within the while loop.\n\n📔 Note: The notebook is used to test the code and is not intended to be run top to bottom. The logic is organized in a later step when inserted into a .py file for the pipeline."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "How to Inspect Messages in a Kafka Topic Using Offsets?",
    "positive": "The Faust library is no longer maintained — see https://github.com/robinhood/faust. We don't recommend using it for the course.\n\nIf you don't know Java, follow the Python streaming materials in the course repo (under 06-streaming/python/) instead, which use Kafka or Redpanda directly. Watching the Java videos to understand the streaming concepts is still useful even if you skip the coding parts.",
    "hard_negatives": [
      "In my setup, all of the dependencies listed in build.gradle were not installed in -1.0-SNAPSHOT.jar.\n\nSolution:\n\n1. In the build.gradle file, add the following at the end:\n   \n   ```groovy\nshadowJar {\n       archiveBaseName = \"java-kafka-rides\"\n       archiveClassifier = ''\n   }\n```\n\n2. In the command line, run:\n   \n   ```bash\ngradle shadowjar\n```\n\n3. Execute the script from java-kafka-rides-1.0-SNAPSHOT.jar created by the shadowjar.",
      "To read from multiple topics in the same Spark session, follow these steps:\n\n1. Initiate a Spark Session:\n   \n   ```python\nspark = (SparkSession\n       .builder\n       .appName(app_name)\n       .master(master=master)\n       .getOrCreate())\n   \n   spark.streams.resetTerminated()\n```\n\n2. Read Streams from Multiple Topics:\n   \n   ```python\nquery1 = spark\n       .readStream\n       ...\n       ...\n       .load()\n   \n   query2 = spark\n       .readStream\n       ...\n       ...\n       .load()\n   \n   query3 = spark\n       .readStream\n       ...\n       ...\n       .load()\n```\n\n3. Start the Queries:\n   \n   ```python\nquery1.start()\n   query2.start()\n   query3.start()\n```\n\n4. Await Termination:\n   \n   ```python\nspark.streams.awaitAnyTermination()  # Waits for any one of the queries to receive a kill signal or error failure. This is asynchronous.\n```\n\n   Note: query3.start().awaitTermination() is a blocking call. It works well when we are reading only from one topic.",
      "When attempting to start the Docker Postgres container, you may encounter the error message:\n\n```Error - postgres port is already in use.\n```\n\nOption 1: Identify and Stop the Service\n\n1. Determine which service is using the port by running:\n   \n   ```bash\nsudo lsof -i :5432\n```\n   \n2. Stop the service that is using the port:\n   \n   ```bash\nsudo service postgresql stop\n```\n\nOption 2: Map to a Different Port\n\nFor a more long-term solution, consider mapping to a different port:\n\n- Map local port 5433 to container port 5432 in your Docker configuration (Dockerfile or docker-compose.yml).\n- If using a VM, ensure that port 5433 is forwarded in the host machine configuration.\n\nThis approach prevents conflicts and allows the Docker Postgres container to run without interruption.",
      "Kestra supports conditional branching within a single workflow to reuse shared orchestration logic while allowing dataset-specific behavior. This approach reduces duplication, keeps related steps together, and makes execution paths explicit within a single workflow definition.\n\nWhen to use a single-workflow branch:\n- Reuse common steps (e.g., data validation, enrichment) across datasets.\n- Implement dataset-specific paths (e.g., Yellow vs Green taxi) within the same flow.\n\nHow to implement:\n- Use a branching mechanism in Kestra (e.g., a condition or switch node) to evaluate a runtime variable (such as the dataset type) and route to the appropriate sub-path.\n- Place shared steps before the branch; place dataset-specific steps inside each branch.\n- Decide how branches converge: continue with a common tail after the branch or treat branches as separate end states.\n\nBest practices:\n- Keep branches modular and minimal; extract shared logic into subflows where possible.\n- Centralize error handling and logging to ensure consistency across branches.\n- Test each branch with representative inputs and monitor branch-specific failures separately.\n\nExample (illustrative pseudo YAML):\n```- id: route_by_dataset\n  type: branch\n  when:\n    - condition: ${workflow.variables.dataset_type == 'yellow'}\n      then:\n        - - id: yellow_tasks\n            tasks: [ yellow_validate, yellow_transform ]\n    - else:\n        - - id: green_tasks\n            tasks: [ green_validate, green_transform ]\n- id: end\n  type: terminate\n```\n\nNote: Adapt the exact branch/task names to your Kestra workflow definitions. The key idea is to place shared logic before the branch, branch on dataset/type, and then execute dataset-specific steps within each branch.",
      "The issue comes down to how Unix processes produce output and how Kestra interprets it:\n\n1. Python's logging module writes to stderr by default. If you, for instance, call logging.basicConfig() without specifying a stream argument, the root handler sends basically everything to stderr.\n\n2. Kestra maps the two standard streams to its own log levels. Anything the container writes to stdout becomes a Kestra DEBUG entry and anything written on stderr becomes ERROR. There is no middle ground.\n\nThe fix\n\nGood news is that the fix is simple: redirect Python logging to stdout:\n\n```python\nimport sys\nimport logging\n\nlogging.basicConfig(\n    level=logging.INFO,\n    format=\"%(asctime)s %(levelname)s %(message)s\",\n    stream=sys.stdout,\n)\n```\n\nThat single stream=sys.stdout argument is enough. After the change, your informational messages will show up as DEBUG in Kestra."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "GCP: OR-CBAT-15 ERROR Google cloud free trial account",
    "positive": "If you are using MS VS Code and running gcloud in WSL2, when you first try to login to GCP via the gcloud CLI with gcloud auth application-default login, you may encounter an issue where the terminal prints a long OAuth URL and then shows a series of \"not found\" errors for browsers:\n\n```Your browser has been opened to visit:\n    https://accounts.google.com/o/oauth2/auth?response_type=code&client_id=...\n\n/usr/bin/xdg-open: 882: x-www-browser: not found\n/usr/bin/xdg-open: 882: firefox: not found\n/usr/bin/xdg-open: 882: chromium: not found\n...\nxdg-open: no method available for opening '...'\n```\n\nVS Code may show a notification: \"Your application running on port 8085 is available\" with an \"Open in Browser\" button. Clicking it may lead to an error page.\n\nSolution:\n\n1. Hover over the long OAuth URL in the terminal output.\n2. Ctrl + Click the link — VS Code will show a dialog: \"Do you want Code to open the external website?\" with buttons Open, Copy, Configure Trusted Domains, and Cancel.\n3. Click Configure Trusted Domains.\n4. A dropdown will appear with options like:\n   - \"Trust https://accounts.google.com\"\n   - \"Trust google.com and all its subdomains\"\n   - \"Trust all domains (disables link protection)\"\n   - \"Manage Trusted Domains\"\n5. Pick the first or second entry (e.g., \"Trust https://accounts.google.com\").\n\nNext time you run gcloud auth, the login page should pop up via the default browser without issues.",
    "hard_negatives": [
      "Reference:\n\nhttps://cloud.google.com/bigquery/docs/external-data-cloud-storage\n\nSolution:\n\n```python\nfrom google.cloud import bigquery\n\n# Set table_id to the ID of the table to create\ntable_id = f\"{project_id}.{dataset_name}.{table_name}\"\n\n# Construct a BigQuery client object\nclient = bigquery.Client()\n\n# Set the external source format of your table\nexternal_source_format = \"PARQUET\"\n\n# Set the source_uris to point to your data in Google Cloud\nsource_uris = [f'gs://{bucket_name}/{object_key}/*']\n\n# Create ExternalConfig object with external source format\nexternal_config = bigquery.ExternalConfig(external_source_format)\n\n# Set source_uris that point to your data in Google Cloud\nexternal_config.source_uris = source_uris\nexternal_config.autodetect = True\n\ntable = bigquery.Table(table_id)\n\n# Set the external data configuration of the table\ntable.external_data_configuration = external_config\ntable = client.create_table(table)  # Make an API request.\n\nprint(f'Created table with external source: {table_id}')\nprint(f'Format: {table.external_data_configuration.source_format}')\n```",
      "This issue occurs when attempting to connect to a GCP VM using VSCode on a Windows machine. You can resolve it by changing a registry value in the registry editor.\n\nOpen the Run command window:\n- Use the shortcut keys Windows + R, or\n- Right-click \"Start\" and click \"Run\".\n\nOpen the Registry Editor:\n- Type regedit in the Run command window, then press Enter.\n\nChange the registry value:\n- Navigate to HKEY_CURRENT_USER\\Software\\Microsoft\\Command Processor.\n- Change the \"Autorun\" value from \"if exists\" to a blank.\n\nAlternatively, you can delete the saved fingerprint within the known_hosts file:\n\nIn Windows, locate the file at C:\\Users\\\\.ssh\\known_hosts and remove the entry for the server.",
      "In this lecture, Alexey deleted his instance in Google Cloud. Do I have to do it?\n\nNo, do not delete your instance in Google Cloud Platform. Otherwise, you will have to set it up again for the week 1 readings.",
      "Error:\n\n```plaintext\nThis project does not have a development environment configured. Please create a development environment and configure your development credentials to use the dbt IDE.\n```\n\nThe error message provides guidance on resolving this issue. Follow the guide in the dbt cloud setup documentation. Additional instructions can be found in the video @1:42.",
      "When running terraform destroy, the following error can occur:\n\n```Do you really want to destroy all resources?\n\nTerraform will destroy all your managed infrastructure, as shown above.\n\nThere is no undo. Only 'yes' will be accepted to confirm.\n\nEnter a value: yes\n\ngoogle_bigquery_dataset.homework_dataset: Destroying... [id=projects/terraform-demo-449214/datasets/homework_dataset]\n\n╷\n\n│ Error: Error when reading or editing Dataset: googleapi: Error 400: Dataset terraform-demo-449214:homework_dataset is still in use, resourceInUse\n```\n\nThis is because the dataset is still in use by a table. To delete the dataset, set the delete_contents_on_destroy property to true in the main.tf file."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "Office Hours: I can’t attend the “Office hours” / workshop, will it be recorded?",
    "positive": "See a list of datasets here: GitHub Datasets",
    "hard_negatives": [
      "You don't need a confirmation email. You're accepted. You can start learning and submitting homework without registering. Registration was just to gauge interest before the start date.",
      "Many people aren’t getting an exact match, but are very close to one of the options. It is suggested to choose the closest option.",
      "Make sure you don’t have a dlt.py file saved in the same directory as your working file.",
      "In this lecture, Alexey deleted his instance in Google Cloud. Do I have to do it?\n\nNo, do not delete your instance in Google Cloud Platform. Otherwise, you will have to set it up again for the week 1 readings.",
      "To read from multiple topics in the same Spark session, follow these steps:\n\n1. Initiate a Spark Session:\n   \n   ```python\nspark = (SparkSession\n       .builder\n       .appName(app_name)\n       .master(master=master)\n       .getOrCreate())\n   \n   spark.streams.resetTerminated()\n```\n\n2. Read Streams from Multiple Topics:\n   \n   ```python\nquery1 = spark\n       .readStream\n       ...\n       ...\n       .load()\n   \n   query2 = spark\n       .readStream\n       ...\n       ...\n       .load()\n   \n   query3 = spark\n       .readStream\n       ...\n       ...\n       .load()\n```\n\n3. Start the Queries:\n   \n   ```python\nquery1.start()\n   query2.start()\n   query3.start()\n```\n\n4. Await Termination:\n   \n   ```python\nspark.streams.awaitAnyTermination()  # Waits for any one of the queries to receive a kill signal or error failure. This is asynchronous.\n```\n\n   Note: query3.start().awaitTermination() is a blocking call. It works well when we are reading only from one topic."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "GCP VM: VM connection request timeout",
    "positive": "This issue occurs when attempting to connect to a GCP VM using VSCode on a Windows machine. You can resolve it by changing a registry value in the registry editor.\n\nOpen the Run command window:\n- Use the shortcut keys Windows + R, or\n- Right-click \"Start\" and click \"Run\".\n\nOpen the Registry Editor:\n- Type regedit in the Run command window, then press Enter.\n\nChange the registry value:\n- Navigate to HKEY_CURRENT_USER\\Software\\Microsoft\\Command Processor.\n- Change the \"Autorun\" value from \"if exists\" to a blank.\n\nAlternatively, you can delete the saved fingerprint within the known_hosts file:\n\nIn Windows, locate the file at C:\\Users\\\\.ssh\\known_hosts and remove the entry for the server.",
    "hard_negatives": [
      "If you are using MS VS Code and running gcloud in WSL2, when you first try to login to GCP via the gcloud CLI with gcloud auth application-default login, you may encounter an issue where the terminal prints a long OAuth URL and then shows a series of \"not found\" errors for browsers:\n\n```Your browser has been opened to visit:\n    https://accounts.google.com/o/oauth2/auth?response_type=code&client_id=...\n\n/usr/bin/xdg-open: 882: x-www-browser: not found\n/usr/bin/xdg-open: 882: firefox: not found\n/usr/bin/xdg-open: 882: chromium: not found\n...\nxdg-open: no method available for opening '...'\n```\n\nVS Code may show a notification: \"Your application running on port 8085 is available\" with an \"Open in Browser\" button. Clicking it may lead to an error page.\n\nSolution:\n\n1. Hover over the long OAuth URL in the terminal output.\n2. Ctrl + Click the link — VS Code will show a dialog: \"Do you want Code to open the external website?\" with buttons Open, Copy, Configure Trusted Domains, and Cancel.\n3. Click Configure Trusted Domains.\n4. A dropdown will appear with options like:\n   - \"Trust https://accounts.google.com\"\n   - \"Trust google.com and all its subdomains\"\n   - \"Trust all domains (disables link protection)\"\n   - \"Manage Trusted Domains\"\n5. Pick the first or second entry (e.g., \"Trust https://accounts.google.com\").\n\nNext time you run gcloud auth, the login page should pop up via the default browser without issues.",
      "To enable CI jobs (running dbt on pull requests), dbt Cloud needs a native integration with GitHub, GitLab, or Azure DevOps — the generic \"Git Clone\" / SSH connection method does not unlock the \"Run on Pull Requests\" trigger and you'll see:\n\n```Triggered by pull requests\nThis feature is only available for dbt repositories connected through dbt Cloud's native integration with Github, Gitlab, or Azure DevOps\n```\n\nThe dbt Cloud Developer plan also doesn't support CI jobs — you need Team or Enterprise. (See the dbt Cloud CI prerequisites.)\n\nSwitch from Git Clone to native GitHub integration\n\n1. Connect your GitHub account in dbt Cloud: Profile Settings → Linked Accounts → connect GitHub and grant the requested permissions. (dbt docs)\n\n2. Disconnect the existing Git Clone connection: Account Settings → Projects → your project → Repository → Disconnect.\n\n3. Reconfigure with the GitHub option, selecting the repository.\n\n4. (If your dbt project lives in a subfolder of the repo) set Project Subdirectory in the project settings to point to that folder.\n\n5. In Deploy → Job → Configuration → Triggers, you should now see \"Run on Pull Requests\" as an available toggle.\n\nCI job runs but says \"valid dbt project was not found\"\n\nUsually one of:\n\n- Project is in a subfolder, but Project Subdirectory isn't set.\n- The CI environment is configured to run on a custom branch that doesn't exist.\n- Uncommitted/unpushed changes.\n\nIn Deploy → Environments → your environment → Settings, set \"Custom branch\" if you're not on the default branch, and confirm it matches a real branch in GitHub.",
      "When attempting to start the Docker Postgres container, you may encounter the error message:\n\n```Error - postgres port is already in use.\n```\n\nOption 1: Identify and Stop the Service\n\n1. Determine which service is using the port by running:\n   \n   ```bash\nsudo lsof -i :5432\n```\n   \n2. Stop the service that is using the port:\n   \n   ```bash\nsudo service postgresql stop\n```\n\nOption 2: Map to a Different Port\n\nFor a more long-term solution, consider mapping to a different port:\n\n- Map local port 5433 to container port 5432 in your Docker configuration (Dockerfile or docker-compose.yml).\n- If using a VM, ensure that port 5433 is forwarded in the host machine configuration.\n\nThis approach prevents conflicts and allows the Docker Postgres container to run without interruption.",
      "When you log into PgAdmin and see an empty database, the following solution can help:\n\nRun:\n   \n```bash\ndocker-compose up\n```\n\nAnd at the same time run:\n\n```bash\ndocker build -t taxi_ingest:v001 .\n\n# NETWORK NAME IS THE SAME AS THAT CREATED BY DOCKER COMPOSE\ndocker run -it \\\n  --network=pg-network \\\n  taxi_ingest:v001 \\\n  --user=postgres \\\n  --password=postgres \\\n  --host=db \\\n  --port=5432 \\\n  --db=ny_taxi \\\n  --table_name=green_tripdata \\\n  --url=${URL}\n```\n\nIt's important to use the same --network as stated in the docker-compose.yaml file.\n\nThe docker-compose.yaml file might not specify a network, as shown below:\n\n```yaml\nservices:\n  db:\n    container_name: postgres\n    image: postgres:17-alpine\n    environment:\n      ...\n    ports:\n      - '5433:5432'\n    volumes:\n      - ...\n  pgadmin:\n    container_name: pgadmin\n    image: dpage/pgadmin4:latest\n    environment:\n      ...\n    ports:\n      - '8080:80'\n    volumes:\n      - ...\n\nvolumes:\n  vol-pgdata:\n    name: vol-pgdata\n  vol-pgadmin_data:\n    name: vol-pgadmin_data\n```\n\nIf the network name is not specified, it is generated automatically: The name of the directory containing the docker-compose.yaml file in lowercase + _default.\n\nYou can find the network’s name when running docker-compose up:\n\n```pg-database Pulling pg-database Pulled \nNetwork week_1_default  Creating\nNetwork week_1_default  Created\n```",
      "1. Install the astronomer-cosmos package as a dependency. Refer to the installation guide here and see a Terraform example.\n\n2. Create a new folder, dbt/, inside the dags/ folder of your Composer GCP bucket and copy your dbt-core project there. See the example.\n\n3. Ensure your profiles.yml is configured to authenticate with a service account key. Refer to the BigQuery example.\n\n4. Create a new DAG using the DbtTaskGroup class. Use a ProfileConfig specifying a profiles_yml_filepath that points to the location of your JSON key file. See this example.\n\nYour dbt lineage graph should now appear as tasks inside a task group like this:"
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "Why do downstream Bruin assets fail with 'Table does not exist' error even though the upstream ingestion asset succeeded?",
    "positive": "In join queries, if you mention the column name directly or enclose it in single quotes, you'll encounter an error saying \"column does not exist\".\n\nSolution: Enclose the column names in double quotes, and it will work correctly.",
    "hard_negatives": [
      "When running terraform destroy, the following error can occur:\n\n```Do you really want to destroy all resources?\n\nTerraform will destroy all your managed infrastructure, as shown above.\n\nThere is no undo. Only 'yes' will be accepted to confirm.\n\nEnter a value: yes\n\ngoogle_bigquery_dataset.homework_dataset: Destroying... [id=projects/terraform-demo-449214/datasets/homework_dataset]\n\n╷\n\n│ Error: Error when reading or editing Dataset: googleapi: Error 400: Dataset terraform-demo-449214:homework_dataset is still in use, resourceInUse\n```\n\nThis is because the dataset is still in use by a table. To delete the dataset, set the delete_contents_on_destroy property to true in the main.tf file.",
      "If you follow the video 1.2.2 - Ingesting NY Taxi Data to Postgres and execute the same steps, you will ingest all the data (~1.3 million rows) into the table yellow_taxi_data. However, running the whole script in the Jupyter notebook for a second time from top to bottom will result in missing the first chunk of 100,000 records. This occurs because a call to the iterator appears before the while loop, leading to the second chunk being ingested first.\n\n- Remove the cell df=next(df_iter) located higher up in the notebook than the while loop.\n- Ensure the first w(df_iter) call is within the while loop.\n\n📔 Note: The notebook is used to test the code and is not intended to be run top to bottom. The logic is organized in a later step when inserted into a .py file for the pipeline.",
      "See a list of datasets here: GitHub Datasets",
      "Error:\n\n```plaintext\nThis project does not have a development environment configured. Please create a development environment and configure your development credentials to use the dbt IDE.\n```\n\nThe error message provides guidance on resolving this issue. Follow the guide in the dbt cloud setup documentation. Additional instructions can be found in the video @1:42.",
      "You might have installed Docker via snap. Run the following command to verify:\n\n```bash\nsudo snap status docker\n```\n\nIf you receive the response:\n\n```error: unknown command \"status\", see 'snap help'.\n```\n\nThen uninstall Docker and install it via the official website.\n\nError message: \"Bind for 0.0.0.0:5432 failed: port is already allocated.\""
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "Data Transformation from Databricks to Azure SQL DB",
    "positive": "To enable CI jobs (running dbt on pull requests), dbt Cloud needs a native integration with GitHub, GitLab, or Azure DevOps — the generic \"Git Clone\" / SSH connection method does not unlock the \"Run on Pull Requests\" trigger and you'll see:\n\n```Triggered by pull requests\nThis feature is only available for dbt repositories connected through dbt Cloud's native integration with Github, Gitlab, or Azure DevOps\n```\n\nThe dbt Cloud Developer plan also doesn't support CI jobs — you need Team or Enterprise. (See the dbt Cloud CI prerequisites.)\n\nSwitch from Git Clone to native GitHub integration\n\n1. Connect your GitHub account in dbt Cloud: Profile Settings → Linked Accounts → connect GitHub and grant the requested permissions. (dbt docs)\n\n2. Disconnect the existing Git Clone connection: Account Settings → Projects → your project → Repository → Disconnect.\n\n3. Reconfigure with the GitHub option, selecting the repository.\n\n4. (If your dbt project lives in a subfolder of the repo) set Project Subdirectory in the project settings to point to that folder.\n\n5. In Deploy → Job → Configuration → Triggers, you should now see \"Run on Pull Requests\" as an available toggle.\n\nCI job runs but says \"valid dbt project was not found\"\n\nUsually one of:\n\n- Project is in a subfolder, but Project Subdirectory isn't set.\n- The CI environment is configured to run on a custom branch that doesn't exist.\n- Uncommitted/unpushed changes.\n\nIn Deploy → Environments → your environment → Settings, set \"Custom branch\" if you're not on the default branch, and confirm it matches a real branch in GitHub.",
    "hard_negatives": [
      "Kestra supports conditional branching within a single workflow to reuse shared orchestration logic while allowing dataset-specific behavior. This approach reduces duplication, keeps related steps together, and makes execution paths explicit within a single workflow definition.\n\nWhen to use a single-workflow branch:\n- Reuse common steps (e.g., data validation, enrichment) across datasets.\n- Implement dataset-specific paths (e.g., Yellow vs Green taxi) within the same flow.\n\nHow to implement:\n- Use a branching mechanism in Kestra (e.g., a condition or switch node) to evaluate a runtime variable (such as the dataset type) and route to the appropriate sub-path.\n- Place shared steps before the branch; place dataset-specific steps inside each branch.\n- Decide how branches converge: continue with a common tail after the branch or treat branches as separate end states.\n\nBest practices:\n- Keep branches modular and minimal; extract shared logic into subflows where possible.\n- Centralize error handling and logging to ensure consistency across branches.\n- Test each branch with representative inputs and monitor branch-specific failures separately.\n\nExample (illustrative pseudo YAML):\n```- id: route_by_dataset\n  type: branch\n  when:\n    - condition: ${workflow.variables.dataset_type == 'yellow'}\n      then:\n        - - id: yellow_tasks\n            tasks: [ yellow_validate, yellow_transform ]\n    - else:\n        - - id: green_tasks\n            tasks: [ green_validate, green_transform ]\n- id: end\n  type: terminate\n```\n\nNote: Adapt the exact branch/task names to your Kestra workflow definitions. The key idea is to place shared logic before the branch, branch on dataset/type, and then execute dataset-specific steps within each branch.",
      "Remove the dataset from BigQuery created by dbt and run again (with test disabled) to ensure the dataset created has all the rows.\n\nDBT: Why am I getting a new dataset after running my CI/CD Job? / What is this new dbt dataset in BigQuery?\n\nWhen you create the CI/CD job, under 'Compare Changes against an environment (Deferral)' make sure that you select 'No; do not defer to another environment'. Otherwise, dbt won’t merge your dev models into production models; it will create a new environment called ‘dbt_cloud_pr_number of pull request’",
      "When you log into PgAdmin and see an empty database, the following solution can help:\n\nRun:\n   \n```bash\ndocker-compose up\n```\n\nAnd at the same time run:\n\n```bash\ndocker build -t taxi_ingest:v001 .\n\n# NETWORK NAME IS THE SAME AS THAT CREATED BY DOCKER COMPOSE\ndocker run -it \\\n  --network=pg-network \\\n  taxi_ingest:v001 \\\n  --user=postgres \\\n  --password=postgres \\\n  --host=db \\\n  --port=5432 \\\n  --db=ny_taxi \\\n  --table_name=green_tripdata \\\n  --url=${URL}\n```\n\nIt's important to use the same --network as stated in the docker-compose.yaml file.\n\nThe docker-compose.yaml file might not specify a network, as shown below:\n\n```yaml\nservices:\n  db:\n    container_name: postgres\n    image: postgres:17-alpine\n    environment:\n      ...\n    ports:\n      - '5433:5432'\n    volumes:\n      - ...\n  pgadmin:\n    container_name: pgadmin\n    image: dpage/pgadmin4:latest\n    environment:\n      ...\n    ports:\n      - '8080:80'\n    volumes:\n      - ...\n\nvolumes:\n  vol-pgdata:\n    name: vol-pgdata\n  vol-pgadmin_data:\n    name: vol-pgadmin_data\n```\n\nIf the network name is not specified, it is generated automatically: The name of the directory containing the docker-compose.yaml file in lowercase + _default.\n\nYou can find the network’s name when running docker-compose up:\n\n```pg-database Pulling pg-database Pulled \nNetwork week_1_default  Creating\nNetwork week_1_default  Created\n```",
      "Reference:\n\nhttps://cloud.google.com/bigquery/docs/external-data-cloud-storage\n\nSolution:\n\n```python\nfrom google.cloud import bigquery\n\n# Set table_id to the ID of the table to create\ntable_id = f\"{project_id}.{dataset_name}.{table_name}\"\n\n# Construct a BigQuery client object\nclient = bigquery.Client()\n\n# Set the external source format of your table\nexternal_source_format = \"PARQUET\"\n\n# Set the source_uris to point to your data in Google Cloud\nsource_uris = [f'gs://{bucket_name}/{object_key}/*']\n\n# Create ExternalConfig object with external source format\nexternal_config = bigquery.ExternalConfig(external_source_format)\n\n# Set source_uris that point to your data in Google Cloud\nexternal_config.source_uris = source_uris\nexternal_config.autodetect = True\n\ntable = bigquery.Table(table_id)\n\n# Set the external data configuration of the table\ntable.external_data_configuration = external_config\ntable = client.create_table(table)  # Make an API request.\n\nprint(f'Created table with external source: {table_id}')\nprint(f'Format: {table.external_data_configuration.source_format}')\n```",
      "Dbt provides a mechanism called ref to manage dependencies between models. By referencing other models using the ref keyword in SQL, dbt automatically understands the dependencies and ensures the correct execution order."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "BigQuery returns an error when I try to run the dm_monthly_zone_revenue.sql model.",
    "positive": "When running terraform destroy, the following error can occur:\n\n```Do you really want to destroy all resources?\n\nTerraform will destroy all your managed infrastructure, as shown above.\n\nThere is no undo. Only 'yes' will be accepted to confirm.\n\nEnter a value: yes\n\ngoogle_bigquery_dataset.homework_dataset: Destroying... [id=projects/terraform-demo-449214/datasets/homework_dataset]\n\n╷\n\n│ Error: Error when reading or editing Dataset: googleapi: Error 400: Dataset terraform-demo-449214:homework_dataset is still in use, resourceInUse\n```\n\nThis is because the dataset is still in use by a table. To delete the dataset, set the delete_contents_on_destroy property to true in the main.tf file.",
    "hard_negatives": [
      "Remove the dataset from BigQuery created by dbt and run again (with test disabled) to ensure the dataset created has all the rows.\n\nDBT: Why am I getting a new dataset after running my CI/CD Job? / What is this new dbt dataset in BigQuery?\n\nWhen you create the CI/CD job, under 'Compare Changes against an environment (Deferral)' make sure that you select 'No; do not defer to another environment'. Otherwise, dbt won’t merge your dev models into production models; it will create a new environment called ‘dbt_cloud_pr_number of pull request’",
      "Reference:\n\nhttps://cloud.google.com/bigquery/docs/external-data-cloud-storage\n\nSolution:\n\n```python\nfrom google.cloud import bigquery\n\n# Set table_id to the ID of the table to create\ntable_id = f\"{project_id}.{dataset_name}.{table_name}\"\n\n# Construct a BigQuery client object\nclient = bigquery.Client()\n\n# Set the external source format of your table\nexternal_source_format = \"PARQUET\"\n\n# Set the source_uris to point to your data in Google Cloud\nsource_uris = [f'gs://{bucket_name}/{object_key}/*']\n\n# Create ExternalConfig object with external source format\nexternal_config = bigquery.ExternalConfig(external_source_format)\n\n# Set source_uris that point to your data in Google Cloud\nexternal_config.source_uris = source_uris\nexternal_config.autodetect = True\n\ntable = bigquery.Table(table_id)\n\n# Set the external data configuration of the table\ntable.external_data_configuration = external_config\ntable = client.create_table(table)  # Make an API request.\n\nprint(f'Created table with external source: {table_id}')\nprint(f'Format: {table.external_data_configuration.source_format}')\n```",
      "When attempting to start the Docker Postgres container, you may encounter the error message:\n\n```Error - postgres port is already in use.\n```\n\nOption 1: Identify and Stop the Service\n\n1. Determine which service is using the port by running:\n   \n   ```bash\nsudo lsof -i :5432\n```\n   \n2. Stop the service that is using the port:\n   \n   ```bash\nsudo service postgresql stop\n```\n\nOption 2: Map to a Different Port\n\nFor a more long-term solution, consider mapping to a different port:\n\n- Map local port 5433 to container port 5432 in your Docker configuration (Dockerfile or docker-compose.yml).\n- If using a VM, ensure that port 5433 is forwarded in the host machine configuration.\n\nThis approach prevents conflicts and allows the Docker Postgres container to run without interruption.",
      "If you are using MS VS Code and running gcloud in WSL2, when you first try to login to GCP via the gcloud CLI with gcloud auth application-default login, you may encounter an issue where the terminal prints a long OAuth URL and then shows a series of \"not found\" errors for browsers:\n\n```Your browser has been opened to visit:\n    https://accounts.google.com/o/oauth2/auth?response_type=code&client_id=...\n\n/usr/bin/xdg-open: 882: x-www-browser: not found\n/usr/bin/xdg-open: 882: firefox: not found\n/usr/bin/xdg-open: 882: chromium: not found\n...\nxdg-open: no method available for opening '...'\n```\n\nVS Code may show a notification: \"Your application running on port 8085 is available\" with an \"Open in Browser\" button. Clicking it may lead to an error page.\n\nSolution:\n\n1. Hover over the long OAuth URL in the terminal output.\n2. Ctrl + Click the link — VS Code will show a dialog: \"Do you want Code to open the external website?\" with buttons Open, Copy, Configure Trusted Domains, and Cancel.\n3. Click Configure Trusted Domains.\n4. A dropdown will appear with options like:\n   - \"Trust https://accounts.google.com\"\n   - \"Trust google.com and all its subdomains\"\n   - \"Trust all domains (disables link protection)\"\n   - \"Manage Trusted Domains\"\n5. Pick the first or second entry (e.g., \"Trust https://accounts.google.com\").\n\nNext time you run gcloud auth, the login page should pop up via the default browser without issues.",
      "1. Install the astronomer-cosmos package as a dependency. Refer to the installation guide here and see a Terraform example.\n\n2. Create a new folder, dbt/, inside the dags/ folder of your Composer GCP bucket and copy your dbt-core project there. See the example.\n\n3. Ensure your profiles.yml is configured to authenticate with a service account key. Refer to the BigQuery example.\n\n4. Create a new DAG using the DbtTaskGroup class. Use a ProfileConfig specifying a profiles_yml_filepath that points to the location of your JSON key file. See this example.\n\nYour dbt lineage graph should now appear as tasks inside a task group like this:"
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "What is the use of Redpanda?",
    "positive": "The Faust library is no longer maintained — see https://github.com/robinhood/faust. We don't recommend using it for the course.\n\nIf you don't know Java, follow the Python streaming materials in the course repo (under 06-streaming/python/) instead, which use Kafka or Redpanda directly. Watching the Java videos to understand the streaming concepts is still useful even if you skip the coding parts.",
    "hard_negatives": [
      "Kestra supports conditional branching within a single workflow to reuse shared orchestration logic while allowing dataset-specific behavior. This approach reduces duplication, keeps related steps together, and makes execution paths explicit within a single workflow definition.\n\nWhen to use a single-workflow branch:\n- Reuse common steps (e.g., data validation, enrichment) across datasets.\n- Implement dataset-specific paths (e.g., Yellow vs Green taxi) within the same flow.\n\nHow to implement:\n- Use a branching mechanism in Kestra (e.g., a condition or switch node) to evaluate a runtime variable (such as the dataset type) and route to the appropriate sub-path.\n- Place shared steps before the branch; place dataset-specific steps inside each branch.\n- Decide how branches converge: continue with a common tail after the branch or treat branches as separate end states.\n\nBest practices:\n- Keep branches modular and minimal; extract shared logic into subflows where possible.\n- Centralize error handling and logging to ensure consistency across branches.\n- Test each branch with representative inputs and monitor branch-specific failures separately.\n\nExample (illustrative pseudo YAML):\n```- id: route_by_dataset\n  type: branch\n  when:\n    - condition: ${workflow.variables.dataset_type == 'yellow'}\n      then:\n        - - id: yellow_tasks\n            tasks: [ yellow_validate, yellow_transform ]\n    - else:\n        - - id: green_tasks\n            tasks: [ green_validate, green_transform ]\n- id: end\n  type: terminate\n```\n\nNote: Adapt the exact branch/task names to your Kestra workflow definitions. The key idea is to place shared logic before the branch, branch on dataset/type, and then execute dataset-specific steps within each branch.",
      "Dbt provides a mechanism called ref to manage dependencies between models. By referencing other models using the ref keyword in SQL, dbt automatically understands the dependencies and ensures the correct execution order.",
      "The issue comes down to how Unix processes produce output and how Kestra interprets it:\n\n1. Python's logging module writes to stderr by default. If you, for instance, call logging.basicConfig() without specifying a stream argument, the root handler sends basically everything to stderr.\n\n2. Kestra maps the two standard streams to its own log levels. Anything the container writes to stdout becomes a Kestra DEBUG entry and anything written on stderr becomes ERROR. There is no middle ground.\n\nThe fix\n\nGood news is that the fix is simple: redirect Python logging to stdout:\n\n```python\nimport sys\nimport logging\n\nlogging.basicConfig(\n    level=logging.INFO,\n    format=\"%(asctime)s %(levelname)s %(message)s\",\n    stream=sys.stdout,\n)\n```\n\nThat single stream=sys.stdout argument is enough. After the change, your informational messages will show up as DEBUG in Kestra.",
      "Use the PostgreSQL COPY FROM feature, which is compatible with CSV files.\n\nSteps:\n\n1. Create the Table\n   \n   First, create your table (example):\n   \n   ```sql\nCREATE TABLE taxis (\n   \n   …\n   \n   );\n```\n\n2. Use COPY Functionality\n\n   Use the COPY command (example):\n\n   ```sql\nCOPY taxis FROM PROGRAM\n   'url'\n   WITH (\n   FORMAT csv,\n   HEADER true,\n   ENCODING utf8\n   );\n```\n\n   - Syntax for COPY:\n\n   ```sql\nCOPY table_name [ ( column_name [, ...] ) ]\n   FROM { 'filename' | PROGRAM 'command' | STDIN }\n   [ [ WITH ] ( option [, ...] ) ]\n   [ WHERE condition ]\n```",
      "When attempting to start the Docker Postgres container, you may encounter the error message:\n\n```Error - postgres port is already in use.\n```\n\nOption 1: Identify and Stop the Service\n\n1. Determine which service is using the port by running:\n   \n   ```bash\nsudo lsof -i :5432\n```\n   \n2. Stop the service that is using the port:\n   \n   ```bash\nsudo service postgresql stop\n```\n\nOption 2: Map to a Different Port\n\nFor a more long-term solution, consider mapping to a different port:\n\n- Map local port 5433 to container port 5432 in your Docker configuration (Dockerfile or docker-compose.yml).\n- If using a VM, ensure that port 5433 is forwarded in the host machine configuration.\n\nThis approach prevents conflicts and allows the Docker Postgres container to run without interruption."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "How to configure and run a Python script in Kestra Docker on Windows 11 without the 'Could not find or install 'Python 3.13' path' error?",
    "positive": "The issue comes down to how Unix processes produce output and how Kestra interprets it:\n\n1. Python's logging module writes to stderr by default. If you, for instance, call logging.basicConfig() without specifying a stream argument, the root handler sends basically everything to stderr.\n\n2. Kestra maps the two standard streams to its own log levels. Anything the container writes to stdout becomes a Kestra DEBUG entry and anything written on stderr becomes ERROR. There is no middle ground.\n\nThe fix\n\nGood news is that the fix is simple: redirect Python logging to stdout:\n\n```python\nimport sys\nimport logging\n\nlogging.basicConfig(\n    level=logging.INFO,\n    format=\"%(asctime)s %(levelname)s %(message)s\",\n    stream=sys.stdout,\n)\n```\n\nThat single stream=sys.stdout argument is enough. After the change, your informational messages will show up as DEBUG in Kestra.",
    "hard_negatives": [
      "Kestra supports conditional branching within a single workflow to reuse shared orchestration logic while allowing dataset-specific behavior. This approach reduces duplication, keeps related steps together, and makes execution paths explicit within a single workflow definition.\n\nWhen to use a single-workflow branch:\n- Reuse common steps (e.g., data validation, enrichment) across datasets.\n- Implement dataset-specific paths (e.g., Yellow vs Green taxi) within the same flow.\n\nHow to implement:\n- Use a branching mechanism in Kestra (e.g., a condition or switch node) to evaluate a runtime variable (such as the dataset type) and route to the appropriate sub-path.\n- Place shared steps before the branch; place dataset-specific steps inside each branch.\n- Decide how branches converge: continue with a common tail after the branch or treat branches as separate end states.\n\nBest practices:\n- Keep branches modular and minimal; extract shared logic into subflows where possible.\n- Centralize error handling and logging to ensure consistency across branches.\n- Test each branch with representative inputs and monitor branch-specific failures separately.\n\nExample (illustrative pseudo YAML):\n```- id: route_by_dataset\n  type: branch\n  when:\n    - condition: ${workflow.variables.dataset_type == 'yellow'}\n      then:\n        - - id: yellow_tasks\n            tasks: [ yellow_validate, yellow_transform ]\n    - else:\n        - - id: green_tasks\n            tasks: [ green_validate, green_transform ]\n- id: end\n  type: terminate\n```\n\nNote: Adapt the exact branch/task names to your Kestra workflow definitions. The key idea is to place shared logic before the branch, branch on dataset/type, and then execute dataset-specific steps within each branch.",
      "You might have installed Docker via snap. Run the following command to verify:\n\n```bash\nsudo snap status docker\n```\n\nIf you receive the response:\n\n```error: unknown command \"status\", see 'snap help'.\n```\n\nThen uninstall Docker and install it via the official website.\n\nError message: \"Bind for 0.0.0.0:5432 failed: port is already allocated.\"",
      "When you log into PgAdmin and see an empty database, the following solution can help:\n\nRun:\n   \n```bash\ndocker-compose up\n```\n\nAnd at the same time run:\n\n```bash\ndocker build -t taxi_ingest:v001 .\n\n# NETWORK NAME IS THE SAME AS THAT CREATED BY DOCKER COMPOSE\ndocker run -it \\\n  --network=pg-network \\\n  taxi_ingest:v001 \\\n  --user=postgres \\\n  --password=postgres \\\n  --host=db \\\n  --port=5432 \\\n  --db=ny_taxi \\\n  --table_name=green_tripdata \\\n  --url=${URL}\n```\n\nIt's important to use the same --network as stated in the docker-compose.yaml file.\n\nThe docker-compose.yaml file might not specify a network, as shown below:\n\n```yaml\nservices:\n  db:\n    container_name: postgres\n    image: postgres:17-alpine\n    environment:\n      ...\n    ports:\n      - '5433:5432'\n    volumes:\n      - ...\n  pgadmin:\n    container_name: pgadmin\n    image: dpage/pgadmin4:latest\n    environment:\n      ...\n    ports:\n      - '8080:80'\n    volumes:\n      - ...\n\nvolumes:\n  vol-pgdata:\n    name: vol-pgdata\n  vol-pgadmin_data:\n    name: vol-pgadmin_data\n```\n\nIf the network name is not specified, it is generated automatically: The name of the directory containing the docker-compose.yaml file in lowercase + _default.\n\nYou can find the network’s name when running docker-compose up:\n\n```pg-database Pulling pg-database Pulled \nNetwork week_1_default  Creating\nNetwork week_1_default  Created\n```",
      "If you are using MS VS Code and running gcloud in WSL2, when you first try to login to GCP via the gcloud CLI with gcloud auth application-default login, you may encounter an issue where the terminal prints a long OAuth URL and then shows a series of \"not found\" errors for browsers:\n\n```Your browser has been opened to visit:\n    https://accounts.google.com/o/oauth2/auth?response_type=code&client_id=...\n\n/usr/bin/xdg-open: 882: x-www-browser: not found\n/usr/bin/xdg-open: 882: firefox: not found\n/usr/bin/xdg-open: 882: chromium: not found\n...\nxdg-open: no method available for opening '...'\n```\n\nVS Code may show a notification: \"Your application running on port 8085 is available\" with an \"Open in Browser\" button. Clicking it may lead to an error page.\n\nSolution:\n\n1. Hover over the long OAuth URL in the terminal output.\n2. Ctrl + Click the link — VS Code will show a dialog: \"Do you want Code to open the external website?\" with buttons Open, Copy, Configure Trusted Domains, and Cancel.\n3. Click Configure Trusted Domains.\n4. A dropdown will appear with options like:\n   - \"Trust https://accounts.google.com\"\n   - \"Trust google.com and all its subdomains\"\n   - \"Trust all domains (disables link protection)\"\n   - \"Manage Trusted Domains\"\n5. Pick the first or second entry (e.g., \"Trust https://accounts.google.com\").\n\nNext time you run gcloud auth, the login page should pop up via the default browser without issues.",
      "When attempting to start the Docker Postgres container, you may encounter the error message:\n\n```Error - postgres port is already in use.\n```\n\nOption 1: Identify and Stop the Service\n\n1. Determine which service is using the port by running:\n   \n   ```bash\nsudo lsof -i :5432\n```\n   \n2. Stop the service that is using the port:\n   \n   ```bash\nsudo service postgresql stop\n```\n\nOption 2: Map to a Different Port\n\nFor a more long-term solution, consider mapping to a different port:\n\n- Map local port 5433 to container port 5432 in your Docker configuration (Dockerfile or docker-compose.yml).\n- If using a VM, ensure that port 5433 is forwarded in the host machine configuration.\n\nThis approach prevents conflicts and allows the Docker Postgres container to run without interruption."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "Setup: Failed to clone repository.",
    "positive": "To enable CI jobs (running dbt on pull requests), dbt Cloud needs a native integration with GitHub, GitLab, or Azure DevOps — the generic \"Git Clone\" / SSH connection method does not unlock the \"Run on Pull Requests\" trigger and you'll see:\n\n```Triggered by pull requests\nThis feature is only available for dbt repositories connected through dbt Cloud's native integration with Github, Gitlab, or Azure DevOps\n```\n\nThe dbt Cloud Developer plan also doesn't support CI jobs — you need Team or Enterprise. (See the dbt Cloud CI prerequisites.)\n\nSwitch from Git Clone to native GitHub integration\n\n1. Connect your GitHub account in dbt Cloud: Profile Settings → Linked Accounts → connect GitHub and grant the requested permissions. (dbt docs)\n\n2. Disconnect the existing Git Clone connection: Account Settings → Projects → your project → Repository → Disconnect.\n\n3. Reconfigure with the GitHub option, selecting the repository.\n\n4. (If your dbt project lives in a subfolder of the repo) set Project Subdirectory in the project settings to point to that folder.\n\n5. In Deploy → Job → Configuration → Triggers, you should now see \"Run on Pull Requests\" as an available toggle.\n\nCI job runs but says \"valid dbt project was not found\"\n\nUsually one of:\n\n- Project is in a subfolder, but Project Subdirectory isn't set.\n- The CI environment is configured to run on a custom branch that doesn't exist.\n- Uncommitted/unpushed changes.\n\nIn Deploy → Environments → your environment → Settings, set \"Custom branch\" if you're not on the default branch, and confirm it matches a real branch in GitHub.",
    "hard_negatives": [
      "Error:\n\n```plaintext\nThis project does not have a development environment configured. Please create a development environment and configure your development credentials to use the dbt IDE.\n```\n\nThe error message provides guidance on resolving this issue. Follow the guide in the dbt cloud setup documentation. Additional instructions can be found in the video @1:42.",
      "In my setup, all of the dependencies listed in build.gradle were not installed in -1.0-SNAPSHOT.jar.\n\nSolution:\n\n1. In the build.gradle file, add the following at the end:\n   \n   ```groovy\nshadowJar {\n       archiveBaseName = \"java-kafka-rides\"\n       archiveClassifier = ''\n   }\n```\n\n2. In the command line, run:\n   \n   ```bash\ngradle shadowjar\n```\n\n3. Execute the script from java-kafka-rides-1.0-SNAPSHOT.jar created by the shadowjar.",
      "You might have installed Docker via snap. Run the following command to verify:\n\n```bash\nsudo snap status docker\n```\n\nIf you receive the response:\n\n```error: unknown command \"status\", see 'snap help'.\n```\n\nThen uninstall Docker and install it via the official website.\n\nError message: \"Bind for 0.0.0.0:5432 failed: port is already allocated.\"",
      "- Star the repository.\n- Share it with friends if you find it useful.\n- Create a pull request (PR) if you can improve the text or structure of the repository.\n- Update this FAQ.",
      "When running terraform destroy, the following error can occur:\n\n```Do you really want to destroy all resources?\n\nTerraform will destroy all your managed infrastructure, as shown above.\n\nThere is no undo. Only 'yes' will be accepted to confirm.\n\nEnter a value: yes\n\ngoogle_bigquery_dataset.homework_dataset: Destroying... [id=projects/terraform-demo-449214/datasets/homework_dataset]\n\n╷\n\n│ Error: Error when reading or editing Dataset: googleapi: Error 400: Dataset terraform-demo-449214:homework_dataset is still in use, resourceInUse\n```\n\nThis is because the dataset is still in use by a table. To delete the dataset, set the delete_contents_on_destroy property to true in the main.tf file."
    ],
    "model_used": "MiniLM-L6"
  },
  {
    "query": "Why does casting TIMESTAMP_NTZ to BIGINT fail in Spark, and how can I convert it to a numeric value?",
    "positive": "Spark processes data in partitions. When you write a DataFrame to disk, Spark writes each partition as a separate output file. For example:\n\n```python\ntrips.repartition(4).write.parquet(\"output/\")\n```\n\nThis creates four parquet files because the DataFrame now has four partitions. This behavior enables Spark to write data in parallel and can improve performance on large datasets.",
    "hard_negatives": [
      "To read from multiple topics in the same Spark session, follow these steps:\n\n1. Initiate a Spark Session:\n   \n   ```python\nspark = (SparkSession\n       .builder\n       .appName(app_name)\n       .master(master=master)\n       .getOrCreate())\n   \n   spark.streams.resetTerminated()\n```\n\n2. Read Streams from Multiple Topics:\n   \n   ```python\nquery1 = spark\n       .readStream\n       ...\n       ...\n       .load()\n   \n   query2 = spark\n       .readStream\n       ...\n       ...\n       .load()\n   \n   query3 = spark\n       .readStream\n       ...\n       ...\n       .load()\n```\n\n3. Start the Queries:\n   \n   ```python\nquery1.start()\n   query2.start()\n   query3.start()\n```\n\n4. Await Termination:\n   \n   ```python\nspark.streams.awaitAnyTermination()  # Waits for any one of the queries to receive a kill signal or error failure. This is asynchronous.\n```\n\n   Note: query3.start().awaitTermination() is a blocking call. It works well when we are reading only from one topic.",
      "This issue occurs when attempting to connect to a GCP VM using VSCode on a Windows machine. You can resolve it by changing a registry value in the registry editor.\n\nOpen the Run command window:\n- Use the shortcut keys Windows + R, or\n- Right-click \"Start\" and click \"Run\".\n\nOpen the Registry Editor:\n- Type regedit in the Run command window, then press Enter.\n\nChange the registry value:\n- Navigate to HKEY_CURRENT_USER\\Software\\Microsoft\\Command Processor.\n- Change the \"Autorun\" value from \"if exists\" to a blank.\n\nAlternatively, you can delete the saved fingerprint within the known_hosts file:\n\nIn Windows, locate the file at C:\\Users\\\\.ssh\\known_hosts and remove the entry for the server.",
      "See a list of datasets here: GitHub Datasets",
      "Many people aren’t getting an exact match, but are very close to one of the options. It is suggested to choose the closest option.",
      "Remove the dataset from BigQuery created by dbt and run again (with test disabled) to ensure the dataset created has all the rows.\n\nDBT: Why am I getting a new dataset after running my CI/CD Job? / What is this new dbt dataset in BigQuery?\n\nWhen you create the CI/CD job, under 'Compare Changes against an environment (Deferral)' make sure that you select 'No; do not defer to another environment'. Otherwise, dbt won’t merge your dev models into production models; it will create a new environment called ‘dbt_cloud_pr_number of pull request’"
    ],
    "model_used": "MiniLM-L6"
  }
]